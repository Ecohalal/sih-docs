# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
.claude/

# Temp files
*.tmp
*~

# Database dumps
*.bkp
*.sql
# Excecao: scripts de carga/correcao curados, que precisam de rastreabilidade.
# A regra acima existe para DUMPS (volumosos, com dado de producao). Ela estava
# jogando fora tambem os scripts que o Renato roda no DBeaver — a divida
# "cargas de dado sem nenhum hash" registrada no §4.2 do BACKLOG-ECOHALAL.
# Nao colocar dump nem dado sensivel aqui: e diretorio de script versionado.
!PLANNING/sql/*.sql

# Test output
test-*.pdf

# Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Fontes externas da FAMBRAS (planilhas FM 7.4.2.7, .rar, docx/pdf de reuniao).
# ~21 MB de binario que nao versionamos aqui — mesma politica das demais fontes
# externas (§8 do BACKLOG-ECOHALAL: C:\HalalSphere\, C:\SIH\).
# Ignorado para o `git status` nao mentir: pasta suja permanente vira ruido e
# faz WIP real passar despercebido.
# ATENCAO: sem versionamento, estes arquivos existem SO nesta maquina.
Reuniões/
