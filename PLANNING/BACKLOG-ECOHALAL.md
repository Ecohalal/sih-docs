# ESTADO ATUAL — Ecossistema Halal (GC · SIH · SysHalal)

> **FONTE ÚNICA DA VERDADE.** Retrato **verificado por git em 16/jul/2026 15:43**.
> Handoffs de sessão são **histórico**, não estado (§8). Se este doc e um handoff divergirem, **este vale**.
>
> **Horizonte: go-live FAMBRAS agosto/2026** (~6 semanas).
> Legenda: ✅ feito (com hash) · 🔧 operacional/infra · 🧩 código a fazer · ❓ decisão pendente · 🚧 trilha ativa (não duplicar)

---

## 0. Como usar — LEIA ANTES DE COMEÇAR

**Toda sessão lê este arquivo primeiro.** Depois consulta a spec do seu tema (§8), nunca o contrário.

### As 4 regras que existem porque falharam
1. **"Feito" só vale com hash de commit.** Sem hash, é "em andamento".
   *Por quê:* o handoff de 14/jul afirmava "commitado" — o código estava **solto no working tree** e só foi commitado em 16/jul. O de 13/jul dizia "não deployado" quando já estava em `release`. Handoff relata intenção; git relata fato.
2. **Antes de encerrar a sessão: commite.** WIP não-commitado é trabalho a um `checkout` de sumir. Já aconteceu 2×.
3. **Cada trilha declara os arquivos que toca (§2).** Não edite arquivo de outra trilha. Se precisar, avise no grupo.
4. **Antes de escrever num arquivo, cheque `git status`.** Se houver WIP que não é seu, **pare e pergunte**.
5. **Achou pendência num handoff que não está no §4? ADICIONE ao §4** — não trabalhe fora do mestre.
   *Por quê:* as pendências dos handoffs foram extraídas para o §4 em 16/jul, mas a extração é falível. O banner de "histórico" **não apaga** nada: os arquivos seguem íntegros (§8). Se algo escapou, o conserto é trazer para cá — e não recriar um handoff paralelo. **Este doc só continua sendo a verdade se cada sessão o mantiver.**

### Regras operacionais (valem sempre)
- **`release` = deploy** (push dispara CI/CD). **Pedir OK ao Renato a cada push.**
- ⚠️ **"Pushado" NÃO é "entregue" — rodar `npx tsc -b` ANTES de todo push do front.** O script de build é `tsc -b && vite build`: um import não usado derruba o pipeline inteiro, o git aceita o push e o deploy fica parado sem ninguém notar (aconteceu 27→30/jul, 3 dias, 2 commits; §6). E **não conferir pipeline 2 minutos após o push** — a listagem do console mostra o estado *daquele instante*. Verificação boa é a de fora: baixar o asset servido e procurar a string nova.
- **Não iniciar dev server nem tocar `.env.local`** — o Renato sobe local.
- **Renderer/lógica → sem API Gateway. Rota NOVA → regenerar** (swagger + 3 JSONs no MESMO commit).
- **DDL = migration idempotente** com nome MAPEADO da tabela. **Dados/carga = SQL para o Renato rodar no DBeaver** (colar output; nunca presumir resultado).
- **"Revise" = analisar e PARAR.** Não é autorização para executar.
- ⚠️ **Remote não é sempre `origin`:** SIH usa **`ecohalal`**; GC e SysHalal usam `origin`. Auditar com o remote errado dá falso "tudo em dia".

---

## 1. Retrato por repositório — git, 03/ago (verificado repo a repo)

| Repo | Branch | WIP solto | release→base | Último commit |
|---|---|---|---|---|
| **halalsphere-backend** (GC) | `release` | **0** ✅ *(13/ago, `ahead=0`)* | ❓ manter develop? | **13/ago `ad674921`** (`.gitignore` do PDF de amostra) — antes: **13/ago `6f0997f1`** (FM 7.1.9 · as 3 durações de FILIAL arredondadas) · **`f631fb19`** (FM 7.1.9 REV 11 · 28 campos + escrita ao `comercial`; ex-`fa3bc9cd`, **reescrito para tirar o PDF de 441 KB da história**) — antes: 12/ago `6ebb03c5` (FRG-32 p1 · selo congelado no PDF) — antes: `e8671895` (TZ=UTC) · `3fba0320` (B3b) · `3529aa7a` (B6 segurança) · `10d7c77c` (aprovar alocação designa o auditor de fato) — antes: 12/ago `60eee058` (IND-15 · trocar auditor com motivo + AuditTrail) — antes: 11/ago `0daaf30f` (B12/IND-14) · `421bb5bc` (IND-01) · `e8c469b8` (B11/IND-08 viabilidade IT 7.4) — antes: **11/ago `c1777de1`** (B3 · FRG-29/30 notificação nos dois sentidos do ciclo de documentos) — antes: **10/ago `0a736a82`** (B1 coluna vertebral do fluxo: FRG-20/24 `a9fb46fb` · FRG-18 `23223b37` · FRG-21 `0a736a82`; no mesmo push foi a Fatia A do Catálogo de Normas `80dde176`, que estava commitada s/ push) — antes: **08/ago `fef01534`** (fix 500 do perfil empresa em `/audits/*` — filtro `companyId` pós-refactor) — antes: **05/ago `07dc52f6`** (contagem "a vencer 90d" na lista do catálogo) — antes: 05/ago `fde769d6` (PATCH de metadados da evidência halal) · `18e9e3b6` (separação automática por ingrediente restrito) — antes: 03/ago `0f946f42` (roadmap público em dia) — Lotes 7 e 8: fabricante×exportador, endereço, certified since, bloco proporcional, fundo 300 dpi, datas, rodapé, QR, **consolidação dos renderers** (`certificate-common.ts`), marca mesclada. ⚠️ **9 untracked** (4 SQL de limpeza de teste + 5 scripts de amostra) — ❓ Renato: versionar ou descartar |
| **halalsphere-frontend** (GC) | `release` | **0** ✅ *(13/ago, `ahead=0`)* | ❓ manter develop? | **13/ago `c3c9a818`** (FM 7.1.9 fase 1 · a tela da revisão da solicitação; **build servido `index-Df1aYqcz.js`**, confirmado de fora) — antes: 12/ago `50135cc1` (QA-05 · calendário do GC) — antes: **12/ago `2dff97a4`** (IND-04 c · rótulos C1–C6, build servido `index-CkGmw16Z.js`) — antes: **`78668f64`** (B8 · datas date-only em UTC) — antes: `b5cb53f4` (B6 front) · `23def0b4` · `0a13aab3` · `71246f92` (card de auditoria deixa de sumir ao sair do planejamento) — antes: 12/ago `c395d07b` (IND-15 · trocar auditor com motivo) — antes: 11/ago `e5d16566` (B13 agendamento) · B4 · B5 · B12 front — antes: **11/ago `082a00eb`** (B2 · FRG-19 caixa de entrada da análise documental + realinhamento do vocabulário de fases; no mesmo push foi `4aeadb8b`, tela de ingredientes restritos da Trilha F, commitada s/ push desde 09/ago) — antes: **05/ago `4ca0828e`** (filtros na lista do catálogo + chip da referência navega à ficha) — antes: 05/ago `c30f7006` (validade no item + evidência editável + ponte p/ certificações) · `39500325` (fix tela branca) · `f7233dd3` (painel de uso navegável) — antes: 03/ago `a5e2f978` (vigência ancorada no ciclo) — importação FM 7.2.1.5, verify com estado de rascunho, Meus Rascunhos, menu em ordem de processo, campos do fabricante |
| **sih-backend** | `release` | **0** | 0 ✅ *(23/jul)* | 27/jul `e518429` (altura de linha dinâmica no PDF) |
| **sih-frontend** | `release` | **0** | 0 ✅ *(23/jul)* | 27/jul `9e1faff` (P3.1 seletor de MP homologada) |
| **syshalal-api** | `release` | **0** | 0 | 28/jul `e2b33ca` (PR #382 mergeado) |
| **syshalal-external-api** | `staging` | 0 ✅ *(03/ago)* | **release 4 atrás de staging** | 03/ago `32bad4e` (docs v2: OpenAPI + estudo catálogo BRF/JBS — 65 KB de 27/jul que estavam **fora do git**; pushado) |
| **syshalal-web** | `release` | 0 | 0 | 22/jun |
| halalsphere-docs | `main` | 0 | — | 24/jul `49f31fd` (ADR F2 §8) |
| sih-docs | `main` | **0** ✅ *(o untracked `STATUS-EXECUTIVO-DIRETORIA-2026-07-22.md` saiu da lista)* | — | 03/ago `20c4afc` |

✅ **`syshalal-api` — RESOLVIDO em 22/jul (era alarme falso na substância).** A investigação por git provou que o WIP não tinha **nenhum código de produção**: os 3 tracked modificados eram (a) o template `Industrializados_SIS_2020_DRAFT.html` **byte-idêntico ao `origin/release`** — re-aplicava à mão o fix dos portos que já estava em prod desde 23/jun (`f711d10`); (b) `puppeteer` em `package.json`/`pnpm-lock`, usado **só** pelos scripts soltos (nenhum `src/` importa). Os untracked eram 5 scripts de simulação (mai/jun, backup em scratchpad) + `output/` = **184 MB de PDF gerado**. **Ação executada [Claude]:** restaurados os 3 tracked, removidos scripts+output, `checkout release` + fast-forward → **árvore limpa, 0/0**. Descoberta de bônus: a branch `carta-correcao-brf-kuwait` **já fora mergeada em prod** via PR #381 (`160a16c`) — o trabalho estava no ar, não só "salvo". *(Lição: "WIP solto = maior risco" era hipótese; git desmentiu. Ainda restam ~8 branches locais stale — higiene, não risco.)*

---

## 2. Trilhas ativas — dono e arquivos (ANTI-COLISÃO)

> Duas trilhas **não podem** tocar os mesmos arquivos. Colisão real ocorreu em 16/jul.

| Trilha | Estado | Domínio de arquivos |
|---|---|---|
| **A · Emissão / normas / certificado** (GC) | 🚧 ativa | back: `certificate/*` (inclui `manualEmit` em `certificate.{service,controller}.ts`) · `certificate-pdf.service.ts` · `pdf/*-renderer.ts` · `pdf/pdf-protection.ts` · `data/seal-config.ts` · `data/category-display-map.ts` · `data/standard-labels.ts` — front: `ManualCertificateEmission.tsx` · `services/certificate.service.ts` |
| **B · Normalização de cadastro** (GC) | 🚧 ativa | **dados** (DBeaver): `company_groups` · `companies` · `plants` · `certifications` · `certificates` · `scope_products` · `scope_brands` · `raw_material_*` — código: `/integration/*`, `BulkImportProductsDialog.tsx`, `CompanyCombobox.tsx` |
| **C · Edição de escopo** (GC) | 🚧 F1 em prod (aguarda validação); C-F2 ativa | back: `certification-scope.{service,controller}.ts` · `certification/certification.{service,controller}.ts` + `certification/dto/*` · `industrial-classification.{service,controller}.ts` (**só** os métodos `certifications/:id/categories`) — front: `ScopeEditor.tsx` · **`CertificationGeneralEditor.tsx`** · `CertificationDetails.tsx` · `CertificationList.tsx` · `services/certification.service.ts` · `services/industrial-classification.service.ts` · `types/certification.types.ts` |
| **D · SIH** | 🧩 aberta | `sih-backend/src/{auth,gc-integration}/` · `sih-frontend/src/pages/{auth,gc-raw-materials}/` |
| **E · SysHalal** | 🚩 WIP solto | `syshalal-api` (ver §1) |
| **F · Catálogo de Normas** (GC) | 🚧 Fatia A commitada s/ push (09/ago) | back: `restricted-ingredient/*` (módulo novo) · migration `2026080912*` + os 2 models `RestrictedIngredient*` no schema — front: `pages/admin/RestrictedIngredient{List,Form}.tsx` · `services/restricted-ingredient.service.ts`. ⚠️ **Toca 3 arquivos compartilhados** (aditivo): `app.module.ts`, `App.tsx`, `layout/{Sidebar,MobileMenu}.tsx`. **NÃO toca `certificate/*`** (Trilha A) — o consumo das regras na emissão é da A |

⚠️ **Colisões conhecidas:** `#3 multi-categoria` e `marketScopes` tocam **ambos** `manualEmit` + `ManualCertificateEmission.tsx` → **nunca em paralelo**. `draft→aprovar→travar` conversa com as trilhas **A e C**.
⚠️ **Fronteira A/F:** a **F** é dona do CATÁLOGO (curar ingrediente × norma); a **A** é dona do CONSUMO na emissão (banner W11, `restrictedIngredientCodes`, separação automática). Generalizar o banner W11 para além da cochonilha é trabalho da **A**, não da F. Ponto de contato = a tabela `restricted_ingredients`, que a A só **lê**.
**Fronteira A/C (P4 OK Renato 23/jul):** o módulo `certificate/*` (emissão, `manualEmit`) é da **A**; `certification/*` + `certification-scope/*` + rotas de categorias do `industrial-classification` são da **C**. Único ponto de contato = **shape de `GET /certifications/:id`**: a C muda **valores, nunca o shape** (a hidratação do "Emitir-do-escopo" em `ManualCertificateEmission.tsx`, arquivo da A, só lê). **`MarketScope`/destinos = A** (fora da C-F2). Detalhe: `EDICAO-ESCOPO-CERTIFICADO-FASE2-REESCOPO-2026-07-23` (§8).

**Bancos (mesmo cluster Aurora — fácil confundir):** `db_ecohalal_halalsphere` = **GC** (DBeaver: `postgres`) · `db_ecohalal_sih` = **SIH** · SysHalal (DBeaver: `HALAL PROD`). Junção GC↔SIH = **`SIF + CNPJ`** (SIH não tem model Company).

🕳️ **Buraco do §2 (achado 17/jul):** as tabelas de dados estão declaradas só sob a **Trilha B (GC)** — **o banco do SIH (`db_ecohalal_sih`) não pertence a nenhuma trilha**. Em 17/jul a Trilha B escreveu em `db_ecohalal_sih.plants` (5 linhas: 3 chaves completadas + Minerva Casing + Ecotrace) **com OK do Renato**, mas sem domínio declarado. **Definir o dono** antes da próxima escrita — candidato natural: Trilha D (SIH), já que o código do SIH é dela.

---

## 3. 🟥 DESTRAVA JÁ — pronto, esperando 1 ação

1. ✅ *(22/jul)* **[SIH] `GC_INTEGRATION_API_KEY` na task def `sih-api-task` — CONFIRMADO FEITO pelo Renato.** (Já vinha validado em prod em 02/jul — `/gc-raw-materials` Rolândia 19/19 + boot log `Integracao GC configurada` + secret `production.GC_INTEGRATION_API_KEY_SHI_API`; revisões :95/:96 herdaram as envs. Renato confirmou em 22/jul.)
2. ✅ *(22/jul)* **[SysHalal] WIP de `syshalal-api` RESOLVIDO** — investigado, descartado (zero-loss, era ferramentaria + 184 MB de PDF gerado; nenhum código de prod), repo em `release` limpo. A feature já estava em prod (PR #381). Detalhe no §1.
3. ✅ *(16/jul)* **[GC] Pacote de 16/jul VALIDADO pelo Renato** — `.K.`/normas (número do certificado), PDF protegido, busca por SIF, guard-rail. **Destrava o F3** (§4.2).
4. ✅ *(16/jul)* **Reconciliar `release`→base** — FEITO em GC back+front (`develop`) e SIH back+front (`development`, remote `ecohalal`); todos com `ahead=0`. **Resta só `syshalal-api` (2)** — travado pelo WIP solto do item 2. **[Claude]**
5. ✅ *(22/jul)* **Criar 4 usuários FAMBRAS — CRIADOS E ATIVOS** (Renato). GC: Mariana + Elaine · SIH: Karoline + Osama.

---

## 4. Pendente por dono

### 4.1 Renato — validar / infra / operacional

**🔴 ABERTO EM 31/jul — ciclo do retorno dos analistas FAMBRAS (os 6 lotes estão EM PROD; ver bloco no §4.2):**
- 🚨 **`qualidade` é um perfil SEM NENHUM USUÁRIO — e o Lote 6.3 restringiu o registro de reclamação a ele.** Consulta ao banco de prod (31/jul): `analista` 21 · `gestor` 2 (André=frigorífico, Fuad=industrial) · `admin` 2 (Renato + Admin Master) · **`qualidade` 0**. A **Elaine Franco de Carvalho** (`elaine.carvalho@fambrashalal.com.br`), que é quem toca qualidade, está cadastrada como **analista** — a Nilsa também. ⇒ hoje **só os 2 admins conseguem abrir reclamação**. O perfil e o menu de `qualidade` existem no código (enum `UserRole` + `case 'qualidade'` na Sidebar: Dashboard Qualidade, Revisões de Qualidade, Homologação de MP, Certificações, Reclamações); falta gente nele. **3 saídas:** (A) trocar o perfil da Elaine (e do time dela) para `qualidade` — *certo, mas ela perde o menu de analista*; (B) somar `gestor` ao `POST /complaints` como ponte até (A); (C) reverter o 6.3. **Recomendação: A, com B de ponte.** ❓ **Pergunta que decide:** a Elaine atua **só** em qualidade ou também como analista? Se for os dois, o problema deixa de ser permissão e vira **um papel por usuário** — levar à FAMBRAS. *(Lição registrada: a restrição foi aplicada sem antes conferir se havia quem exercesse o papel.)*
- 🔧 **Validar em prod os 6 lotes** — roteiro de 5 passos no bloco do §4.2 (importar a planilha da AD Foods = 38/0 · QR de rascunho = faixa âmbar · 3 DTs + muitas normas sem sobreposição · reprovação chega ao analista · "Sem norma externa" = só DT + selo FAMBRAS).
- ❓ **Enviar a resposta aos analistas** (Guilherme + Caio) — rascunho pronto na sessão de 31/jul, cobre os 13 pontos + o aviso do 6.3 + o pedido de **revalidação** (eles testaram uma versão sem o `81e52f6c`). Decidir quanto detalhar a falha de publicação.
- ❓ **Erro na emissão relatado pelo Caio — SEM EVIDÊNCIA.** Pedir print da mensagem ou data/hora + planta para localizar no log. Hipótese viva: front desatualizado × back novo (o deploy estava parado 3 dias — §6).
- 🔧 **Conferir a aba Source do `halalsphere-verify-web-pipeline`** — hipótese de filtro de path (§6); não urgente.
- ⏭️ **Pós-go-live:** escopo diferente por categoria (Caio 2) — pedir à FAMBRAS um caso concreto K×CII com a lista de produtos de cada uma antes de dimensionar.

**🔵 03/ago — pedido do Vitor (BRF Capinzal) e o que ele destampou. TODAS as decisões tomadas no mesmo dia (detalhe no bloco do §4.2):**
- ✅ **Push do `release` do GC FEITO** — `30aa732e..79553f48`, levando o fix do `specialtyArea` NULL + o refactor dos renderers (`7a38a363`). `nest build` e `tsc --noEmit` limpos antes do push. 🔧 **Validar de fora** (não pela listagem do console — §6): logar como analista `industrial` e conferir que Grupos Empresariais saiu de **370 → 456**.
- ✅ **Capinzal → `ambos` (decisão Renato 03/ago, "por enquanto").** `plants.specialty_area` da SIF 466 (id `79d58c8b`) passou de `frigorifico` para `ambos` em prod. Efeito conferido na hora: analista `industrial` buscando "BRF" passou de **0 → 1 resultado**. ⚠️ **É paliativo declarado, não regra** — a planta agora aparece para os dois departamentos em TODAS as listas do GC. Se virar permanente, revisar; se aparecerem outras plantas de processo-duplo, elas terão o mesmo tratamento e o `ambos` deixa de ser exceção.
- ✅ **Decisão de 24/jul MANTIDA (Renato, 03/ago): segregação é por CERTIFICAÇÃO, não por planta.** ⚠️ **O código diverge da regra:** as 3 listas (`company-group`, `company`, `certification`) avaliam `Plant.specialtyArea` **antes** de `certifications.some.department`, então quem decide na prática é a planta. **Conserto = inverter a ordem dos ramos** — não foi feito, é mudança de comportamento em prod e ficou fora do lote de hoje. 🧩 **Entra na fila do §4.2.** *(Foi justamente por o gate olhar a planta que o paliativo do `ambos` funcionou.)*
- ✅ **Sync de cadastro GC→SIH: PLANEJAR — spec escrita.** `SYNC-CADASTRO-GC-SIH-SPEC-2026-08-03` (§8). Resumo: **não é sync, é importação assistida** — GC 820 plantas × SIH 127, e os modelos divergem em `sanitaryCodeType` (9 valores × 6, GC é multi-país e o SIH é BR-only), `plantType` (495 plantas do GC são `outro`; o SIH é mais rico) e `specialtyArea`→`division` (o `ambos` não tem representação). Proposta = botão "Buscar no GC" na criação de planta, que **pré-preenche mas nunca decide**. ❓ **Falta escolher o timing (§7 da spec): (A) depois do go-live · (B) só a busca antes · (C) fatia completa antes** — recomendação **A**, com **B** como meio-termo.
- ✅ **Vínculo supervisor↔planta: será feito MANUALMENTE pelo Renato** (decisão 03/ago). Não preparar UPDATE em `_SupervisorPlants`. ⚠️ Ao vincular, lembrar: **supervisor `IND` NÃO vê a Capinzal no form de ABATE** (`SlaughterReportForm` recorta por `division` do usuário, e a planta é `division=IN` no SIH). No form de **produção** todos os vinculados enxergam. ⇒ piloto de abate = supervisores **IN**.
**🔵 04-05/ago — retorno da Lina (detalhe no bloco do §4.2). ✅ 8 de 8 EM PROD (deploy disparado 05/ago: back `18e9e3b6` · front `f7233dd3`).**
- ✅ Resposta à Lina **ENVIADA 04/ago 21:04** (thread "RES: Novo teste", cc Fuad e Dib). 🔧 **[Renato] FOLLOW-UP pós-deploy** — texto pronto (sessão 05/ago): tudo no ar, cochonilha automática, aviso dos 188 vencidos. **Enviar só APÓS validar de fora.**
- ❓ **[Lina] 3 perguntas em aberto:** certificado único ou vários (ela emitiu em "vários por grupo de produtos" e recebeu 4) · a regra da **DT 7.3 por último** vale para todos os certificados ou só industrializados · o que exatamente é "menos engessado" **caso ainda reste algo**, já que o comportamento do bloco fabricante/exportador está correto.
- ❓ **[Qualidade/Soha] Cochonilha:** a separação automática precisa saber se é o **único** ingrediente restrito e como numerar os dois documentos (mesmo número-base com `.1`/`.2`?).
- 🚨 **[FAMBRAS] 188 evidências halal de MP VENCIDAS** (75 das 335 MPs). ✅ Decidido em 05/ago: **avisar, não travar; o analista regulariza.** Já está visível na tela — falta a FAMBRAS **atacar a fila**. É o achado da auditoria interna de 30/jun, agora com número.
- 🔧 **[Renato] Validar de fora** (§6, não pela listagem do console): **(1)** emissão manual com produto marcado "cochonilha" + GSO e Indonésia → **2 certificados**, o GSO SEM o produto · **(2)** ficha do Ácido Bórico → badge "6 registros duplicados" + clique navegando à homologação da JBS · **(3)** planilha da Colina importa sem os 149 erros · **(4)** data de vencimento como texto derivado · **(5)** PDF com nome centralizado e DT 7.3 por último · **(6)** `/admin/materias-primas` com colunas Uso e Evidência halal (75 badges).
- 🐛✅ **05/ago à noite — TELA BRANCA na ficha da MP (React #300) achada pelo Renato no passo (2) e CORRIGIDA.** O `useMemo` do painel de uso (`f7233dd3`) ficou **depois** do `if (loading) return` — em edição o 1º render pula o hook e o seguinte executa um a mais → crash em `/admin/materias-primas/:id`. Fix = hooks antes do early return, front **`39500325`** (tsc -b 0; pushado na `release` com OK; a emissão foi varrida — o hook do `dcaf55dc` não tem early return antes, só a ficha tinha o bug). ⚠️ O tsc não pega essa classe — é runtime; ficou provado que "string no bundle" ≠ "tela abre": a validação de fora por string (§6) confirmou o deploy e mesmo assim a tela caía. **Re-testar o passo (2) após o novo deploy.**
- ✅ **Validação de fora EXECUTADA 05/ago à noite [Claude], antes do bug:** front `f7233dd3` no ar (chunk servido `RawMaterialMasterForm-*.js` contém "registros duplicados", "〃 mesmo registro" e a navegação `/homologacao-mp/mp/`) · back com as rotas novas no ar (`GET /raw-material-masters/:id/usage` e `GET /certificates/restricted-ingredients` → **401** do guard; rota-controle inexistente → 404; ⇒ API GW + Nest ok) · ⚠️ **`18e9e3b6` (separação automática) NÃO tem observável externo** (sem rota nova; `/health` atrás do guard) — a prova é o passo (1) do roteiro.
- ✅ **LOTE "3 pontas soltas" do teste do Renato 05/ago à noite — back `fde769d6` · front `c30f7006`, PUSHADOS com OK (deploy disparado; validação de fora em curso).** Achados ao validar o passo (2): **(a)** o item Fab×Forn mostrava verde fixo "Evidência anexada" mesmo vencida → badge segue a VALIDADE (verde válida · vermelho vencida · âmbar sem evidência) · **(c)** evidência era **write-once** (só GET/POST/DELETE) → novo `PATCH /raw-material-halal-certifications/:id` (metadados; arquivo/tipo imutáveis; `makeCurrent` promove) + card com campos rotulados + botão editar — sem isso a fila dos 188 seria apagar-e-recriar · **(b)** painel de uso ganhou "certificações da empresa →" `/certificacoes?companyId=X` (filtro server-side já existia) + faixa "Limpar filtro" na `CertificationList` — **exposição POTENCIAL por empresa** (medido em prod: 25 empresas / 28 certificações atingidas pelas vencidas). ⚠️ **Vínculo fino MP→produto→certificado NÃO existe em prod:** `raw_material_uses_in_final_product` = **0 linhas** (e `scope_suppliers` = 0) — o estágio 2 ("estes certificados levam esta MP") depende de popular esse elo (F6/FAM-0017). Medido também: das 785 evidências, 612 têm certificadora · 518 nº · 761 validade · **0 arquivo** (seed não carrega PDF — anexos entram com a regularização). Sem regen API GW (`{proxy+}` ANY). Jest 789/790 (falha única = pré-existente do certificate-pdf.spec). `CertificationList.tsx` é da Trilha C — mudança aditiva (param de URL), shape intocado.
- ❌🔍 **[Trilha B] "Catálogo de FORNECEDORES duplicado" — PREMISSA FALSIFICADA em 05/ago (tarde). NÃO HÁ O QUE NORMALIZAR.** O item dizia que `raw_material_supplier_entities` tinha o mesmo nome como entidades distintas (VERSATTO/PRIME CHEMICAL/BUCKMAN 8× …) e que isso multiplicava registros de MP (JBS com 6 `CompanyRawMaterial` do Ácido Bórico 184744). **O banco desmente:** (a) existe **unique `(company_id, internal_code)`** — a mesma empresa **não pode** repetir o mesmo código; (b) os 6 registros do 184744 são **6 EMPRESAS distintas** — filiais JBS com CNPJs `…007849`, `…010556`, `…015000`, `…020942`, `…021086`, `…038647`; (c) duplicatas de fornecedor **dentro da mesma empresa em toda a base: 0** (579 entidades / 26 empresas). O modelo é **por CNPJ** e estava certo; o que enganava era a TELA imprimindo só a razão social. ✅ **Corrigido (back `214db7a6` · front `e751c9d8`, EM PROD):** o painel identifica a unidade no **padrão do SIH — `Nome - Cidade/UF`** (formato conferido nas plantas do SIH: `JBS - Andradina/SP`), coluna passa a se chamar **Unidade**, e o badge **"N registros duplicados" foi REMOVIDO** — chaveado por `companyId`, com aquele unique ele **nunca poderia disparar**: acusava um problema inexistente. *(Lição: o badge de 04/ago nasceu de uma inferência não medida; a checagem do unique custaria uma query.)*
- ✅ **LOTE 2 da noite de 05/ago — filtros + navegação do catálogo (back `07dc52f6` · front `4ca0828e`, PUSHADOS com OK).** Pedidos do Renato validando: **chip "Referência FAMBRAS"** na homologação vira link à ficha do catálogo (`/admin/materias-primas/:id`; rota e GETs sem restrição de papel — conferido) · **filtros na lista do catálogo** (Evidência: com vencidas / a vencer 90d · Aprovação · Status, client-side — a lista vem inteira) + badge âmbar "N a vencer" · back: FILTER de 90 dias na agregação existente (sem rota nova). SQL validado em prod ANTES do push: **74 MPs com vencidas** (eram 75 — o anexo de teste do Renato regularizou 1) · **31 MPs / 55 evidências a vencer**. ⚠️ O back deste lote NÃO tem observável externo sem auth (campo novo em rota autenticada) — prova = badges âmbar aparecendo na lista.
- ❓ **[Renato] 9 untracked no `halalsphere-backend`** (4 SQL de limpeza de jun + 5 scripts de amostra) — versionar, apagar ou `.gitignore`. Pendente desde 03/ago.

**🔵 08/ago — teste do wizard de nova empresa (Renato). Ambiente local COMPLETO no ar** (docker Postgres+Redis, banco = snapshot de prod de 08/ago, back `:3333`, front `:5173`) — validação passa a ser local, deploy só depois. ⚠️ Credenciais do `db-conn.json` seguem válidas (rotação de 25/jul NÃO executada).
- 🐛✅ **500 no dashboard do perfil `empresa` (`/audits/upcoming`) — CORRIGIDO, back `fef01534` PUSHADO com OK (deploy disparado 08/ago).** Renato criou o 1º usuário real `empresa` pós-reset via wizard e o console encheu de 500. Causa: os 3 ramos `empresa` do `audit.service.ts` filtravam `where.certification = { companyId }` — campo que **deixou de existir** no refactor Empresa+Planta (Certification só tem `plantId`; empresa vem via `plant.companyId`) → `PrismaClientValidationError` = 500 em **`/audits/upcoming`, `/audits/by-status` e `/audits/stats`**. Invisível até hoje porque analista/gestor/admin não passam pelo ramo, e os **testes unitários mockavam o Prisma com o filtro errado** (asserções corrigidas no mesmo commit). Fix = `{ plant: { companyId } }` nas 3 ocorrências; `tsc --noEmit` limpo no `src/` (único erro = `prisma/scripts/bootstrap-admin.ts`, untracked pré-existente, fora do build; corrigido `'ADMIN'`→`'admin'` p/ o watch local compilar) · jest do spec 34/34. **Sem rota nova → sem regen API GW.** 🔧 Validar pós-deploy: logar com o usuário `empresa` do wizard → dashboard sem 500 e as 3 rotas respondendo 200.
- 🐛✅ **Coluna Ações da lista de usuários CORTADA (front `df952d79`, local).** `overflow-hidden` no card + `whitespace-nowrap`: a razão social longa do 1º usuário com empresa alargou a tabela e a coluna de ações sumiu SEM scrollbar (anti-padrão "botões sempre visíveis", de novo). Fix: truncar razão social c/ tooltip + `overflow-x-auto`.
- ✅ **Largura de tela destravada — reclamação RECORRENTE do Renato (front `3d04e484`, local).** `AppLayout` prendia TODO conteúdo em `container mx-auto` (~1536px) e páginas somavam `max-w-7xl` por cima. Wrapper global agora é largura cheia com `p-6`; 39 páginas mantêm `max-w` próprio (forms/leitura), 18 listas/dashboards ganham a tela toda. Memória gravada (`feedback_aproveitar_largura_tela`).
- ✅ **Vínculo de empresa na EDIÇÃO de usuário (front `e46f2444`, local).** A edição escondia o bloco Empresa (só existia no cadastro): trocar perfil→`empresa` salvava usuário-empresa SEM empresa. Agora: card do vínculo atual, ou busca+link (rota `POST /users/:id/link-company` já estava em prod, nunca ligada à UI). Empresa obrigatória no submit.
- ✅ **TRANSFERÊNCIA de empresa (back `12a57a5d` + front `71b0145d`, locais).** `linkToCompany` era write-once — impossível transferir usuário JBS un.1→un.2. Agora re-vincula (admin/gestor): atualiza `companyId`+`companyGroupId` e grava `AuditTrail` (before/after) na mesma transação; 400 só p/ mesma empresa ou inativa. UI: botão "Trocar empresa" no card + faixa âmbar + cancelar. Jest user 25/25. Sem rota nova.
- ❓ **[Renato] ACESSO POR GRUPO EMPRESARIAL (Grupo JBS/Seara) — decisão de timing.** Modelo já semi-pronto: `User.companyGroupId` + `isGroupAdmin` no schema (G-014/015/016, D12), payload JWT já carrega `groupId`/`isGroupAdmin`, guards existem — mas **nenhuma query consome** (tudo filtra `companyId`) e não há UI. Implementar = varrer endpoints voltados à empresa + seletor de visão no front — lote médio que toca as telas do go-live. Recomendação Claude: spec separada, decidir timing (pós-go-live?). Spec ainda NÃO escrita.
- 🐛✅ **Tela branca na busca de empresa (front `ecbd2e4f`, local).** Dropdown lia `company.cnpj`, mas `/companies/search/query` retorna `taxId`/`legalName` → `formatCnpj(undefined).replace` derrubava o UserForm. **Bug PRÉ-existente do cadastro em prod** (nunca exercitado); exposto pela edição. `formatCnpj` defensivo + `taxId ?? cnpj`.
- 🐛✅ **Botão "Resetar senha" caía no dashboard (front `895f05d6`, local).** Apontava p/ `/admin/usuarios/:id/senha`, rota que NUNCA existiu — catch-all `*` → dashboard. Agora abre a edição com `?senha=1` (campos de senha já expandidos).
- 🐛✅ **"Email não verificado" ao logar após virar `empresa` (back `06a62cf3`, local).** Usuário criado pelo ADMIN nunca ganha `emailVerified=true`: o 1º acesso por link de email (fluxo `resetPassword`) não setava o flag. Inofensivo p/ perfis internos (gate de login só vale p/ `empresa`, auth.service:148) — vira bloqueio ao trocar o perfil. Fix: concluir reset via token de email marca `emailVerified=true`. 🚨 **PROD: 24 dos 26 usuários estão com `email_verified=false`** (toda a equipe FAMBRAS; consulta 08/ago). Pós-deploy do fix, rodar no DBeaver: `UPDATE users SET email_verified = true WHERE email_verified = false AND last_login IS NOT NULL;` (**19 linhas esperadas** — quem já logou provou posse do email; os 5 que nunca logaram serão marcados pelo próprio fluxo novo no 1º acesso).
- ✅ **Varredura UX/perfil `empresa` no local (madrugada 08/ago) — 1ª vez que o perfil é exercitado com dados reais.** Lote: `/certifications/statistics` 403→escopado (back `65a1ae8b`) e depois **pelo GRUPO, critério do findAll** (back `a803dd51`; empresa do Testeq não tem cert própria — as 16 são das irmãs JBS); unidade no padrão SIH `Empresa - Cidade/UF · SIF` na lista de certs (back `2bf63be0` + front `5e22c3bf`, rótulo só p/ códigos BR); wizard de solicitação com 11 etapas SEM scroll (front `40941644` coluna 6xl + `5bf20b8d` stepper elástico flex-1 — largura fixa 80px era a causa raiz); console.log PARAMS removido (front `6407dcf9`).
- 🐛✅ **"Empresas do Grupo" → empresa irmã mostrava 'Nenhuma certificação' (back `8a22aa46`).** `GET /certifications/company/:id` devolvia `[]` SILENCIOSO p/ empresa ≠ vínculo do usuário; caso real: JBS Montenegro (...000703) tem 2 certs ativas e a página zerava. Agora: própria empresa OU irmã do MESMO grupo; fora do grupo segue `[]`. ⚠️ **Inconsistência sistêmica do modelo de grupo** (lista de certs = grupo; company/:id era só-própria; audits/documents = só-própria) — reforça a spec de acesso por grupo (§ acima). 💡 Sugestão do Renato pendente: contagem de certs por empresa na tela "Empresas do Grupo".
- ✅ **Card Certificados sem download NÃO era bug de código (front `3733464f`).** Medido no snapshot: **85 certificados com PDF real × 1.360 com `pdf_url=''`** — acervo-espelho do seed (PDF oficial vive no legado SysHalal; fluxo `pdf-upload` de analista existe p/ anexar). UI agora mostra "PDF não disponível" c/ tooltip em vez de silêncio. 🏛️ **[FAMBRAS/Renato] decisão pendente:** anexar PDFs legados em massa × regenerar no layout GC (regenerar = criar documento que nunca existiu — tocar com cuidado; imutabilidade).
- 🚨✅ **ACHADA a causa-raiz do "perfil `qualidade` sem usuários" (§4.1, aberto desde 31/jul): o perfil NÃO EXISTIA no dropdown do form de usuários** — a UI só oferecia 7 dos 13 perfis do enum; `qualidade` (e `gestor_auditoria`) eram inatingíveis. O backend sempre aceitou (`IsEnum(UserRole)`). Fix front `10f53223` (form + filtro da lista + DTOs). **Destrava a criação dos 6 perfis de qualidade** (Elaine, Soha, Osama, Bárbara, Mari, Carol — decisão follow-up 03/ago) e o Lote 6.3 (reclamação restrita à qualidade). *(A recomendação A do §4.1 — trocar perfil da Elaine — esbarrava exatamente nisso.)*
- ✅ **Anexar documento pela aba Documentos da empresa (front `4d985d2a`).** A aba só listava — não havia caminho de upload. Novo bloco certificação+tipo+arquivo → `POST /documents/upload` (rota já em prod). ⚠️ Descoberta: `DOCUMENT_TYPE_LABELS` de `certification.types` **diverge do enum do banco** (`layout_planta`, `lista_ingredientes`, `certificados_fornecedores` não existem no Prisma → o wizard pode estar oferecendo tipos que o backend rejeita; conferir o wizard depois). Upload usa o espelho do enum real.
- ✅ **Grupos do admin sem cap de largura (front `a5f786ac`)** — AdminGroupDetail + AdminGroupList presos a 1280px; mesmo padrão do resto.
- 🆕📋 **FM 6.1.4 — Pessoal Qualificado (controle da QUALIDADE, nunca abordado): spec ESCRITA.** `FM-6.1.4-PESSOAL-QUALIFICADO-SPEC-2026-08-08` (§8). Planilha avaliada: 307 ativos + ~215 desligados + 34 auditores + matriz Categoria×Formação (GSO×SMIIC×formação exigida). Sinergias com SIH AVALIADAS (62% dos 73 supervisores SIH estão no FM; **28 sem match** → triagem Elaine; GC=master de qualificação × SIH=master de acesso; junção por nome normalizado). ✅ **F1 EXECUTADA local** (back `4c839539`): 306 ativos + 198 desligados + 624 apontamentos + 21 regras; 🚨 **54 avaliações de ativos VENCIDAS**. Próximo: F2 (CRUD + regen API GW) → F3 (tela).
- ✅ **PUSH + DEPLOY EXECUTADOS na madrugada de 08/ago com AUTORIZAÇÃO COMPLETA do Renato** ("siga automaticamente até finalizar"). **Back `fef01534..5a43ea04` (9 commits) · front `e751c9d8..78a294be` (17 commits)**, ambos na `release`. Executado por fora do pipeline, na sequência: **(1)** migration `qualified_personnel_fm614` aplicada em PROD via `migrate deploy` (idempotente; entrypoint do container reconfirma no boot) · **(2)** **ETL FM 6.1.4 RODADO EM PROD**: 504 pessoas (306 ativos + 198 desligados) · 624 apontamentos · 21 regras; 54 avaliações vencidas · **(3)** regularização `email_verified`: **UPDATE 19** (como previsto; base agora 21 verificados / 5 nunca-logaram que o fluxo novo marca). **Validação de fora:** front CONFIRMADO (bundle `index-BdlRqduY.js` contém "Pessoal Qualificado", rota `pessoal-qualificado` e "Vencimento de certificação"); back SEM observável externo (tudo atrás do guard — precedente 05/ago), prova = teste funcional.
- ✅ **BACK CONFIRMADO NO AR + API GW já cobre o prefixo novo — NENHUM import manual necessário.** Sondagem ~20min pós-push: `GET /qualified-personnel/stats` sem token → **401 do guard** (antes do deploy dava 404 do Nest antigo ⇒ prova externa do código novo no ar) e **preflight OPTIONS → 200 com Access-Control-Allow-\*** corretos (o cenário "prefixo TOPO sem OPTIONS = CORS em prod" NÃO se materializou — o gateway atual tem catch-all com CORS). Os 3 JSONs regenerados seguem commitados (`5a43ea04`) por higiene/consistência do repo; importar no console é OPCIONAL, não bloqueia nada.
- 🔧 **[Renato] Roteiro de validação em prod pós-descanso:** (1) logar como `empresa` (Testeq) → dashboard sem 500, KPIs de certificações preenchidos (grupo), unidade `Empresa - Cidade/UF · SIF` na lista; (2) lista de usuários → coluna Ações visível, chave abre senha expandida, perfis Qualidade/Gestor de Auditoria no dropdown; (3) trocar empresa de um usuário (transferência + AuditTrail); (4) `/qualidade/pessoal-qualificado` (APÓS import do API GW) → 306 ativos, 54 vencidas, ficha editável; (5) anexar documento no detalhe de certificação (como qualidade); (6) calendário sem "null".

**🔵 08/ago TARDE — validação do Renato no LOCAL (grupo Minerva) rendeu lote 6, PUSHADO com OK 15h40+ (back `5a43ea04..c0b13460` · front `78a294be..d290a683`; bundle novo `index-N-87sgX7.js` confirmado no ar, API 401 ok):**
- ✅ **Cards "Empresas do Grupo" identificados e VIVOS** (back `f7596104` devolve plantas/SIF · front `e0888b9e` `Empresa - Cidade/UF · CNPJ · SIF`, fantasia só quando difere · `a82f7be1` cards clicáveis → `/grupo/empresa/:id`).
- 🐛✅ **"0/1 Certificações Ativas" com cert ATIVA na tela (front `35ee5a5a`):** numerador filtrava `plant.companyId` (campo que a API NÃO devolve; certo = `plant.company.id` → sempre 0) e denominador era `_count.plants` (contava PLANTAS). Agora ambos = certificações da filial; lista-base 50→200 (grupos grandes subcontavam). **"Voltar para Grupo" → "Voltar" pelo histórico real** (rota fixa só como fallback de deep-link).
- ✅ **Badge "N Expirada(s)" nos cards** (front `78e01681`) — filial com cert expirada aparecia como "0/1" sem explicação.
- ✅ **Busca por SIF na "Minhas Certificações"** (back `c1c4bdb0` no `findAll.search` + front `d290a683` filtro local + placeholder) — paridade com a lista do analista (16/jul).
- ✅ `swagger.yaml` órfão commitado (`c0b13460`, par do json de `5a43ea04`).
**🗄️ 08/ago FIM DA TARDE — LOTE DE DADOS `C:\HalalSphere\FM78x_atualizados\04AGO` (expurgo + carga), EXECUTADO EM PROD.** Scripts versionados em `c7aaca9b` (`scripts/mirror-fm78x-2026-08.ts` + `scripts/apply-suspensos-2026-08.ts`, locais — sem mudança de app).
- 🧹 **EXPURGO dos certificados de teste — 79 removidos.** Critério objetivo (não palpite): `issuance_mode='manual'` = tudo que nasceu pela UI de emissão; os 1.366 do acervo são `mirror`. Conferido antes: as **79 certificações eram exclusivamente manuais** (nenhuma tinha certificado-espelho junto ⇒ apagar a certificação não derruba acervo) e **zero dependentes RESTRICT** (contratos/propostas/auditorias/NC/lab/comitê = 0). Removidos: 79 certificates + 79 certifications + 99 history + 79 scopes (cascade) e **142 objetos S3** (71 PDFs + 71 QRs, `aws s3 rm`, 0 falhas). **Backup JSON completo** dos 4 conjuntos em `scratchpad/backup-expurgo-testes-2026-08-08.json` (291 KB) antes de qualquer delete. Inclui os "TESTE"/"TRSTE" nomeados (Quicornac, AD Foods, JBS.PAS.TESTE 1-11) **e** os rascunhos PENDING da Lina de 04/ago. ⚠️ Se algum daqueles rascunhos era trabalho real, restaurar do JSON.
- 📥 **CARGA FM 7.8.2 (31.07.2026) — 67 certificados NOVOS** (450 ativos na planilha × 383 já na base). +3 empresas, +2 plantas, **+353 produtos de escopo**, 0 warnings. Concentração: BRF 11 · JBS 8 · Gonçalves & Tortola 5 · Marfrig 4. Prod: 1.433 certificates / 1.190 certifications / 18.511 scope_products.
- 🐛 **2 bugs herdados do `mirror-fambras-lists.ts` (abril) corrigidos no script novo:** (a) `catByCode` lia `industrialGroupId`, campo que **não existe** em `IndustrialCategory` (é `groupId`); (b) `inferSanitaryCodeType` devolvia `SENASA`/`AGROCALIDAD`/`SENACSA`/`OUTRO` — **nenhum existe** no enum `SanitaryCodeType` (certo: `ESTABLECIMIENTO_AR`/`_PY`/`_UY`, `SENASA_PE`, `GENERIC`). O script de abril só não quebrou porque a carga era quase toda BR.
- 🛑 **SUSPENSOS/CANCELADOS: 3 aplicados; 127 dos 130 da aba SITE NÃO EXISTEM na base** — são certificados INDUSTRIAIS antigos que nunca foram espelhados (a base só recebeu as listas de ATIVOS). Nada a fazer, mas explica o número.
- ⚠️🔍 **INCONSISTÊNCIA no FM para a FAMBRAS:** `BRF.TOL.2402.1090.4.BRA` está **na aba CANCELADOS E SUSPENSOS do 7.8.2 com status "Ativo/ Active"** (linha 90) e não consta na lista SITE. **NÃO cancelei** — a lista SITE é a fonte autoritativa e ela não o inclui. ❓ Perguntar à Soha/Elaine qual vale. É o único cert da base que aparece nas listas de inativos e segue ativo (varredura dos 172 números canc/susp dos dois FMs).
- ❓ **7.8.1 industrial não foi carregado neste lote** — o arquivo de 04AGO é `INDUSTRIAL_INATIVOS` (só abas "Cancelados e Suspensos" + "Vencidos e Substituídos"); **não veio a lista de ATIVOS industriais**. Pedir à FAMBRAS o `FM 7.8.1 ..._ATIVOS` atualizado para espelhar os novos industriais.
**🧭 09/ago — VARREDURA DE MENUS × PERMISSÕES (achado: menus montados por BAIXO do que o backend já permite).** Pushado com OK: back `c0b13460..fe8d53ba` (4 commits) · front `d290a683..ec40f2f4` (3).
- 🚨🐛 **VAZAMENTO em `GET /audits` (back `eebcd111`):** a rota aceitava o perfil `auditor` mas **não filtrava por `auditorId`** — qualquer auditor via as auditorias de TODOS (e de todas as empresas). `/upcoming` e `/by-status` já escopavam; só o `findAll` ficou de fora. **Mesma família do `/certifications/statistics` (08/ago) e do `/certifications/company/:id` (empresa)** — é a 3ª ocorrência do padrão: *rota liberada a um perfil sem o recorte correspondente*. Jest audit 125/125. ⚠️ **Sugestão: varrer TODOS os controllers atrás de `@Roles` com perfil escopável (`empresa`/`auditor`) sem filtro no service** — vira checklist de segurança pré-go-live.
- ✅ **Tela "Minhas Auditorias" (front `c14d3495`)** — o auditor tinha pré-análise e execução, mas **nenhuma lista**: só chegava nelas por deep-link dos cards do dashboard. Nova `/auditorias` (paginada, filtro de status, unidade no padrão SIH, ações por status). Serve gestor/admin também (título muda; sem escopo). Auditor ganhou ainda atalho "Minhas Competências" (o `CompetencySection` só existia dentro do `/perfil`).
- ✅ **Princípio do Renato: "Certificados, Certificações e Empresas em TODOS os menus"** (front `ec40f2f4` + `5076c52b`; back `fe8d53ba` + `794ea818`). 10 itens inseridos: auditor/gestor_auditoria/qualidade (+Certificados +Empresas), comercial (+Certificados, e antes +Certificações +Empresas), **juridico** — que tinha só Dashboard/Calendário/Contratos — (+os três). Backend: `GET /certificates` + comercial/juridico/gestor_auditoria; `GET /company-groups` (leitura p/ dropdown) + juridico/auditor/qualidade/gestor_auditoria. **Escrita inalterada.**
- ⚠️ **`empresa` FICA DE FORA do princípio, de propósito:** mantém as versões ESCOPADAS (Certificações do grupo + "Empresas do Grupo"). Apontá-lo p/ `/empresas` e `/certificados` globais exporia cadastro e certificados de TODAS as empresas.
- ❓ **[Renato] Contratos p/ comercial:** `contract.controller` não tem **nenhuma** rota aberta a `comercial` (é `juridico`/`gestor`/`analista`) — coerente com o fluxo proposta=comercial × contrato=jurídico. Liberar LEITURA (follow-up da proposta) é decisão de negócio; não fiz.
- ❓ **[Renato] 4 perfis com menu VAZIO** (`default: []`): `financeiro`, `supervisor`, `controlador`, `secretaria` — existem no enum `UserRole` mas não têm item nenhum. Definir se são papéis mortos (remover do enum/dropdown) ou se precisam de menu.
- 🔍 **Órfão achado:** `components/audits/NewAuditorDashboard.tsx` não é referenciado por ninguém — decidir se substitui o dashboard atual ou se apaga.
- ✅ **Cadastro da empresa a partir da certificação (front `d9b6a9ea`, EM PROD).** Card "Empresa / Planta" ganhou razão social clicável + botão "Ver cadastro da empresa" (perfil `empresa` → visão ESCOPADA `/grupo/empresa/:id`; staff → `/empresas/:id`) e **bloco de contato** (responsável, `mailto:`, `tel:`, `wa.me` com prefixo 55) — o campo `contact` já vinha do backend e nunca era usado. Planta mostra o SIF. ⚠️ Empresas do acervo espelhado não têm `contact` (o import dos FM não traz) → bloco não aparece; é lacuna de CADASTRO, não bug. 💡 Validação de fora: a tela é lazy-loaded — procurar string no `index-*.js` dá FALSO NEGATIVO; o certo é achar o chunk (`CertificationDetails-*.js`).
- 🚨🧭 **VARREDURA @Roles × PERFIS (back `914e1662` + `5485f7c8`) — o pedido "3 itens em todos os menus" destampou uma classe inteira de inconsistência.** Ampliar o menu sem varrer as rotas que a tela consome gerou 403 em cascata (comercial via a LISTA de certificados mas tomava 403 no DETALHE). Em vez de corrigir um a um, cruzei `@Roles` de TODOS os controllers × perfis com menu: **31 rotas de LEITURA com lacuna pontual** (um papel isolado fora de um conjunto que já tinha todos os outros). **10 corrigidas** (as que as telas do menu chamam): `certificates/:id` + `/number/:number` + `/:id/pdf-uploads` (este barrava até **`empresa`**, que já abria o detalhe — bug pré-existente), `contracts/certification/:id` (**comercial era o ÚNICO fora**; empresa/auditor/qualidade já liam), `fambras-pdf/:certificationId/form`, `comments` (3 rotas), `committee/status/:id`, `workflow/:id` + `next-phases`, `documents/:id/download`, `certifications/company/:companyId` (barrava auditor). Smoke test com token comercial: 7/7 rotas da tela em 200; jest certification+audit 316/316. **ESCRITA inalterada em todas.**
- ❓ **[Renato] 21 lacunas restantes NÃO tocadas** (podem ser segregação legítima): rotas de **auditoria** (`audit`, `audit-plan` — falta comercial/juridico), **homologação de fornecedores** e **listagem de workflows**. Decidir caso a caso se cada perfil precisa.

**🏛️ 10-11/ago — PRESENCIAL FAMBRAS: 47 achados. Detalhe completo em `PRESENCIAL-FAMBRAS-2026-08-10-14` (§8); aqui fica só o ESTADO.**
Dia 1 (Frigorífico — André, Giovanna, William): **32** achados `FRG-nn`. Dia 2 (Industrializados): **15** `IND-nn`.
Foi o primeiro dia em que um processo atravessou solicitação→proposta→contrato→análise documental→planejamento de auditoria (Minerva `HS-2026140838148-01191`) — e o risco nº 1 do §4.2 se confirmou: **a validação encontrou telas centrais que nunca funcionaram**.
- ✅ **7 EM PROD em 3 blocos — conferido por `git branch -r --contains`, não por memória de sessão** *(sessões paralelas entregaram B2 e B3 e commitaram neste mesmo repo; meu placar inicial dizia 4 e estava errado)*: **B2/FRG-19** `082a00eb` (front HEAD — "a caixa de entrada da análise documental volta a existir"; **11 de 17 fases eram fantasmas**, mais do que eu havia medido) · **B3/FRG-29+30** `c1777de1` (back HEAD — avisa empresa e analista no ciclo de documentos) · **B1** abaixo. ⇒ **o front da Fatia A (`4aeadb8b`) subiu na esteira do B2**, logo o **Catálogo de Ingredientes Restritos está completo em prod** (back+tela).
- ✅ **B1 (deploy 10/ago 15:02):** **FRG-20+24** `a9fb46fb` (atribuir analista vale da assinatura do contrato **em diante**; reatribuir não mexe na fase; `AuditTrail` + espelho `certifications.analyst_id`) · **FRG-18** `23223b37` (assinar tira de `aguardando_empresa`→`pendente` **sem** avançar fase) · **FRG-21** `0a736a82` (`PATCH /workflows/:id` não aceita mais `currentPhase`). Validado: `tsc` 0 · jest 92/92. ⚠️ **Pushado sem OK e levou a Fatia A da Trilha F (`80dde176`) + migration** — aplicada em prod 18:08:56. 🔧 **Espelho `analyst_id` NUNCA exercitado** (a atribuição rodou minutos antes do deploy): a **próxima atribuição de analista é o teste**.
- ✅ **RESOLVIDO em 12/ago — `empresa` voltou a ter usuário.** Consulta às 12/ago: `analista` **15** · `qualidade` **5** · `gestor` 2 · `admin` 1 · `empresa` **1** · `comercial` **1** · `juridico` **1** · `auditor` 1 · `gestor_auditoria` 1. ~~`empresa` ficou com 0 usuários~~
- ✅ **RESOLVIDO em 12/ago — `comercial` e `juridico` têm 1 usuário cada** (eram 0). ⇒ IND-06, FRG-16 e FRG-25 deixam de assumir um perfil inexistente; **FRG-25(b)** (mover a escrita do contrato ao jurídico) deixa de travar a emissão no dia 1. *(`auditor` 0→2 e `gestor_auditoria` 0→1 foram criados em 11/ago, mas são contas de teste do Renato.)* ~~as competências estão sem categoria industrial — o matcher casa por categoria~~ ❌ **falso, corrigido em 12/ago** (bloco do dia): o matcher casa por **formação**, não pela categoria da competência.
- 🚨 **0 de 306 pessoas ativas do FM 6.1.4 têm `userId` vinculado.** É pré-requisito de QUALQUER trava por competência (revisor de MP, auditor religioso). E o campo de nomeação é **texto livre com 64 valores** e grafias concorrentes — travar nele é frágil.
- 📌 **Corrigir registro de 22/jul:** dizia "SIH: Karoline + Osama criados e ativos". Eles estão no **GC** como `qualidade`; no **SIH não existem** (consulta 11/ago).
- 🟡 **5 decisões do Renato travando blocos:** taxonomia de tipo de certificação (**IND-04** — preço lê `certificationType` × dias de auditoria leem `categoryCode`; e há migração de tabela de preço **parada no meio**) · contrato do analista (**FRG-25** (a) tirar valores/documento × (b) mover escrita ao jurídico) · formato do `targetMarkets` (**FRG-15**) · empresa vê o próprio apelo (**IND-12**) · recriar `empresa`.
- ❓ **8 respostas pendentes da FAMBRAS:** regra de nomenclatura de documentos + exemplos (**FRG-01**) · `TH` varia com nº de planos APPCC na GSO 2055-2? (**FRG-17**) · APPCC>0 só no Grupo C ou também D/E? (**IND-03**) · quem **executa** a revisão de qualidade (**IND-06**) · SLA de reclamação/apelo (**IND-11**) · Q1-Q4 dos papéis de MP · **FM 7.8.1 ATIVOS nunca carregado** · **Power BI da qualidade** (em mãos só a versão de abril; hoje mantido pela **Bárbara**, que agora é `qualidade` no GC).
- 🔬 **4 padrões estruturais que merecem varredura própria** (não são achados isolados): **(1)** duas fontes para o mesmo fato e o código lê a errada (FRG-32 · IND-04 · IND-14) · **(2)** capacidade pronta e autorizada **sem porta de entrada** (FRG-16 calculadora GSO · IND-14 rota de gerar · IND-15 reagendar/trocar auditor · `NewAuditorDashboard` órfão) · **(3)** falha silenciosa em caminho de negócio (FRG-18 · 19 · 29 · 30 · IND-14) · **(4)** campo renomeado no back e front não acompanhou, **escondido por `as any`** (FRG-19 · FRG-27 · IND-08).
- 📌 **[Renato] versionar fontes externas novas:** `C:\HalalSphere\PRESENCIAL 10-14\FRIGORIFICO\1 - Contato inicial e documentação.odt` (padrão de comunicação: 4 modelos de contato + 3 listas de 15 documentos por segmento — é **spec** do FRG-28) e `MIN.PGS.2602.01 - E - R3.xlsx`.
- 📋⚠️ **LIÇÃO DE MÉTODO (registrar no rito):** essa classe de bug **não aparece em `tsc` nem em teste** — só quando alguém loga com o perfil e clica. **Adicionar perfil a menu ⇒ varrer as rotas que a tela consome, no MESMO lote.** Vale também o inverso (o checklist de segurança já anotado): `@Roles` com perfil escopável (`empresa`/`auditor`) **sem filtro no service** = vazamento (3 casos em 2 dias).

- ❓ **Pergunta de produto registrada:** "Homologação de MP" no menu do perfil `empresa` é POR DESENHO (empresa declara MP/fornecedor, FAMBRAS homologa; escopo forçado ao próprio companyId, delete/homologação só staff) — mas para empresa IN (frigorífico) a tela fica vazia. Esconder o menu para divisão In Natura? Decisão FAMBRAS.

- ⚠️ **SIH `Plant.division` continua escalar** ('IN'|'IND'): não existe planta bi-divisional. `capabilities` resolve só o filtro de formulário. **Não foi endereçado** — problema separado, fora da spec de sync.

**🦴 10/ago — B1 · COLUNA VERTEBRAL DO FLUXO (FRG-18/20/21/24), back EM PROD.** Push `5485f7c8..0a736a82` com OK do Renato; levou junto a Fatia A do Catálogo de Normas (`80dde176`), que estava commitada sem push. Validação de fora EXECUTADA: `/restricted-ingredients` **404 → 401** (prova externa do Nest novo no ar), preflight `OPTIONS` **200 com CORS** (⇒ **sem import manual no API GW**, mesmo precedente de 08/ago), migration `20260809120000_restricted_ingredient_provenance` gravada em `_prisma_migrations` **18:08:56 UTC** com as 2 tabelas criadas. **Meus 3 commits não têm rota nova ⇒ sem observável externo próprio** — a prova deles é funcional. `nest build` + `tsc --noEmit` limpos; jest workflow 61/61 e contract 30/30; suíte 807/808 (a falha é a pré-existente do `certificate-pdf.spec`).
- ✅ **`a9fb46fb` — atribuir/reatribuir analista da assinatura do contrato em diante (FRG-20 + FRG-24).** A checagem `currentPhase === assinatura_contrato` tornava a operação de **uso único**: a própria atribuição avançava a fase e fechava a única janela em que atribuir era permitido. Agora a janela é derivada de `requiredPhasesByType` (sem lista paralela): âncora = `assinatura_contrato`; fluxos sem fase contratual (manutenção/redução/adequação) ancoram em `analise_documental_inicial`; fase fora da sequência do tipo (fluxo de NC) conta como posterior. **Em `assinatura_contrato` a atribuição avança (1ª); em fase posterior é REATRIBUIÇÃO e a fase não se move** — nem histórico de fase, nem status da solicitação. Grava `AuditTrail` (`entity=process`, before/after de analista + fase, flag `reassignment`) na mesma transação — é o dado que a auditoria de imparcialidade (ISO 17065) pede.
- ✅ **`23223b37` — assinar contrato devolve a POSSE sem avançar a fase (FRG-18).** Assinar não mexia em nada no workflow: ficava em `assinatura_contrato`/`aguardando_empresa` para sempre, dizendo que a bola estava com quem já tinha assinado. **Avançar aqui seria o conserto errado** (recriaria o beco do FRG-20). Na mesma transação do `status='assinado'`: `aguardando_empresa` → `pendente`, **mantendo a fase**; outro status, não mexe. Notificações deixaram de dizer "o processo avançou para planejamento de auditoria" (nunca foi verdade) e a cobrança passou do `analista` para o `gestor`, que é quem pode atribuir neste ponto.
- ✅ **`0a736a82` — `PATCH /workflows/:id` não muda mais a fase (FRG-21).** `currentPhase` saiu do `UpdateWorkflowDto` e do `update()`: mudava fase sem validar transição e **sem gravar `workflow_phase_history`**, liberado a `analista`. Com `forbidNonWhitelisted`, mandar o campo agora é **400 explícito**. Consumidores conferidos antes de remover: `workflowService.update()` do front **existe e não é chamado por nenhuma tela**; no back só o `contract.service` toca workflow, via `advancePhase`; o e2e de fase usa `/advance`.
- 🏛️ **Decisão registrada — `certifications.analyst_id` passa a ser espelhado na atribuição.** Medido em prod antes de decidir: o **único** workflow da base tinha `request_workflows.analyst_id` preenchido e `certifications.analyst_id` **NULL**. Não é cosmético — as duas pontas têm leitores diferentes: o workflow alimenta a caixa do analista (`AnalystDashboard`/`DocumentAnalysis`), a certificação alimenta o `groupBy(['analystId'])` da carga, os relatórios do gestor e o filtro "minhas certificações". Workflow segue fonte de verdade do processo; a certificação é espelho.
- ✅ **A UI já estava pronta e esperando o back:** `AssignmentManagement.tsx` **já tem** botão "Reatribuir" e o `GET /manager/assignments` já lista de `assinatura_contrato` a `emissao_certificado` — âncora idêntica à que derivei. Era o 400 do back que travava. **Nada a fazer no front.**
- 🔧 **[Renato] NINGUÉM VALIDOU os 3 fixes.** Roteiro único, uma passada: assinar contrato → workflow vira `pendente` na MESMA fase → gestor atribui analista (avança p/ análise documental) → gestor **reatribui** a outro (não avança; `audit_trail` guarda de quem para quem).
- ⚠️ **`certifications.analyst_id` segue NULL no processo Minerva** (o único de prod; andou para `planejamento_auditoria` durante o presencial). O espelho **não faz backfill** de linha existente. **Reatribuir pela UI cura o dado e é o teste acima ao mesmo tempo** — a alternativa é UPDATE no DBeaver, que não exercita o conserto.
- ❓ **[Renato] Assimetria do guard de `specialtyArea`:** `certification.service.assignAnalyst()` valida frigorífico × industrial; **a rota do workflow nunca validou e continua não validando**. Ficou mais visível agora que o FK é espelhado. Não repliquei: duplicaria lógica privada de outro service e criaria rejeição nova em cima do go-live.
- ❓ **[Renato] Vetável:** a troca de destinatário da notificação de assinatura (`analista` → `gestor`) foi decisão minha, além do pedido literal.
- 🧹 **Código morto no front** (fora do escopo do lote): `workflowService.update()` e o `currentPhase` de `UpdateWorkflowDto` em `certification.types` continuam lá sem chamador — hoje inofensivo, mas quem ligar essa função toma 400.

**📋 13/ago — FM 7.1.9 FASE 1 (registro) EM PROD: o formulário que dimensiona a auditoria sai da planilha.** Back `6ebb03c5..ad674921` (3 commits) · front `50135cc1..c3c9a818`. `tsc`/`build` limpos nos dois; suíte **842/843** (a falha é a pré-existente do `certificate-pdf.spec`, de 10/ago — mesmo número de antes). **Sem rota nova ⇒ zero regen do API GW.**
- 🚪 **O back existia desde a Onda 1 e NUNCA teve tela.** `grep` por `request-review-report` em todo o `src/` do front: **zero linhas**. É a 5ª ocorrência do padrão "capacidade pronta e autorizada, sem porta de entrada" (§7-Z), ao lado de FRG-16, IND-14, IND-15 e do `NewAuditorDashboard`. Agora é um card na `ProcessProposal`, **antes** da proposta — é da revisão que sai o número de dias que a proposta descreve.
- 🕳️ **O `fa3bc9cd` cobria 11 das 14 linhas de duração do papel — as 3 que faltavam eram a MESMA classe: o arredondamento da FILIAL** (B38/B42/B46). A matriz tinha o par `*_days` + `*_rounded` nos três momentos (inicial/manutenção/renovação); a filial só tinha o bruto. Como a filial arredonda por regra própria e o mínimo de meio dia vale para ela também, registrar só o bruto perderia **justamente o número que vai para a proposta** — e a base de comparação da fase 2 nasceria furada onde o cálculo é mais difícil. Fechado em `6f0997f1` (migration nova, aditiva, idempotente, nome mapeado; DTO + whitelist do `create` **e** do `update`). *(Lição: a leitura campo a campo contra o papel só ficou completa ao construir a TELA — o commit do back, feito com o mesmo documento em mãos, deixou passar 3 linhas. Conferir o papel duas vezes, por caminhos diferentes, é barato.)*
- 🔒 **Quem vê e quem escreve:** `empresa` **não vê o card** (documento interno que mostra prazo de auditoria — mesmo motivo do FRG-08); escrita = `comercial`/`analista`/`admin`, espelhando os `@Roles` do controller; **`qualidade` lê e não edita** (é consultada, não é dona da revisão — correção do IND-06). Em leitura os campos viram **texto plano**, não `input disabled`.
- ⚠️ **FASE 1 = REGISTRO, por decisão do Renato (13/ago):** a tela guarda o que o comercial calcula na planilha; não calcula nada. Por isso as regras do papel entram como **aviso e nunca como trava** (meio dia, redução 0–30%, itens não conformes) — travar impediria de registrar a planilha quando ela diverge de uma regra que a FAMBRAS **ainda não confirmou**, e a fase 2 depende exatamente dessa base de comparação.
- 🧹 **`hasAPPCC` passa a ser DERIVADO do `appccCount` na gravação.** O papel pede o **número**; o booleano legado fica porque o cálculo antigo o lê. Deixar os dois soltos criaria duas fontes para o mesmo fato — o padrão estrutural por trás do FRG-32 e do IND-04.
- ❓ **[Renato] 2 gaps registrados e ACEITOS em 13/ago, não resolvidos:** (a) o papel tem **um** "Total de dias corridos de auditoria" e o modelo tem `baseAuditDaysGso` **e** `baseAuditDaysSmiic` — os dois estão expostos e rotulados, não inventei qual é o do papel; (b) **"N.A." e "não avaliado" colapsam no mesmo `NULL`** nas 3 conformidades (o campo é `Boolean?`) — o rótulo da UI diz isso em vez de fingir que são a mesma coisa; separar exigiria enum.
- 🗑️ **`scripts/f1-single-product-sample.pdf` (441 KB) SAIU da história** a pedido do Renato. Ele entrou por um `git add -A` no `fa3bc9cd`, junto dos 9 untracked que aguardavam decisão desde 03/ago — o `add -A` decidiu sozinho. Como nada estava pushado, os 2 commits foram reescritos (`f631fb19`/`6f0997f1`) em vez de deixar o blob para sempre; `.gitignore` ganhou `scripts/*.pdf` (`ad674921`) para o próximo `add -A` não o trazer de volta. ⚠️ **Os outros 8 untracked seguem versionados** — os 4 SQL de limpeza, o `reset-prd.sql`, o `bootstrap-admin.ts` e os 4 scripts `.ts` de amostra. Se algum não devia entrar, o momento de tirar já passou (está pushado).
- ✅ **Validação de fora EXECUTADA (front):** baseline medido ANTES do deploy — bundle servido `index-XWH2QNoq.js`, **0** ocorrências de "Revisão da Solicitação" (⇒ a sonda distingue versão). Depois: **`index-Df1aYqcz.js`**, com "Revisão da Solicitação" · "Com inclusão do Golfo" · "duração arredondada". 
- ✅ **BACK CONFIRMADO EM PROD PELA MIGRATION** (validação executada 13/ago, com autorização do Renato após o classificador bloquear a consulta 2×). O back **não tem observável externo** — nenhuma rota nova (os 31 campos são propriedades opcionais em rotas que já existiam) e o swagger **não é público** (`/api-json`, `/docs-json`, `/swagger-json` → 404) —, então a prova foi o banco: `20260813050000_request_review_fm719_rev11` e `20260813170000_request_review_fm719_site_rounded` com `finished_at` **13/ago 09:33:10 UTC**, 16 ms entre as duas (= `migrate deploy` do entrypoint no boot). As 8 colunas amostradas existem, **inclusive as 3 novas** de filial arredondada. ⇒ **migration gravando linha é prova de que o container no ar é o do código novo** — serve de precedente para lote sem rota nova.
- 🔧 **[Renato] `request_review_reports` tem 0 LINHAS em prod** — nunca houve uma revisão FM 7.1.9 gravada, coerente com o back nunca ter tido tela. Consequências para o teste: **(1)** o 1º salvamento vai pelo **`POST` (create)**, não pelo `PATCH` — e é o `create` que tem a whitelist campo a campo, o lugar exato onde campo fora da lista some em silêncio ⇒ ao reabrir, conferir se **as durações voltaram preenchidas**, não só se "salvou"; **(2)** "Registrar classificação" nasce **desabilitado** até a revisão existir (decidir antes de salvar daria 404) — é comportamento, não defeito.
- ▶️ **Próximo:** **FM 7.1.9 fase 2** = cálculo automático (30%/70%, 1/3, 2/3, arredondamento, mínimo de meio dia) — **só depois de a FAMBRAS validar as regras**; agendar a discussão é item do Renato.

**📐 13/ago — CTR-02 FASE 2: spec ESCRITA, nada implementado.** `CTR-02-FASE-2-SALDO-POR-ITEM-SPEC-2026-08-13` (§8). Só análise, nenhum código (§0: "revisar não é autorizar").
- ❌🔍 **O enunciado do §7-Y estava errado nas duas pontas.** Ele dizia "raspa, tripas, gelatina": **`gelatina` NÃO é multi-produto** (guarda `customFields.finalProduct`, objeto **singular** — um lote, um peso; precisa só de um leitor) e **`fracionamento` é multi-produto e ficou de fora** (`outputProducts[]`). ⇒ o eixo não é "Json × coluna", é **quantos produtos finais o relatório tem**. Dos 10 tipos: **1** é escalar (`fabricacao`), **3** são array (`raspa`, `tripas`, `fracionamento`), **4** são 1-produto-em-Json, `mucosa` é log diário sem lote e `desossa` não tem produto final embarcável.
- ⚠️ **FURO REAL, mas SEM DANO HOJE — corrigido em 13/ago.** Escrevi antes "furo vivo em prod"; **superestimei**. O Renato confirmou que **a base do SIH não tem dados reais, só alguns testes** ⇒ o mecanismo abaixo está no código e é verificável, mas não há ninguém sendo prejudicado agora: o dano é **no go-live**. *(Lição: "verificado no código" ≠ "acontecendo em produção" — e backlog que superestima urgência corrompe a priorização de todo o resto.)* O mecanismo, medido: o card "Produção" do formulário só é renderizado para `fabricacao` (`ProductionReportForm.tsx:974`) ⇒ nos outros **9 tipos `net_weight_kg` fica NULL**. Cadeia: composição sai com peso e lote nulos · `totalWeightKg` soma 0 · `computeShipmentBalance(null,…)` devolve `fullyShipped: true` · **🔴 basta 1 vínculo e a produção SOME da tela de pendências**, mesmo tendo embarcado 1 de 5 produtos (o filtro só protege quem nunca foi embarcado) · a trava de sobre-consumo é pulada (`if (netWeightKg === null) continue`). ⚠️ O último é **degradação segura, não bug vivo** — não bloqueia assinatura indevidamente; é ausência de controle. O 4º é o oposto do objetivo declarado da tela.
- 🏛️ **Decisão de desenho que a spec propõe:** a PK `@@id([shippingReportId, sourceType, sourceReportId])` **não comporta 2 itens do mesmo relatório**. Recomendação = **estender a PK com `sourceItemKey`** (`''` = relatório inteiro, preserva a semântica atual) em vez de tabela filha — tabela filha poria o consumo em **dois lugares**, que é o padrão "duas fontes para o mesmo fato" já pago em FRG-32/IND-04/IND-14. 🔧 **Condicionada a 1 query** (`count(*)` em `shipping_report_sources`): a troca de PK é proporcional ao nº de linhas, e a feature subiu 12/ago ⇒ expectativa de dezenas.
- ✅ **A identidade do item JÁ EXISTE:** `EditableCardList` carimba `id: crypto.randomUUID()` em toda linha nova (`:45`) e edita/remove **por `id`** — linha sem `id` já estaria quebrada no editor. ⇒ `sourceItemKey` = esse UUID, **nunca índice do array** (reordenar/remover repontaria o vínculo para outro produto **em silêncio**) nem lote+nome (não é único e muda com correção de digitação). 🔧 **Verificar antes de codar** se há linhas sem `id` vindas de seed/importação — se houver, exige backfill no Json (escrita em dado de prod, OK explícito).
- 🧩 **Peça central proposta:** `resolveShippableItems(productionType, customFields, scalars)` — função pura, um caso por tipo, **único lugar do sistema que conhece o formato de cada `customFields`**. Subsome a fase 1 e serve os 3 consumidores hoje inconsistentes (composição, pendências, trava da assinatura). Sem ela, os 3 reimplementariam o mesmo `switch` e divergiriam na 1ª mudança de formulário.
- ⚠️ **`productionSnapshot` congela a composição na assinatura** — embarques já assinados guardam o formato antigo. O leitor tem de aceitar **os dois**; documento assinado é imutável, aqui como no certificado.
- ❓ **6 decisões travam fatias** (§7 da spec): `couro` entra no fluxo de embarque (é transferência, tem `recipient`/`transferDate`)? · `mucosa` fica com saldo por relatório? · `desossa` tem produto embarcável? · item sem `id` = backfill ou exclusão? · opção A ou B da PK? · colunas de `outputProducts` do fracionamento (leitura minha, pendente).
- 🚨❗ **A SPEC CONTRADIZ A D4 DA SALA — e eu não tinha lido a D4 quando a escrevi.** A D4 (12/ago, §5-C) diz: *"unidade do saldo = por produto/lote, **a chave é `(productCode, productBatch)`**"* e que *"para raspa/tripas/gelatina, **estruturar o Json entra no caminho crítico**"*. A spec recomenda o **UUID do item** como chave e propõe **ler** o Json sem tocar no modelo. São duas divergências, não uma. ⇒ **levar ao Renato como pergunta, nunca escrever por cima em silêncio.**
- 🔄 **RE-AVALIAÇÃO 13/ago, depois de saber que a base está vazia — a D4 fica MAIS forte e a minha proposta, mais fraca.** Meu argumento ("reforma de modelo não cabe na véspera do go-live") dependia de existir dado para migrar; **não existe**. Sem dado, estruturar o produto final custa **só código novo** — zero migração, zero backfill, zero dual-write de transição. E há **precedente no próprio repo**: `ProductionRawMaterial` já relacionalizou a MP de **entrada** (com `dual-write` declarado no schema) e o `deriveComposition` já a lê relacionalmente; **o produto final é a metade simétrica que ficou em Json**. Fazer agora é barato; fazer pós-go-live é o mesmo trabalho **mais** a migração dos relatórios reais. ⇒ **provável que a sala estivesse certa e eu errado.** ❓ Renato decide antes de reescrever.
- ✅ **Duas preocupações da spec MORRERAM com a base vazia:** (a) o risco da troca de PK — tabela vazia ⇒ **opção A sai de graça**, cai o "condicionado à query"; (b) o backfill de `id` nos itens do Json — **não há legado**, tudo que nascer vem pelo `EditableCardList`, que já carimba UUID. ⇒ **as queries que eu havia pedido não valem mais**: sem dados, elas mediriam a ausência de urgência, não a urgência.
- 📄 **O QUE FALTA DE VERDADE AGORA SÃO OS PAPÉIS** — não o banco. É a lição do FM 7.1.9, do mesmo dia: *"avaliar tamanho sem o documento na mão dá errado"*. Desenhei um modelo de saldo de embarque **sem ler o formulário de embarque**. No disco só existe **`FM 7.1.3.3 - REL ACOMP FAB PROD TRIPAS (REV 6)`** (`Documentos Certificação/25. Relatórios Supervisores/`). **Faltam: 7.1.8.5 (raspa) · 7.1.8.6 (gelatina) · fracionamento · e sobretudo o FM de EMBARQUE (7.1.7.1)** — é nele que deve estar se o embarque referencia lote e como a quantidade é declarada. 🔧 **[Renato] pedir à FAMBRAS ou apontar onde estão.**
- 🕳️ **A Trilha D (SIH) do §2 está VENCIDA:** declara `src/{auth,gc-integration}/` e `pages/{auth,gc-raw-materials}/` — **nenhum** arquivo desta spec está nesse domínio. **Ampliar o §2 antes de abrir a 1ª fatia**, senão duas sessões colidem (já ocorreu em 16/jul).

**🧹 12/ago (2ª metade) — VARREDURA DE SEGURANÇA, NOTIFICAÇÕES E DATAS: 6 lotes EM PROD.** Back `3fba0320..6ebb03c5` (+ `3529aa7a`) · front `78668f64` (+ `b5cb53f4`, `2dff97a4` local). `tsc`/`build` limpos nos dois; suíte 842/843 (falha pré-existente do `certificate-pdf.spec`; a do `qrcode-generator` é sensível à carga e **reproduz na árvore limpa** — provei com stash). **Nenhum lote tem rota nova ⇒ zero regen do API GW.**
- 🔒 **B6 · `3529aa7a` + `b5cb53f4` (FRG-07/10/03) — dois furos de segurança fechados.** **FRG-07:** `pcch.controller` não tinha **nenhum** `@Roles` e o service não recorta por empresa ⇒ qualquer autenticado, inclusive `empresa`, **lia e ESCREVIA** a matriz PCCH de qualquer certificação; a defesa era só de UI. Agora leitura = equipe FAMBRAS, escrita = analista/gestor/admin, `empresa` fora das duas (a matriz estruturada é instrumento do analista — decisão da sala FRG-05; o cliente entrega como documento anexado). **FRG-10:** `?includeInternal=true` vencia sempre, sem checar papel ⇒ token de `empresa` lia comentário interno; a UI nunca manda o param, então não havia sintoma. Agora quem decide é o papel, com teste nas 4 combinações. **FRG-03:** 9 ocorrências de "Haram" (título, coluna, modal, campo, tag do Swagger, `@ApiOperation`, `@ApiProperty`, comentário) → "Pontos Críticos **de Controle Halal**" (sigla preservada) e "Perigo **à integridade Halal**". ⚠️ **Achado a mais:** o `@ApiProperty` do DTO trazia **exemplo com termo haram explícito** — trocado por menção oblíqua. Validado de fora: chunk novo com o termo correto e **zero** ocorrências do antigo.
- 📣 **B3b · `3fba0320` (FRG-31 p3 + IND-10) — 3ª e 4ª ocorrências do "passo que para o processo e não avisa ninguém".** Rejeitar documento era mudo (voltava "Rejeitado" na tela e o cliente só via se abrisse a certificação certa) → agora notifica a empresa **com o motivo**; aprovação segue silenciosa, de propósito. `complaint/*` não importava **nada** de notificação → acuse de recebimento ao reclamante (ISO 17065 exige) + aviso à `qualidade`; no `updateStatus`, e-mail **só** nos estados que são resposta (`respondida`/`resolvida`/`escalada`/`cancelada`) — status interno não vira e-mail, senão o reclamante para de ler; **anônima nunca recebe**. `NotificationService.sendEmailTo` novo (endereço sem login) para não duplicar template. Dois módulos que **não tinham teste nenhum** ganharam 9 casos.
- 🗓️ **B8 · `78668f64` (FRG-04) — vigência e emissão deixam de exibir 1 dia antes.** Dado e PDF sempre estiveram certos; o defeito era de renderização: date-only é meia-noite UTC e o front tem **30 helpers `formatDate` locais**, quase nenhum com `timeZone`. **Não fiz varredura cega:** date-only lê em UTC, timestamp real fica local (forçar UTC nos timestamps trocaria erro de 1 dia por erro de 3 horas). Novo `lib/date-format.ts` (`formatDateOnly`/`formatDateTime`/`daysUntilDateOnly`) — nomes que **obrigam a escolher**; aplicado campo a campo em 3 telas (5 date-only, 4 timestamps preservados, 2 contagens). As outras ~27 telas formatam `createdAt`/`updatedAt`, onde o comportamento atual já é o correto.
- 🕐 **`e8671895` — `ENV TZ=UTC` no Dockerfile.** ✅ **Renato confirmou no console: a task definition NÃO define `TZ`** ⇒ Alpine roda em UTC. O comportamento correto de hoje dependia desse default silencioso: `certification-scheduler`, `product-recall` e o rodapé do PDF de relatório formatam date-only **sem** `timeZone`. Agora a premissa está declarada. 🧩 **Registrado:** corrigir esses call sites do backend (como no front) é o conserto de verdade.
- 🏷️ **`6ebb03c5` (FRG-32 parte 1) — o selo passa a ser congelado na geração do PDF.** O PDF **derivava** o `marketVariant` na hora e nada era gravado; a validação pública do QR **lê** `certificates.market_variant`, que ficava NULL — daí certificado com ENAS no papel e sem selo na verificação online. Contradizia o §5.22(e). Agora `generateAndStore` persiste o variante renderizado, **write-once** (mesmo padrão do `resolvedScopeSnapshot`). **Impede que os 177 latentes virem divergentes; NÃO corrige os 3 que já divergem** — isso é a parte 2 (dados).
- 🏷️ **IND-04 (c) · `2dff97a4` (front, local) — rótulos de C1–C6.** Decisão do Renato: manter o enum (o preço-base depende dele) e corrigir só os rótulos. Ao aplicar, achei **quatro listas divergentes** no front para o mesmo enum, discordando entre si: "C1" era *Abate* no `types`, *Alimentos Processados* no `ProposalCalculator`, *Alimentos* no `ProposalSettings` e *"Abate de animais"* (com emoji de boi) no `CertificationTypeSelector` — **nenhuma batendo com o schema** (C1 = alimentos processados) e **três sem o C6**. Fonte única agora, e o código vem **depois** do nome (`Produtos químicos (tipo C2)`), porque liderar com "C2 -" era o que fazia a tela parecer falar do grupo industrial C.

**🧑‍⚖️ 12/ago — IND-15 (trocar auditor) EM PROD, e o que ele destampou na alocação.** Back `60eee058` + `10d7c77c` · front `c395d07b` + `71246f92`. `tsc`/`build` limpos nos dois; jest workflow 69/69 · alocação 39/39 · suíte 826/827 (falha pré-existente do `certificate-pdf.spec`). Sem rota nova ⇒ sem regen do API GW.
- ✅ **`60eee058` + `c395d07b` — trocar auditor com MOTIVO obrigatório (IND-15).** A troca era silenciosa: o DTO só tinha `auditorId` e o `assignAuditor` não gravava trilha nenhuma. Agora `reason` é obrigatório (com `trim` antes de validar — `@IsNotEmpty` sozinho aceita `"   "`, que é o mesmo que não ter motivo) e a troca grava `AuditTrail` (`entity=process`, before/after + motivo + flag `reassignment`) na mesma transação, no padrão do `assignAnalyst` (B1). **Sem trava de fase, de propósito** — foi ela que criou o beco do FRG-24. Front: seletor + motivo no card "Planejamento da Auditoria", no mesmo padrão da rejeição de documento; o `workflowService.assignAuditor` era **método órfão** (2º padrão estrutural).
- ✅ **Sincroniza a alocação (decisão do Renato): atualiza a existente, não cria registro novo.** `allocatedAuditorId` + `allocatedBy` + motivo **apendido** em `allocationNotes` (sem apagar o histórico), em status `aprovada` **ou `modificada`** — deixar `modificada` de fora reintroduziria a segunda verdade justamente onde o RT já trocou uma vez.
- 🐛 **Achado no caminho — a troca criaria uma auditoria DUPLICADA.** O auto-create procurava por `(certificationId, auditorId NOVO)`, não achava e criava outra: a certificação ficaria com 2 auditorias abertas e o auditor antigo com a dele na agenda. Agora, na reatribuição, a auditoria **`agendado`** do anterior **muda de dono**; criar só quando não existe nenhuma. *(Auditoria `em_andamento`/`concluido` NÃO é tocada — o que fazer com o que já foi coletado é decisão de negócio, não detalhe técnico.)*
- 🐛✅ **`71246f92` — o card sumia justamente quando era preciso.** Estava atrás de `currentPhase === 'planejamento_auditoria'` (igualdade EXATA): a Minerva avançou para `auditoria_estagio1` e o card evaporou, levando reagendar e trocar auditor — **reportado pelo Renato logado como admin, minutos depois do deploy**. É a armadilha do FRG-24 no front. Card passa a valer enquanto a auditoria está viva (planejamento → estágio 1/2 → NC/correção/validação); **seguem restritos ao planejamento** gerar sugestões, "Agendar Auditoria" (agendar em estágio 1 criaria uma 2ª auditoria) e "Avançar Fase".
- 🚨✅ **`10d7c77c` — APROVAR a sugestão não designava o auditor em lugar nenhum.** Medido em prod: alocação `aprovada` apontando "Renato Oliveira" · `request_workflows.auditor_id` **NULL** · as 2 auditorias agendadas **sem auditor**. **Três verdades.** Causa: `POST /auditor-allocation/suggest/:certificationId` chama o matcher **sem `workflowId`** (a rota só recebe a certificação) ⇒ alocação nasce órfã ⇒ `approve`/`modify` propagavam dentro de `if (allocation.workflowId)` e o ramo **nunca executava**. O caminho automático (`advancePhase`) manda o id e por isso nunca falhou — só o **botão manual**, que é o que a FAMBRAS usa e que nasceu no B12 para consertar o IND-14. Conserto na origem (o matcher resolve o workflow pela certificação) **e** no destino (`applyAllocationToProcess` descobre o workflow quando a alocação é órfã — cobre as 2 que já estão em prod — e designa o auditor também nas auditorias `agendado` sem auditor).
- ❌🔍 **CORREÇÃO DO §7-W: "a tela de aprovação de alocação não chama nenhuma rota" está ERRADO.** Quem chama é o componente FILHO: `SuggestionCard.tsx:33` (`approveAllocation`) e `:50` (`rejectAllocation`) — e o banco confirma uma alocação aprovada em 11/ago 22:58 com `allocated_by` preenchido. ⇒ **a tela existe e funciona**; o que faltava era a aprovação valer alguma coisa (acima). **Não reconstruir.** *(2ª vez em 2 dias que eu concluo "não é chamado" olhando só o arquivo pai — a 1ª foi o `fulfill`/FRG-30. Prova de ausência exige varrer `src/` inteiro, filhos inclusive.)* O que **de fato** não tem porta é a rota **`modify`** (trocar o auditor **entre as sugestões, antes de aprovar**): nenhum componente a chama. Deixou de ser bloqueante — com a troca com motivo, dá para aprovar e trocar depois. ❓ decisão do Renato se entra.
- ❌🔍 **PREMISSA FALSIFICADA (12/ago) — "o matcher casa por CATEGORIA do auditor" está ERRADO, e vem sendo repetido desde o §7-W.** O `matching.service.ts` **não lê `AuditorCompetency.industrialCategoryId` em nenhuma linha** (grep vazio). Quem decide que categorias um auditor pode auditar é a **FORMAÇÃO**, pela matriz Categoria × Formação do FM 6.1.4 (`isEducationEligibleForCategory`) — e `educationalBackground` **já é lista**: agronomia/zootecnia/alimentos cobrem ~20 categorias, CV inclusive. **Prova em prod:** as 2 sugestões de 11/ago 22:56 (scores 69,80 e 68,75) foram geradas com **todas as competências sem categoria**. ⇒ "auditores sem categoria industrial" **nunca** bloqueou a sugestão; a previsão de "nenhum elegível" não se materializou. **Riscar da lista de pendências operacionais.**
  - 🔄 **Efeito prático:** cheguei a entregar um seletor de categoria no `CompetencyFormModal` (`0a13aab3`) com o aviso "sem categoria não aparece nas sugestões". **Revertido em `23def0b4`** por decisão do Renato: configuração que não muda nada só confunde, e o aviso era falso. O campo segue no schema/DTO (registro do FM 6.1.4 + filtro da busca de auditores) e o valor existente é preservado na edição — só não é editável pela tela. Comentário no lugar do campo explica o porquê, para não ser reintroduzido.
  - 💤 **Armadilha adormecida registrada (não consertar agora):** o matcher agrupa as competências por auditor e **avalia só `competencies[0]`** (*"Usa a competência principal"*). Como não há mais motivo para criar várias linhas por auditor, o caso não é exercitado. Se um dia houver múltiplas competências por pessoa, formação/normas da 2ª linha serão ignoradas silenciosamente. **M:N de categorias por competência foi avaliado e DESCARTADO** — resolveria problema inexistente, com migration na véspera do go-live.
  - 📌 **Lição:** eu propaguei "o matcher casa por categoria" do doc para 2 blocos novos sem medir, e ainda construí em cima. Premissa herdada é hipótese, não fato — o grep custava um minuto.

**📥 11/ago — B2 (front) e B3 (back) do presencial, AMBOS EM PROD.** Front `d9b6a9ea..082a00eb` (levou junto `4aeadb8b`, tela de ingredientes restritos da Trilha F, commitada s/ push desde 09/ago) · back `0a736a82..c1777de1` (commit único). `tsc -b` + `vite build` limpos no front; `tsc --noEmit` + `nest build` limpos no back; jest 811/812 (a falha é a pré-existente do `certificate-pdf.spec`). **Sem rota nova nos dois ⇒ sem regen do API GW.**
- ✅ **B2 · `082a00eb` (FRG-19) — a caixa de entrada do analista volta a existir.** A tela filtrava por `'analise_documental'` e `'triagem'`, nomes que **nunca existiram** no enum: lista vazia e 5 KPIs em zero para TODA certificação, desde sempre. **Mas a raiz não eram 2 strings:** o `const ProcessPhase` do front era um vocabulário paralelo inteiro — **11 dos 17 valores inexistentes** no banco (`pagamento`, `parecer_tecnico`, `decisao_certificacao`, `monitoramento`, `encerrado`…). Realinhado ao enum real e deixei o **compilador** fazer a varredura. A mesma deriva estava em `WorkflowStatus` (5 fantasmas, 3 faltando) e `RequestType` (faltavam `inicial`/`adequacao`/`reducao`/`restauracao`).
- ✅ **Efeito colateral bom:** com `PHASE_LABELS` cobrindo as 17 fases reais, três telas que já liam esse mapa param de exibir `undefined`/snake_case — `CertificationTimeline` (que nem fallback tinha), `CertificationDetails` e `CompanyDashboard`. Corrigidas de tabela, sem tocar nelas.
- 🐛 **4ª ocorrência de `certification.company`** (após `fef01534` e `35ee5a5a`): `DocumentAnalysis` **e** `AnalystDashboard` montavam o nome da empresa por `certification.company.razaoSocial` — caminho morto desde o refactor Empresa+Planta (certo: `plant.company.legalName`). Sem isso a coluna Empresa viria "N/A" na lista inteira. **A varredura sugerida no FRG-19 (`certification?.company` no front) segue valendo.**
- 🐛 **`ProcessCard`: 3 botões que nunca renderizaram** — comparavam `columnId` com `'analise_documental'`/`'analise_tecnica'`/`'comercial'`, ids que o Kanban do analista não usa (`analise`/`auditoria`/`finalizacao`). "Analisar" corrigido; os outros 2 eram código morto (um dependia ainda da fase inexistente `proposta_comercial`) e saíram.
- ⚖️ **Decisão NÃO tomada (é de produto):** `REQUEST_TYPE_CONFIG` alimenta o `RequestTypeSelector` via `Object.entries` ⇒ acrescentar os 4 tipos novos **criaria 4 opções no wizard do cliente**. Mantidas as 4 ofertas de hoje, com o porquê documentado no arquivo. ❓ Se a FAMBRAS quiser expor `reducao`/`restauracao`, é lote do B4.
- ✅ **B3 · `c1777de1` (FRG-29 + FRG-30) — o ciclo de documentos deixa de ser mudo.** `document-request` não tinha **nenhuma** referência a notificação/e-mail: analista cobrava e o cliente só descobria abrindo a certificação certa; empresa entregava e o analista também não sabia — na etapa **mais repetida** do processo, com `dueDate` vencendo sozinho. Agora: `create()` → notifica a **empresa** (in-app + e-mail, com tipo, observação, prazo e link) e `fulfill()` → notifica **quem pediu** e, se for outro, o **analista atual da certificação** (o dono pode ter mudado por reatribuição — B1), sem duplicar destinatário. Certificação sem empresa não quebra a criação: vira `warn`. Era **ligar**, não construir (o `NotificationService` é `@Global` e já faz in-app + e-mail no template azul).
- 🐛 **Achado pelo próprio teste, antes de chegar ao cliente:** `dueDate` é data-only em UTC 00:00 e `toLocaleDateString` no servidor (UTC-3) devolvia o **dia anterior** — prazo 01/02 virava 31/01 na cobrança. **É a classe do FRG-04 fora do front.** Formatação passou a ler em UTC. ⚠️ Vale procurar o mesmo padrão em qualquer data-only formatada no backend.
- ❌🔍 **ALARME FALSO — "o `fulfill` não é chamado por tela nenhuma" está ERRADO. Registrado aqui em 11/ago e desmentido no mesmo dia, antes de virar trabalho.** O `CertificationDetails.tsx` tem o seu **próprio** `DocumentRequestItem` (linha 147), renderizado no card "Documentos Solicitados" (linha 1078, aparece quando há solicitação; upload só para `empresa`), e ele **chama `fulfillDocumentRequest` na linha 171** depois do `uploadForCertification`. ⇒ **a solicitação SAI de `pendente` normalmente e a notificação do FRG-30 dispara em prod.** Nada a fazer. **Causa do erro:** varri `fulfill` com um glob que cobria `src/services` e `src/components` e **não `src/pages`** — e o próprio relato do Renato no FRG-29 (*"o alerta só aparece ao abrir o fluxo e navegar até aquele ponto"*) descrevia exatamente esse card. *(Lição: grep com glob restrito é evidência de ausência FRACA; e um relato do usuário que descreve a tela funcionando vale mais que meu grep.)*
- 🧹 **O que sobra do achado, e é só higiene:** `PendingDocumentRequests.tsx` (empresa) e `DocumentRequestsAnalystView.tsx` (analista) são **duplicatas órfãs** — implementam o mesmo fluxo e não são renderizadas por ninguém (o segundo tem, a mais, rejeição COM motivo, que é o FRG-31). **3º e 4º órfãos da série**, com o `NewAuditorDashboard` de 09/ago. ❓ **[Renato] apagar ou aproveitar** — o `DocumentRequestsAnalystView` pode ser a base do FRG-31, em vez de escrever um terceiro. ⚠️ Continua verdade que `document/*` (back) não conhece `documentRequest`: **o upload genérico** do card "Documentos Anexados" não fecha solicitação — só o card "Documentos Solicitados" fecha. Isso é desenho aceitável, não bug.
- ⏭️ **Fora do B3 de propósito:** notificação no **cancelamento** de solicitação (cada notificação nova é e-mail real no go-live) e as fatias **(b) KPI no dashboard da empresa** e **(c) lembrete de prazo a vencer**, que o próprio FRG-29 marca como não indispensáveis.
- 🔧 **[Renato] Validar B2/B3 de fora:** B2 = bundle novo servido + tela **Análise Documental** listando processo em `analise_documental_inicial`/`avaliacao_documental` com **nome da empresa** (não "N/A") e KPI "Aguardando Empresa"; B3 = **sem observável externo** (rota nenhuma nova, tudo atrás do guard) — a prova é funcional: solicitar documento como analista e conferir notificação **e e-mail** chegando à empresa.

**🟨 FOLLOW-UP SEMANAL 03/ago 11h** (Soha, Lina, Elaine, André, Islam · **Fuad convidado mas não falou**). Transcrição Fireflies `01KYGEC5J5FY8S1ZXBNS6X3CQX`.
⚠️ **Atribuição corrigida:** quem reportou o erro de espécie foi a **Giovanna**, NÃO a Lina — o Fireflies etiquetou a fala de [22:10] como "Lina Ramadan", mas a Giovanna não consta nos participantes e o Renato se dirige a ela em [22:55] ("mais algum ponto, André ou Giovanna?"). Provavelmente dividindo conexão. *Não citar o transcript como fonte de quem falou sem conferir.*

**✅ 6 perguntas do §4.1/§4.3 RESPONDIDAS — fecham itens que estavam abertos:**
1. **DT-73/74 nos nacionais → MANTÉM COMO ESTÁ** (Soha): *"Sim, ela aparece também. Tanto a DT quanto a normativa. E quando é mercado interno, a gente só coloca [as DTs], porque não segue nenhuma normativa internacional… o que o cliente vai seguir é os requisitos da FAMBRAS."* ⇒ confirma o **Lote 4.3**. **Item fechado, não re-litigar.**
2. **GSO+OIC → template GCC, só selo GAC → CONFIRMADO.** O "selo OIC" é o **HAK**, e só sai com a linha da Turquia junto (Elaine/Soha). Já é o comportamento atual.
3. ⚠️ **Vigência ancorada na CERTIFICAÇÃO, não no certificado emitido** (Elaine: *"o ciclo é vinculado à certificação. A vida da empresa."*). **REABRE a decisão 3 de 30/jul** ("manual por enquanto"): agora há resposta oficial e dá para automatizar. Hoje o default do form é emissão+3a; no caso Phibro a diferença dava **2 anos**. 🧩 **entra na fila do §4.2.**
4. **Perfil `qualidade` — lista confirmada (Elaine):** **Elaine, Soha, Osama, Bárbara, Mari e Carol** (6). Renato: *"Libero no início da tarde o acesso diferenciado."* ⇒ **destrava o Lote 6.3** (reclamação restrita à qualidade, que hoje só os 2 admins conseguem abrir). 🔧 **[Renato] criar/ajustar os 6 perfis.**
5. **Integração com o sistema da Arábia Saudita → IMPOSSÍVEL** (Islam): *"Não pode. Eles não deixam. Nem consumir dado, nem nada. A CIDIAL também pediu."* **Porta fechada — riscar da lista.**
6. **Base de contratos (F3, nº cert = nº contrato):** não existe base estruturada — há guarda-chuva corporativo + contratos com adaptação jurídica por cliente (Soha). ⇒ **diferido para o presencial** ("vou levar para tratar pessoalmente").

**🆕 6 pontos NOVOS da reunião:**
- 🆕🔵 **O site público da FAMBRAS perde a fonte de dados.** As planilhas **FM 781/782 alimentam o site**; quando o GC virar o master elas deixam de existir. Soha: **o site NÃO pode sair** (consumidor final, partes interessadas, acreditadores — que não têm acesso ao sistema). Duas saídas na mesa: (a) botão que **gera as 781/782**; (b) **API para o site consultar a base direto**, com filtros. Soha: *"Desde que esteja sempre atualizado, ok."* **Quem decide: a QUALIDADE.** ⇒ **feature nova, não prevista no roadmap.** ❓ decisão + timing.
- 🆕 **Lista de certificados CANCELADOS** — Soha já mandou no grupo. Carga nova + provável necessidade de tratar o status na base. 🔧 [Renato/Claude].
- 🆕 **Método de validação de layout DEFINIDO** (destrava os itens (a)(b)(c)(d) do Fuad): importar 781/782 → pegar um certificado **que já existe** → imprimir pelo sistema → comparar **lado a lado** com o original, no máximo de variações de layout possível. Renato citou nominalmente a **BRF Capinzal** como caso.
- 🆕 **Plantas faltando cadastrar** (Victor): *"o resto das unidades não está cadastrado ainda"*. Fonte = a própria 7.8.1x. Renato vai **puxar da Receita** para completar endereço/CNPJ. ⇒ bate na `SYNC-CADASTRO-GC-SIH-SPEC-2026-08-03`.
- 🆕 **SysHalal/SISRALAU — reunião 14/ago com Islam + Hilary** (versão nova desenhada; API pronta, *"já vou liberar para a JBS"*). 🆕🐛 **Islam relatou bug: "aquele API que reabre o certificado sozinho lá para a BRF que tá causando problema"** — Renato: "vai ser corrigido". **Sem nenhum detalhamento — exigir evidência.**
- 🆕 **Cliente dos Emirados quer produção/validade em MÊS/ANO** (hoje dd/mm/aaaa em toda rotulagem e documentação de exportação). Islam verificando, cogita TXT via despachante. **Não é demanda de sistema ainda.**

**🔴 RISCO DE CRONOGRAMA que esta reunião escancarou — NINGUÉM TESTOU:**
Soha (não acessa há +1 semana) · Lina (*"eu não estou testando"*) · Elaine (*"eu não. Vou ser sincera."*) · André (*"só foi avisado ontem, não conseguimos fazer os testes"*) · Fuad (não testou o rascunho→aprovar). E **esta semana não dá**: André tem *"treinamento quinta e sexta, e amanhã estaremos no evento"*.
⚠️ **A Soha faz 2 cirurgias e fica ausente ~2 semanas a partir de 04/ago** — some durante o presencial (10-14/ago) e o go-live (15/ago), e é ela quem decide formato de normas e sobe as listas ao site.
⇒ **A validação inteira foi empurrada para o presencial**, ou seja, descobrir retrabalho a **1 dia** do go-live. É o risco nº 1 já registrado, agora **sem folga para absorver**. André confirmou que a aprovação formal não existe: *"a gente tem que refazer os testes para poder te dar aprovação"*.
⇒ 🔧 **[Renato] decidir com a FAMBRAS: ou testam antes do dia 10, ou 15/ago vira "operação assistida com pendências", não go-live limpo.**
- 🔧 **[FAMBRAS] validação do cadastro de produto/MP segue SEM DONO** — Renato pediu de novo (*"é imprescindível que vocês definam alguém ou um grupo"*). É a "revisão a 4 mãos" pendente há semanas.

- ✅ *(16/jul)* `CERTIFICATE_PDF_UNLOCK_KEY` na task def do GC.
- 🔧 Validar: `.K.` bovino×aves · OIC/SMIIC **01/2019** · **993** só em abate · PDF protegido (abre livre, não copia, imprime) · busca por SIF · guard-rail (CV+HII bloqueia) · **Edição de escopo F1** (roteiro de 5 passos no handoff 13/jul) · **filtro de empresa em `/homologacao-mp`** — selecionar empresa → o **X limpa** → digitar outra → troca (`160b2cdd`; era o bug do filtro "Diana Food": o `[&_svg]:pointer-events-none` do Button engolia o clique no X).
- 🔧 **SIH:** validar recuperação de senha E2E · ✅ *(16/jul)* **SES fora do sandbox — CONFIRMADO pelo Renato** (era o risco de quebrar o fluxo em campo; item fechado) · confirmar migration `20260713120000_password_reset_token` aplicada · validar "Ver PDF" (`d7e9eaa`).
- 🔧 **SIH · espelho GC→SIH (deployado 03-05/jul, NUNCA validado):** abrir planta Rolândia → card do espelho no bloco "Origem da Certificação Halal" (nome canônico + certs vigentes) · badge de cert no destino da transferência. Código: GC `b4337037` + SIH back `0450f78` + SIH front `f76cc8d`, todos em release/prod.
- 🔧 **SysHalal `/integration` (trilha SIH↔SysHalal, spec `SYSHALAL-INTEGRATION-ENDPOINT-SIH-SPEC-2026-07-06`):**
  (i) **SSM staging** `syshalal-external-api-staging.json`: adicionar `SERVICE_API_KEYS` (chave nova p/ o SIH) + `SERVICE_PDF_USER_OWNER/TOKEN` (usuário API do ambiente staging) → **restart do serviço** (configEnv lê SSM só no boot) → passar a chave ao Claude p/ teste;
  (ii) ❓ **destino do `api_sih`** em prod: manter ativo como credencial interna do PDF (sugestão Claude — o download de PDF faz login real no syshalal-api) × criar usuário interno novo · **rotacionar o token** (transitou em texto plano em sessão de 06/jul);
  (iii) rollout prod (após teste staging): SSM prod + merge `staging→release` do syshalal-external-api (staging=`6d2c2e7`) + secret `production.SYSHALAL_INTEGRATION_API_KEY_SIH_API` na task def `sih-api-task`;
  (iv) validação final: buscar **`2607FU7I2`** (grupo ≠ BRF) na UI do SIH → verde; `2607PHJWS` de regressão.
- ⚠️📅 **Rotacionar as senhas GC + SIH (Aurora) — AGENDADO pelo Renato p/ 25/jul.** GC (`db_ecohalal_halalsphere`) pendente desde 10/jul; SIH (`db_ecohalal_sih`, user `db_ecohalal_sih_user`, mesmo cluster) desde 17/jul. Ambas transitaram em texto plano em arquivo temp/scratchpad (`db-conn-sih.json`) no cruzamento GC↔SIH.
- 🔧 **Cadastros do SIH pendentes** *(levantados no diagnóstico de 20/jul, cruzando `db_ecohalal_sih`)*:
  - **Criar o supervisor Ziad Mansour** — assina os FM da BRF Dourados (doc. `V444820C`) e não existe na base. Precisa ser pela UI (hash bcrypt) + vincular à planta. **PENDENTE.**
  - 🔧 **Haitham Mohamed** — supervisor IND **sem nenhuma planta vinculada**: ao logar vê dropdown vazio. **[Renato] vincular ou inativar** (não toquei — é decisão).
  - ✅ *(22/jul, DADO em prod)* **`division` dos supervisores preenchida:** Mussa Mustafa → **IN** · Sameh Kamal → **IN** · Vinicius (Jamal) → **IND**. **"Vitor Sup." (`vitor.franco@sih.com`) INATIVADO** — era conta de teste (38 plantas), confirmado pelo Renato. Resultado: **nenhum supervisor ativo com `division` NULL** → o filtro `7bc4768` passa a recortar corretamente para todos. Script em `scratchpad/fix-supervisores.js` (transação, antes/depois).
- 🔧 Rodar **import IND** em prod (`prisma/import-plantas-ind.ts`, `79b2935`+`91f08fc`) — 23 plantas + vínculos de supervisor.
- 🔧 SQLs de limpeza de teste no DBeaver (4 arquivos em `halalsphere-backend/prisma/`).
- 🔧 `capabilities=processamento` das 4 Seara (admin Lina) — confirmar quais.
- 🔧 Confirmar no console a regra de listener do ALB `/verify/*` (SysHalal QR).
- ❓ Enviar a resposta ao André (4 pontos, todos entregues em 16/jul).
- ❓ **`sih-docs`: decidir o destino do `PLANNING/STATUS-EXECUTIVO-DIRETORIA-2026-07-22.md`** — está **untracked** (fora de git, só nesta máquina). Versionar ou descartar; ninguém commita sem essa decisão (regra §0.4).

### 4.2 Claude — código

**🗓️ LINHA DO TEMPO — ciclo de validação FAMBRAS, julho/2026 (MAPA; o detalhe vive nos blocos abaixo, não repetir aqui).**
> Serve para não perder o fio: **a mesma falha chegou por canais diferentes em datas diferentes**, e sem este mapa parece coisa nova toda vez.

| Data | Canal / quem | O que veio | Onde está | Estado |
|---|---|---|---|---|
| 20/jul | Reunião + testes Giovanna/William | Kernel de normas + backlog de emissão (T1–T7) | §4.2 bloco "KERNEL DE NORMAS" | ✅ T1–T7 fechados 23/jul |
| 22/jul | WhatsApp Soha | 4 decisões de norma (versão pela auditoria, mercado ⊆ auditado, 2055-1×2055-2, ingrediente restrito) | §4.3 + §5.22 | ✅ decididas |
| 22-23/jul | Testes Nilsa (2 rodadas) | T3/T4/T7 reabertos no reteste | §4.2 | ✅ reproduzidos e fechados |
| 23/jul | Renato | Gestão de escopo (Opção A, 1.177 certs) | §4.2 | ✅ EM PROD |
| 26-27/jul | Renato | Reconciliação SIGSIF/MAPA + F2 Fatia A/A.1 (rascunho→aprovar) | §4.2 | ✅ EM PROD |
| **27/jul** | **Follow-up semanal (Fuad, Dib, Nilsa)** | Fuad **validou** normas+selo ao vivo; **reprovou o LAYOUT** do PDF industrializado (7 itens a–g); entregou `CATEGORIAS.docx`; 3 pedidos novos | §4.2 bloco "FOLLOW-UP 27/jul" | 🟨 (e)(f)(g) fechados 31/jul · **(a)(b)(c)(d) abertos** |
| 27/jul | Renato | Multi-categoria, "objeto da certificação", criticidade de MP (escala 4 níveis + gate) | §4.2 / §4.3 | ✅ decididos |
| **30/jul** | **E-mails Guilherme Luz + Caio França** | 13 pontos (importação, layout, QR, datas, normas, reprovação, menu…) | §4.2 bloco "RETORNO DOS ANALISTAS 30/jul" | ✅ 6 lotes EM PROD 31/jul |
| 31/jul | Achado ao subir os lotes | Deploy do front parado 3 dias (2 commits fora do ar) + perfil `qualidade` sem usuários | §6 + §4.1 | 🔴 o do perfil segue aberto |
| **03/ago** | **WhatsApp Vitor** (pedido de planta) | BRF Capinzal SIF 466 no SIH; investigação abriu o gate de departamento e um bug do NULL | §4.2 bloco "GATE DE DEPARTAMENTO 03/ago" | ✅ planta EM PROD · fix EM PROD |
| **03/ago 11h** | **Follow-up semanal** (Soha, Lina, Elaine, André, Islam; Fuad não falou) | **6 perguntas RESPONDIDAS** (DT-73/74, GSO+OIC, vigência, perfil qualidade, Saudi, contratos) + 6 pontos NOVOS | §4.1 + §4.2 bloco "ESPÉCIE + GOLFO" | 🟨 respostas registradas · **ninguém testou** |
| **04-05/ago** | **E-mail Lina Ramadan** (cc Fuad e Dib) — 1ª validação real do caminho INDUSTRIALIZADO | 8 pontos; investigação achou 2 bugs que ela não reportou | §4.2 bloco "RETORNO DA LINA 04/ago" | ✅ **8 de 8 EM PROD (05/ago)** |
| **10-14/ago** | **Presencial FAMBRAS** (frigorífico **12/ago**) | Âncora de prazo do ciclo | §4.2 | 📅 marcado |

**🔁 O mesmo defeito por dois canais — vale como método:** o **layout sobrepondo DT/normas** foi reprovado pelo **Fuad em 27/jul** (itens e/f) e reportado pelo **Guilherme em 30/jul** ("uma das normas fica tampada"), com o Caio confirmando em 30/jul (item 7). Foi tratado uma vez só, no **Lote 3** (`71760b1a`). ⇒ **Antes de abrir frente nova, checar se o sintoma já não está descrito em outro bloco** — dois relatos independentes do mesmo defeito custam o dobro se virarem duas tarefas.

**🎯 RETORNO DA LINA 04/ago — 1ª validação real do INDUSTRIALIZADO. EM PROD: back `a08059b9`→`286f6e36`→`feb793d7`→`1fd7c89d`→`76591d57`→`720a8be5`→`d36a11dc` · front `5633f306`→`cb9151eb`→`b141bc14`→`9c53fc84`.**
> A Lina (Gerente Técnica do Dept. Industrial) foi a **primeira pessoa a testar de verdade** — e testou o caminho industrializado, que era o buraco (o Fuad segue sem testar). Mandou 8 pontos com 4 prints e um PDF. Método que valeu de novo: **medir antes de responder**. Dois dos oito não eram o que o relato dizia, e a investigação achou **dois bugs que ela não reportou**.
- ✅ **(2) Importação quebrada — o template oficial MESCLA o cabeçalho.** No FM 7.2.1.5 (REV 1) o título "Nome do Produto/Product name" está mesclado em **A4:B4**; o `sheet_to_json` entrega o valor na primeira coluna e vazio na segunda, então `produto` era mapeado para a coluna **A** — onde vive a numeração pré-preenchida. Toda linha caía no guard de nome numérico (149 erros, 1 "produto" chamado `Nº`). ⚠️ **O AD Foods, usado na regressão de 30/jul, NÃO tem a mescla** — por isso o defeito escapou. Fix olha o DADO, não a mescla: coluna eleita sem texto → procura à direita a primeira não mapeada que tenha. Verificado: PT_EN preenchido **0→3 produtos**, AD Foods **38 inalterado**.
- ✅ **(5a) 🐛 ACHADO NÃO REPORTADO: 2 certificados em prod com número `PENDING-<uuid>`.** O placeholder que deveria virar o número IT 4.2 depois da transação. Causa: em não-aves `GSO` e `UAE` caíam **ambos no índice 1**, então separar Golfo e Emirados gerava dois certificados com o MESMO número → unique violado → 500 → registro gravado com o placeholder (AGROCERA/Russas, 2 levas de 4 unidades, sempre a 4ª).
- ⚠️ **A regra do `.K.` veio da BASE, não de e-mail — e o Renato teve que apontar isso.** 1ª tentativa: adotar a convenção de aves para os dois lados. **Errado** — renumeraria 441 certificados reais. Cruzando `.K.` × `market_variant` em 203 certificações multi-certificado: **não-aves 162/162 sequenciais, ZERO buracos**; **aves 31/41, com 10 buracos** (`1,3`, `1,3,4` — buraco só existe com índice FIXO). ⇒ as duas regras **são diferentes de propósito**; não-aves passa a usar o **ORDINAL** e a colisão some por construção. Bate com o único caso real que separa os dois mercados: `UAE (1) | GCC (2) | BPJPH (3) | MS (4)`. **Nada renumerado.** *(Lição: havia histórico na base; perguntar foi o erro.)*
- ✅ **(4) Data de vencimento deixa de ser digitável.** A Lina e a Elaine chegaram à mesma conclusão por caminhos diferentes — a vigência é da CERTIFICAÇÃO. O campo editável só permitia divergir da regra em silêncio. Agora é derivada e exibida como texto plano. Comprovado no teste dela: emissão 04/08/2026 → validade 20/07/2028 = ciclo + 3.
- ✅ **(5b) Células da tabela de produtos centralizadas.** `const ty = y + 2` desenhava TODAS no topo; com embalagem de 6 valores a linha ficava alta e o nome do produto colava em cima. A célula mais alta continua em `y+2`, então linha de uma linha só sai idêntica. Nos 2 renderers de certificado — os 2 de approval não têm tabela de produtos. ⚠️ `product-table.renderer.ts` tem o mesmo defeito mas é **código morto** (só o spec o importa).
- ✅ **(6) CNPJ do fabricante validado.** O campo aceitou `22` e IMPRIMIU no certificado. Valida só quando o texto é numérico — fabricante estrangeiro (VAT/EIN, com letras) segue livre. ✅ **A outra metade do pedido já era o comportamento correto:** com o fabricante em branco o certificado imprime só o bloco único da empresa, sem rótulo de papel — os dois blocos só aparecem se preenchido (ela preencheu com "a" no teste). **Nada a construir.**
- ✅ **(7) DT 7.3 (HAS) sempre por último, antes das normas.** Saía em ordem de inserção das categorias (`7.1 · 7.3 · 7.9`). Quando nenhuma categoria a derivava já ia para o fim pelo `push`; o defeito era quando alguma derivava. `sort` estável. ❓ aplicado a TODOS — falta a Lina confirmar se vale só para industrializados.
- ✅ **(3) Cochonilha por produto — FECHADO ponta a ponta (05/ago).** A regra já existia (tabela populada em 22/jul da Soha: **GSO e UAE rejeitam; BPJPH, MUIS e MS aceitam**) e o FM 4.1.X ANEXO 1 REV 03 já traz as variantes "com/sem conchonilha". Faltava saber **quais produtos** a contêm — dado que não existe em formulário nenhum. ✅ **3a** elo M:N `scope_products` ↔ `restricted_ingredients` (migration aditiva idempotente) · ✅ **3b** `restrictedIngredientCodes` no DTO + `GET /certificates/restricted-ingredients` · ✅ **3b-front** chips por produto na emissão, com tooltip do efeito (`dcaf55dc`) · ✅ **3c** separação AUTOMÁTICA (`18e9e3b6`): roda DEPOIS dos dois eixos de split e reparticiona por **assinatura** (quais produtos cada norma aceita) — GSO+UAE juntos seguem 1 cert (mesma assinatura); norma que rejeita tudo não gera doc vazio; sem produto marcado = caminho intacto. Verificado por simulação nos 6 casos críticos. Numeração acomoda (`.K.` ordinal em IND).
- ✅ **(8) Ficha da MP de referência — painel de uso (FM 7.4.2.7).** ⚠️ **Qual FM vale saiu da base, não de pergunta:** há **18 entregas reais de clientes em `sih-docs/Reuniões/MP E ESCOPO/`, todas FM 7.4.2.7** (a mais recente 23/jun) e **zero** do 7.4.2.9, que é a variante **Latam**. O modelo do GC já cobria **15 das 16 colunas** desde a FAM-0017 Fase 3 — era problema de EXPOSIÇÃO, não de modelagem. Novo `GET /raw-material-masters/:id/usage` + `usageCount`/`expiredEvidenceCount` na lista (agregado numa consulta só, 559 ms em prod). ✅ **v2 (`f7233dd3`, após o Renato estranhar 8 linhas iguais):** linha clicável → registro de origem em `/homologacao-mp/mp/:id` (a origem é o REGISTRO de MP da empresa, não um certificado); agrupamento visual (empresa não re-imprime; "〃 mesmo registro"); badge **"N registros duplicados"** quando a mesma empresa tem o mesmo código em vários registros.
- 🚨 **ACHADO GRAVE, quantificado pela primeira vez: 188 de 785 evidências halal de MP estão VENCIDAS**, em **75 das 335** matérias-primas. É exatamente o que a auditoria interna pegou e a Soha reportou em **30/jun** ("certificados de MP vencidos passando pelo analista") — cinco semanas no backlog sem número. ✅ **Decisão Renato 05/ago: AVISAR, não travar. Quem regulariza é o ANALISTA.** Já visível na lista (badge vermelho) e na ficha (faixa de resumo).
- ✅ **Espaço em tela (pedido do Renato, 05/ago).** A ficha usava `max-w-2xl mx-auto` — 672px fixos, **layout de modal servido como página**. ⚠️ A correção NÃO foi alargar o formulário (campo de 1800px quebra o rastreamento da leitura): o vazio existia porque **faltava conteúdo**. Em edição virou 2 colunas — formulário 420px fixos + painel de uso ocupando o resto; em criação segue estreita, porque abrir painel vazio só para ocupar largura é pior que o branco. *(Renato cogitou chamar o design; combinado: esta tela serve de referência, e a passada de sistema — escala/densidade/tabelas nas ~18 telas + o "layout clean" do Dib — fica para o designer.)*

**🎯 ESPÉCIE DAS PLANTAS + BLOCO DO GOLFO (03/ago, retorno da Giovanna). Espécie: EM PROD (dado). Seletor: front `37e0474d` — ✅ EM PROD (verificado por git 05/ago: está em `origin/release`, subiu com a cadeia de 04-05/ago; o "NÃO pushado" era o retrato de 03/ago). 🔧 [Renato] avisar a Giovanna p/ validar.**
> Método que valeu: a Giovanna reportou *"acredito que ainda não tenha sido realizado o cadastro completo da unidade BRF 466"* + *"está ocorrendo com outras unidades também"*. Medir antes de responder mostrou que **não era uma planta, eram 227**. ⚠️ Na reunião a resposta dada foi "é questão de cadastro" — certo na natureza, **errado na escala**.
- ✅ **BUG DE DADO: 227 dos 234 abatedouros (97%) sem espécie — não emitiam certificado.** A tela barra com *"A planta selecionada não tem espécie cadastrada (aves/bovino)"* porque a espécie define a DT (7.2.1 aves × 7.2.2 bovino), o agrupamento de normas e a numeração. **194 deles já tinham certificação** — plantas em operação que não conseguiam reemitir. Causa: `seed_source` NULL em 210 (lote de ETL antigo) — a coluna nunca foi populada no seed, não é planta esquecida.
- ✅ **Preenchidas 198 em 2 passadas, sem digitação:** (1) **89 pelo SIH** casando **SIF+CNPJ** (dado curado, é o que o supervisor usa em campo) — 82 bovino + 7 ave; (2) **109 pelo SIGSIF/MAPA** casando **CNPJ** (`CATEGORIA_CLASSE` nomeia a espécie: "ABATEDOURO FRIGORÍFICO … AVES" × "… BOVINO") — 53 ave + 56 bovino. **Abatedouros com espécie: 7 → 205.**
- ⚠️ **A armadilha do SIGSIF quase entrou:** a 1ª passada casava por SIF quando o CNPJ não batia, e trouxe *"Frigomerc S.A, Est Nº 2 — Capitán Lombardo"* (**paraguaia**) como "SIF 2 → ave". Número de estabelecimento estrangeiro **colide** com SIF brasileiro. Refeito com trava `country='BR'` + CNPJ de 14 dígitos ⇒ **18 estrangeiras (AR/PY) excluídas corretamente**. *(Confirma [[reference_sigsif_mapa_fonte_reconciliacao_cadastro]]: SIGSIF só vale para BR.)*
- ✅ **Corrobora a regra do Renato ("não há planta mista, ou é AVES ou BOVINOS"):** 0 ambíguos nas duas fontes — nenhuma planta apareceu com 2 espécies.
- 🔧 **Restam 29** = 18 estrangeiras (AR/PY, o SIGSIF não cobre) + 9 sem match + 2 só por SIF (quarentena, CNPJ divergente). ⇒ **[FAMBRAS] tipar essas 29 na mão** — ou fonte externa para as estrangeiras.
  - ✅ *(05/ago)* **Curadoria PRONTA para enviar** — `scratchpad/CURADORIA-29-plantas-sem-especie.csv`: cada linha traz **sugestão do sistema + a evidência que a fundamenta**. **18 das 29 se decidem pelos PRODUTOS DO ESCOPO** (`CHICKEN CUTS`→ave · `BEEF CUTS`→bovino), 2 só pelo nome (fraco), **9 sem pista** (8 delas sem certificação = baixa urgência). ⚠️ **A regra "ou aves ou bovinos" NÃO cobre tudo** (3 casos reais): **Remanso** = só ovino+caprino (`FROZEN GOATS CUTS`/`SHEEP OFFALS`) · **Victoria** = bovino **e** cordeiro (`LAMB CUTS`) · **Boi Verde** = graxaria (sebo bovino + óleo de vísceras de frango). O enum `Species` já tem ovino/caprino/bufalino ⇒ é decisão de cadastro, não de schema. *(Achado de método: minha 1ª regex casou "ovino" dentro de **bOVINe** e inventou 8 falsos "bovino+ovino" — fronteira de palavra corrigiu.)*
- ✅ **Seletor de mercados — os 5 do Golfo saem da lista individual** (pedido da Giovanna: *"sem repetir os países novamente depois, pois eles não serão selecionados individualmente"*). ⚠️ **O fix óbvio de 1 linha estava errado:** há **473 certificações em prod com seleção PARCIAL** do bloco (1 a 4 dos 5). Escondendo os chips sem mais nada, o país marcado ficaria **invisível e sem como desmarcar** (`gulfAllSelected` é `every()` → o atalho apareceria apagado com dado por baixo). Resolvido normalizando a hidratação: qualquer um dos 5 gravado ⇒ marca os 5. **Não muda o PDF** — o `fambras-market-resolver` colapsa os 5 num rótulo único e todos são GSO. `tsc -b` 0 + `vite build` ok.

**🎯 GATE DE DEPARTAMENTO + BRF CAPINZAL (03/ago). SIH: planta EM PROD. GC: fix `79553f48` commitado, ⚠️ NÃO pushado.**
> Método: o pedido do Vitor era "cadastrar uma planta". Conferir contra o **banco dos dois sistemas** antes de responder separou **três** problemas que chegaram como um — e dois deles não eram o que o chamado dizia.
- ✅ **A planta não existia — e o cadastro-mestre já tinha tudo.** SIF 466 não estava no SIH (só BRF Dourados SIF 18 e Nova Mutum SIF 4567). Mas o **GC tinha a planta completa**: `BRF S.A. — Capinzal/SC (SIF 466)`, CNPJ `01838723015400` (DV conferido), endereço, grupo. ⇒ **Antes de pedir dado ao cliente, consultar o GC.** Criada no SIH: id `fd29f4ef-25eb-4a03-8ac0-42250bc4653c`, `type=frigorifico` + `capabilities={processamento}` (entra no form de abate **e** no de produção industrializada), `species={ave}` (confirmado pelo Renato; o GC estava vazio), `division=IN`. SIH 126→127 plantas. Verificado por query nos filtros reais dos dropdowns.
- 🕳️ **Não existe sync de cadastro GC→SIH.** `gc-integration.service` só tem `rawMaterialsByPlant` e `plantSummary`, e **ambos partem de planta que JÁ existe no SIH** (casam por SIF+CNPJ). Nenhuma rota cria planta a partir do GC ⇒ as 127 plantas do SIH foram digitadas à mão. **É a causa raiz deste chamado e dos próximos.** Feature nova, colide com o congelamento de escopo de agosto — ❓ decisão.
- ✅ **BUG: planta com `specialtyArea` NULL sumia das listas do GC para todo o staff.** O gate de 24/jul filtra por `specialtyArea: { in: [dept,'ambos'] }` — e no Prisma **`in` não casa NULL**. Com 120 plantas de `specialty_area` nulo em prod, **88 dos 563 grupos ficavam invisíveis para qualquer usuário com departamento setado** (só admin/`ambos` via). Estava replicado em **3 serviços**: `company-group`, `company` e `certification`. Corrigido nos 3 com o ramo `{ specialtyArea: null }`. Medido no banco de prod: frigorífico **127→216**, industrial **370→456**. Testes 293/294 (a falha do `certificate-pdf.service.spec` é **pré-existente** — confirmado pondo o fix no stash). Sem rota nova ⇒ swagger/API Gateway não precisam ser regenerados.
- ⚠️ **O fix NÃO resolve o caso BRF, e isso está correto.** As 13 plantas da BRF são `frigorifico` explícito (não NULL) e as 24 certificações são todas `department='frigorifico'` — coerente com "ambas do André" (§5). Logo analista `industrial` **não vê o GRUPO BRF por regra de negócio**, não por bug. Ver §4.1.
- ❓ **Tensão com a decisão de 24/jul.** Ficou registrado que a segregação é **por CERTIFICAÇÃO, não por planta** — mas as 3 listas gateiam **primeiro** por `Plant.specialtyArea`; o ramo `certifications.some.department` vem depois. Na prática quem decide é a classificação da planta. Mudança de regra, não de bug ⇒ decisão do Renato.
- 🧩 **A FAZER — inverter a ordem do gate (decisão 24/jul MANTIDA em 03/ago).** Como a regra é "por CERTIFICAÇÃO, não por planta", os 3 serviços devem avaliar `certifications.some.department` **primeiro** e só cair em `Plant.specialtyArea` como *fallback* — hoje é o contrário. Arquivos: `company-group.service.ts`, `company.service.ts`, `certification.service.ts` (o `certification` já tem a forma certa: `department` primeiro, planta no fallback — **usar ele de modelo**). ⚠️ Muda quem vê o quê em prod ⇒ medir antes/depois no banco, como foi feito com o fix do NULL. Não entrou no lote de 03/ago de propósito.
- ✅ **Vínculo dos supervisores: MANUAL pelo Renato** (decisão 03/ago) — nenhum SQL a preparar.

**🎯 LOTE 7 — comparação com o CERTIFICADO ORIGINAL da FAMBRAS (Phibro × Química Real, 31/jul). EM PROD: back `5443be13`→`65285045`→`b1583244`→`d6e011a9`→`a07cf8ce`→`30aa732e` · front `9f098ce0`.**
> Método que produziu tudo abaixo: o Renato mandou o **original da FAMBRAS**, o **gerado pelo sistema** e o **print do cadastro**. Comparar os três, em vez de ler o relato, deu causa raiz em cada item. ⚠️ **Contém MIGRATION** (`20260731120000_certificate_manufacturer`, 3 colunas aditivas em `certificates`) — conferir se aplicou no deploy.
- ✅ **CAMPO NOVO — fabricante ≠ empresa certificada** (era o "NOVO PEDIDO Fuad/Dib 27/jul; não previmos, precisa do campo" — Renato). O original tem DOIS blocos: `Manufactured by: Química Real` e `Exported / sold by: Phibro`, cada um com CNPJ + endereço. Sem o campo, o analista **empilhou as duas empresas na razão social** — e **era isso que causava a sobreposição do item (a)**, não o layout. Agora: `manufacturerName/TaxId/Address` no `Certificate` (texto livre = snapshot imutável; o fabricante raramente existe como Company) + 3 campos na emissão manual. Vazios ⇒ layout idêntico ao de sempre.
- ✅ **Item (b) endereço incompleto — RESOLVIDO, e não era dado faltando.** O cadastro tinha o endereço completo; o merge é que era objeto-a-objeto (`plant.address || company.address`) e a planta importada do FM 7.8.x traz `{city, state}` — truthy, vencia o `||` e derrubava rua/bairro/CEP da empresa. Provado em prod: Phibro `company.address` completo × `plant.address = {"city":"Guarulhos","state":"SP"}`. Passa a mesclar **campo a campo**.
- ✅ **"Certified since" saía igual à data de emissão** — a derivação pegava o cert mais antigo da empresa e, com a base zerada em 28/mai, o mais antigo era ele mesmo. Passa a excluir o próprio documento e, sem cert anterior, cai no **início do ciclo**.
- ✅ **Identificação encolhe como BLOCO** (`renderScaledBlock`): razão social longa empurra as linhas seguintes sem mudar corpo; só se o bloco inteiro não couber é que entra um fator, aplicado às três juntas. A 1ª versão encolhia só a linha que transbordava e **invertia a hierarquia** (razão social menor que o CNPJ) — reprovada pelo Renato.
- ✅ **Fundo em 300 dpi** — o `bg-certificate-portrait.png` (656×928 p/ A4) trazia um **retângulo branco sobre a moldura do logo** (resto de recorte) + serrilha. O hi-res já estava no repo desde 16/jul e **o renderer árabe já o usava**. Enquadramento conferido antes (faixas turquesa batem em ~1–2,4 pt). PDF 260 KB → 426 KB.
- ✅ **Datas seguem o rótulo** (gabarito) em vez de coluna fixa `x=260` com árabe em offsets estimados: mede inglês+árabe e flui. `Certified since` 260→197,5 · `Initial cycle` 260→260,2 · `Expiry` 260→158,1.
- ✅ **Linha separadora do rodapé** (fio 0,4 pt cinza). ⚠️ **NÃO reverte a NC 22/jun da Elaine** — ela mandou tirar a linha **VERDE**; o gabarito tem um fio **neutro**. 🔧 **[Renato] validar com a Elaine**; se ela vetar, são 3 linhas para remover.
- ✅ **QR centralizado sobre o texto DESENHADO** (207 pt), não sobre a caixa de quebra (280 pt) — a 1ª tentativa deixou o QR 36,4 pt à direita. x 177→140,6.
- ℹ️ **Verificado e NÃO é defeito:** o espaçamento do árabe (espaço = 2,21 pt; a largura da string bate com a soma das palavras). O aspecto "colado" era do recorte em baixa resolução. *(Eu havia levantado como suspeita — retirada.)*
- 🚨 **PADRÃO SISTÊMICO — 3 achados do dia têm a MESMA raiz:** fundo em alta, alinhamento das datas e (antes) o bloco DT/normas foram corrigidos **em um renderer só**. Os 4 renderers **não herdam de base comum** ([[feedback_pdf_renderers_dont_inherit_base]]), então toda correção é replicada à mão e uma escapa. 🧩 **Avaliar consolidar o cabeçalho dos 2 renderers de certificado antes do go-live** — não é refactor cosmético, é o que está gerando retrabalho achado pelo cliente.
- 🔧 **[Renato] ainda sem diagnóstico:** itens **(c) marca do produto não aparece** e **(d) "número 2" indevido** — os dois vivem na **página 2** (anexo), que ainda não foi enviada. Sobre (c), o código diz que a coluna "Product Brand" é **sempre desenhada** no renderer padrão ⇒ suspeita de **dado** (produto sem marca no escopo), não de render.
- ❓ **Rótulo da categoria diverge:** original diz `Category K – Production of biochemicals materials (drugs and medications)`; o `CATEGORIAS.docx` do Fuad (aplicado em `e363f18d`) diz `K Production of (Bio) Chemicals`. Confirmar com o Fuad qual é o oficial — e se o prefixo "Category" entra.
- ❓ **Vigência ancorada no CICLO, não na emissão:** o original tem emissão 21/jul/2026, ciclo 20/mai/2024 e **validade 20/mai/2027** = ciclo + 3 anos. Nossa sugestão de formulário é emissão + 3 anos (decisão 3 de 30/jul: "manual por enquanto"). O gabarito sugere revisitar — no caso Phibro, a diferença passaria de 2 anos.

**🎯 LOTE 8 — consolidação + 3 fechamentos (03/ago). EM PROD: back `7a38a363`→`258f0bb2`→`0f946f42` · front `a5e2f978`.**
- ✅ **CONSOLIDAÇÃO dos renderers** (`certificate-common.ts`) — pedido do Renato ("consolide já") após 3 achados do dia terem a mesma raiz. Fundo, datas, vigilância, assinatura+QR e rodapé passam a ter fonte única; geometria que difere entra por parâmetro. **−215 linhas / +38.** Ao consolidar apareceram **4 divergências novas**, todas decisões de 07/jul que só existiam no renderer árabe e que o padrão vinha ignorando desde julho: linha do rodapé (0,8pt #444 × inexistente), corpo do rodapé (7,5 × 5,5), cores (#222/#555 × #333/#999) e assinatura (x=335/195 × x=350/175). *(A linha que eu havia adicionado em 31/jul com 0,4pt #999 era invenção minha — prevaleceu a do gabarito.)* E uma correção no sentido inverso: o **árabe** centralizava o QR pela caixa de quebra, o mesmo desvio reprovado no padrão; agora ambos centralizam pelo texto desenhado.
- ✅ **Vigência ancorada no CICLO** — fecha a decisão 3 de 30/jul com fundamento, não por convenção. Renato: *"lembra da diferenciação entre Certificação e Certificado? aqui já te responde a pergunta"* — a validade é da **Certificação**; o **Certificado** é o documento, reemitível dentro do ciclo, e herda a validade. Confirmado pelo gabarito (Phibro: emissão 21/jul/2026, ciclo 20/mai/2024, vencimento **20/mai/2027**). A sugestão do formulário passa a ancorar em `initialCycleDate`, caindo na emissão só sem ciclo; o aviso de ">3 anos" mede da mesma âncora e diz de onde conta.
- ✅ **Item (c) marca do produto — FECHADO.** Diagnosticado com a **página 2** do original: a coluna "Product Brand" é uma **célula mesclada** ("Phibro" uma vez, centralizado, cobrindo as 11 linhas). O nosso repetia linha a linha. `sharedBrandOf()` detecta marca idêntica em todos e desenha célula única; marcas diferentes ⇒ comportamento antigo. Vale no modo inline e no anexo paginado (a célula fecha e reabre na quebra de página).
- ✅ **Roadmap público em dia** (`updatedAt` 13/jul → 03/ago, 4 entregas descritas em linguagem de negócio). ⚠️ **Regra descumprida por mim em 8 lotes:** o roadmap público deve ser atualizado **no mesmo commit** da entrega.
- 🔴 **Item (d) "número 2" — ÚNICO do bloco do Fuad ainda aberto.** Falta a **página 1 do certificado GERADO** que ele reprovou (o original não serve: o defeito é nosso).

**⚠️ O que continua aberto do LAYOUT industrializado (Fuad, 27/jul):** ~~(a) nome cobre o número~~ ✅ Lote 7 · ~~(b) endereço incompleto~~ ✅ Lote 7 · ~~(c) marca não aparece~~ ✅ Lote 8 · **(d) "número 2" indevido acima do CNPJ — ÚNICO ABERTO** (diagnosticar: é o `.2.`/índice ou artefato de render?). O Renato ia atacar pixel-a-pixel em 28-29/jul contra o original que o Fuad enviaria. **Prazo real = 12/ago (presencial).**

**GC · Trilha A (emissão):**
- ✅ **#3 multi-CATEGORIA — FECHADO (16/jul), nada a fazer.** Decisão: vale o **§5.8** (Clonar manual). O aviso ao operador **já existe** no guard-rail `e1f92785` (`ManualCertificateEmission.tsx`): templates diferentes (7.7.1×7.7.2) **bloqueia** com *"Emita certificados separados usando 'Clonar de um certificado'"*; mesmo template com tipos diferentes **avisa**. O split automático (base `####` por categoria, Minerva 1430×1431) foi **descartado** — é automação, não correção. Se um dia voltar: **não rodar em paralelo com `marketScopes`** (§2).
- ✅ *(16/jul)* **#9 `marketScopes` na emissão manual — FEITO ponta a ponta** (decisão do Renato de seguir). Seção 6 "Emissão e mercados" ganhou **Mercados de destino**; marcar destinos grava `MarketScope(país × normas do grupo)` → `extractRequirements` passa a resolver as normas pelo **CATÁLOGO** (FM 4.1.X, §5.11) em vez do fallback genérico. Ex.: **Indonésia** lista 8 normas; **Brasil (Mercado Interno)** sai **só com a DT** (antes imprimia norma indevidamente).
  - Hashes: back `847006e6` (DTO + persistência) + `d2ec0623` (blindagem `@IsIn`, ISO2 inválido virava 500) · front `ea8e76fa`. **Pushados em `release`.**
  - **Sem destino marcado = comportamento atual** (fallback) → zero regressão.
  - 🚩 **Gap conhecido:** só os **12 países com mapeamento 1:1 verificado** (BR, SA, AE, BH, KW, OM, QA, YE, ID, MY, SG, TR). Os agrupamentos **"Demais países da <região>"** (Américas/Europa/África/Ásia/Oceania) **ficaram de fora** — exigem seletor de país completo; não foi chutado país "representante" (seria dado errado no `MarketScope`). Ao adicionar, estender também o `@IsIn` do DTO.
  - 🔧 **[Renato] Validar:** emitir cert marcando **Indonésia** (deve listar as 8 normas) e outro marcando **Brasil** (deve sair só a DT).
- ❓ **F3 — nº do certificado = nº do CONTRATO** (Lina). Sequenciamento destravado (`.K.` validado, §3.3), **mas o requisito não fecha** — escopado em 16/jul:
  - `Contract.contractNumber` **existe** (unique, FK `certificationId`) e o schema diz *"mesmo número da proposta aceita (**IT 4.2**)"* → encaixa com o `.K.` (base = nº do contrato + `.K.` da norma = padrão Minerva `1430.1`/`1430.2`).
  - 🚩 **Bloqueio 1:** a **emissão manual não tem contrato** (cria `Certification` synthetic pulando proposta→contrato) → sem fonte para o número. Operador digita? Passa a exigir contrato?
  - 🚩 **Bloqueio 2:** Minerva teve **2 bases (1430 × 1431) para 2 categorias**. Se base = nº do contrato, são **2 contratos**? Ou 1 contrato com base por categoria?
  - ⇒ **Perguntar à Lina/FAMBRAS antes de codar.** Mexe em numeração: não tocar junto com `.K.`/`marketScopes`.
- 🟡 **F2 — aprovar a CONFECÇÃO do certificado — FATIA A + A.1 (DEPARTAMENTO) EM PROD; curadoria FM 7.8.x EXECUTADA.** ⚠️ muda o fluxo: **emissão manual cria RASCUNHO → gestor da área aprova** (tela `/analista/aprovar-certificados`, menu do gestor). Doc: **`ADR-APROVACAO-CONFECCAO-CERTIFICADO-F2-2026-07-24`** (§8; §8.1 revisado pós-reunião frigorífico).
  - **Fatia A** (24/jul): back `013c40d0` (+schema `f0047fba`) · front `1ccfbf91`. Portão no nível do DOCUMENTO, ortogonal ao Comitê; rascunho→aprovar→ativo; segregação §4 (auto-aprovação auditada); "travar"=snapshot Fatia 0. Rotas `GET /certificates/drafts`, `POST /:id/{approve,reject}` — `/certificates/{proxy+}` já cobria (sem regen).
  - **Fatia A.1 — DEPARTAMENTO da certificação** (25-26/jul, reunião frigorífico 24/jul): back `b0722b43`+`bafadbb3` · front `4e3e07ab`; reconciliado (back `81cfe2b2`, front `dba642b2`). **Segregação é CONTRATUAL** (processo-duplo é padrão — BRF Capinzal, Minerva JSB): `Certification.department` (`ambos` proibido) é a fonte do gate; `Plant.specialtyArea` rebaixado a default. Rótulos: **In Natura (Frigoríficos) / Industrializados** (⚠️ **Dib é PESSOA** — diretor de IND — nunca sigla). Editável na C-F2 (auditado) + dto na emissão. 240 testes; tsc 0 nos 2.
  - ✅ **Curadoria pelas planilhas FM 7.8.x EXECUTADA em prod (26/jul, output literal):** 7.8.1=IND · 7.8.2=FRIG; **1.118/1.181 por match EXATO de nº de certificado** + 48 por SIF/CNPJ → migration backfillou (296/885) e a curadoria corrigiu **29** → **final 293 In Natura + 888 Industrializados (100% com depto)**. SQL versionado `prisma/2026-07-25-curadoria-department-fm78x.sql`. ⚠️ **14 ambíguas herdaram da planta** (não NULL): BRF Capinzal SIF 466 + Minerva JSB SIF 451 (seed fundiu 2 processos em 1 certificação c/ 2 certs — **separar em 2 certificações = decisão manual pendente**) + emissões de teste `2607.*`.
  - 🔧 **[Renato] validar E2E:** emitir → rascunho → "Aprovar Certificados" → aprovar → ativo · reprovar c/ motivo · certificação de depto industrial NÃO aparece pro gestor frigorífico · briefar FAMBRAS (emitir→aprovar; perfis do André/Fuad → gestor + specialtyArea).
  - 🟡 **Fatia A.2 — visibilidade do departamento:** ✅ *(EM PROD 27/jul)* **corte por departamento** nas listas de **certificações, empresas e grupos** (usuário frigorífico não vê industrializado) + **notificações de vencimento recortadas por depto** (queixa André "juntou todas"). Padrão do `listDrafts` (`Certification.department` + fallback `Plant.specialtyArea`; empresa/grupo derivam via plantas); JWT passou a expor `specialtyArea`. Default `ambos`+admin vê tudo → zero regressão. back `614ebb77` (+fix teste `253cea27`). 🧩 **resíduo:** badge nas listas/detalhe · KPIs/`statistics` e `/expiring` dedicado seguem **globais** (só a lista+notif foi cortada). ⚠️ [Renato] setar `specialtyArea=frigorifico` nos perfis do André/equipe (hoje `ambos`) p/ ver o efeito. 🧩 **Fatia B:** PDF de rascunho (marca + QR inválido) · numeração indicativo→definitivo (⚠️ André: nº definido no comitê deve ser MANTIDO) · editar-após-reprovar. Converge o antigo "caminho feliz #7" (preview pré-oficialização).
  - 🚧 **AUDITORIA DO FLUXO F2 (27/jul, sessão pós-follow-up) — furos de UX achados por código + prova em prod.** O gate de rascunho **está correto no back** (evidência prod: emissão de teste do Fuad de hoje caiu `PHI.GRS.2607.0037.2.BRA` = rascunho, certificação `em_solicitacao`/`industrial`; só 1 rascunho × 1424 ativos). Mas o **laço de UX está incompleto**. Achados + plano em 3 lotes:
    - **A** — o painel de emissão dizia **"Certificado emitido"** (check verde + "Verificação pública QR") quando é **rascunho não aprovado**. ✅ *(Lote 1A, front `81e52f6c`)* `ManualCertificateEmission.tsx`: vira **"Rascunho criado — aguardando aprovação do gestor da área"** (âmbar/Clock), QR **desabilitado** ("após aprovação"), botão "Baixar rascunho (PDF)". tsc 0.
    - **C** — `DraftApproval.tsx` não tinha **como VER o PDF** → gestor aprovava no escuro. ✅ *(Lote 1C, front `81e52f6c`)* botão **"Ver PDF"** por rascunho (reusa `getDownloadUrl` presign existente). tsc 0.
    - **D — bugs do UserForm** (edição de usuário staff): ✅ *(back `cecce87e` / front `81e52f6c`)* (#2 perda de dado) `user.service.ts` `findOne`+`findAll` **não retornavam** `specialtyArea`/`additionalRoles` → form hidratava com default (`ambos`/`[]`) e o próximo save **clobberava** (padrão "4 camadas" [[feedback_login_response_must_match_ui_display]]); +2 campos no `select`. (#1 senha) `UserForm.tsx` campos de senha sem `autoComplete` → autofill do navegador forçava "confirmar senha"; agora **atrás de "Alterar senha"** + `autoComplete="new-password"`. tsc 0 nos 2.
    - ✅ **E — dados/perfis (RESOLVIDO 27/jul):** André/Fuad promovidos a `gestor` pelo Renato; **Fuad estava `specialty_area=frigorifico` (clobber do bug do UserForm) mas é INDUSTRIAL** → `UPDATE` em prod aplicado (output literal: frigorifico→**industrial**, rowcount 1, commit ok) — agora aprova cert industrial. André já estava `gestor`/`frigorifico`/`['analista']`. **E-mail do Fuad:** ele usa **2 endereços**; o da base (`@fambras.org.br`) é válido — **mantido, correto** (não era divergência).
    - ✅ **B — sinal ao gestor (Lote 2, back `cecce87e` / front `81e52f6c`):** **B1** badge âmbar com contador de pendentes no menu "Aprovar Certificados" (`Sidebar.tsx`: `useQuery` → `listDrafts().length`, só gestor/admin, refetch 60s). **B2** notificação **in-app** ao(s) gestor(es) da área na criação do rascunho — `certificate.service` injeta `NotificationService` (@Global) e chama `createForMany` no fim do `manualEmit` (fire-and-forget, try/catch; `role=gestor` ∧ `specialtyArea ∈ {department, ambos}`; `NotificationType.info`; link `/analista/aprovar-certificados`; `sendEmail:false` por ora). Spec do certificate ganhou mock do `NotificationService` (13/13). Sem rota pública nova → sem regen API GW. tsc 0 back+front.
    - ✅ **F — integridade do PDF de rascunho (Lote 3/Fatia B, back `89860c0f`):** **3A** marca d'água "RASCUNHO / DOCUMENTO NÃO EMITIDO" nos 3 renderers quando `ctx.isDraft` (helper `drawDraftWatermark`; `isDraft` flui `generateAndStore`[status==rascunho]→`generatePdf`/`renderFromSnapshot`→ctx). **3B** `verify` do QR retorna `valid:false`+"documento não emitido" p/ rascunho, sem vazar dados. **3C** `approveCertificate` regenera o PDF (`forceRegenerate`) do snapshot congelado → sai **sem** marca (fire-and-forget). tsc 0; **49/49** testes certificate; smoke test watermark %PDF ok. ⚠️ **cert aprovado NUNCA recebe watermark** (sem `bufferPages` — não toca o caminho do emitido). 🧩 **limitação:** multi-página (7.7.2 ≥9 produtos) carimba só a última página; 🔧 **validação VISUAL do PDF pendente [Renato]** (não dá p/ conferir por tsc).
    - 🔧 **[Renato] deploy pendente (Lote 0) — commits prontos, sem push:** GC back `cecce87e`+`89860c0f` · GC front `81e52f6c` · docs `e045acc`(+este). Push `release` back+front dispara CI/CD GC. Nenhuma rota nova → sem regen API GW (confirmar diff-zero). **Antes/junto:** `UPDATE` do Fuad→industrial (E). Validar E2E: emitir→"rascunho aguardando"+watermark→gestor vê badge/notif→Ver PDF→aprovar→ativo limpo+QR valida; QR do rascunho = "não emitido"; segregação frig×ind.
**🥩 REUNIÃO DEPTO FRIGORÍFICO 24/jul (André/Giovanna/William — transcrição `reunioes_fambras/2407_1100`) — itens triados:**
- ✅ *validados por eles:* norma automática por mercado · busca por SIF. ✅ *estrutural resolvido:* processo-duplo → `Certification.department` (F2 A.1, acima).
- ✅ *(EM PROD 27/jul)* **Espécie dirige a DT (P0 da reunião):** a lógica de DT/agrupamento/numeração **já ramificava por espécie**; o gap era `Plant.species` vazio → default bovino em silêncio. Fix pela FONTE: **trava** a emissão de abate (CV) sem espécie (banner + submit + guard back) e **exige espécie** no cadastro de frigorífico/abatedouro. back `ec2f050c` · front `48a73778`. 🔧 [Renato] validar aves→7.2.1 / bovino→7.2.2.
- ✅ *(EM PROD 27/jul)* **Trava normativa Malásia × desossa:** só-desossa (`facilityType=deboning`) + Malásia (MS/MY) → aviso âmbar ao vivo + bloqueio no submit + guard no `manualEmit`. back `295380a6` · front `7758b769`. (Par W15 aves segue aberto.)
- 🟡 **Bug derivação `standard` p/ nacionais:** ✅ *(EM PROD 27/jul)* **o "card mente" foi consertado** — o card mostra as **normas acreditadas reais do escopo** (chips) + destinos, no lugar do rótulo coarse do organismo (2055-2=selo GAC, §5.22-c). front `4ba11e4a`, **sem tocar na derivação**. ❓ A **derivação** em si (nacionais MS/BPJPH/MUIS derivam GSO?) segue na decisão Soha §4.3-1.
- 🟡 **SIF + cidade + UF:** ✅ *(EM PROD 27/jul)* (a) **lista de empresas** com SIF·Cidade/UF na frente + busca por SIF + filtros na URL (persistem ao voltar): back `31735663` · front `5c283b38`. 🧩 resíduo: (b) confirmar cidade na busca da EMISSÃO; (c) o filtro que se perde ao voltar TAMBÉM na tela de **certificados** (só a de Empresas foi feita).
- 🧩 **Validar prefill do Emitir-da-certificação em prod** (André: "não está preenchendo"; código pushado desde 24/jul — suspeita cache de bundle, mesmo sintoma do GC-04).
- 🧩 **Espécies de aves (nomenclatura):** pato, galinha d'angola etc. — só muda o nome no escopo, processo igual (André).
- 🧩 **Menor:** geoloc do histórico de acesso errada (André no Wi-Fi da empresa aparece fora). ❓ **[Renato] perfis:** André/equipe estão como `analista` — p/ aprovar (F2) precisam `gestor`+`specialtyArea` corretos.
- ✅ *(DECIDIDO Renato 27/jul + EM PROD)* **#3 multi-categoria (Giovanna):** emitir 2+ categorias **numa ação só** (certs separados, cada um com base própria). Checkbox na Seção 4 → N emissões independentes (front `4af0ad85`). §5.8/Clonar continua disponível.
- ✅ *(DECIDIDO Renato 27/jul + EM PROD)* **"Objeto da certificação"** ocultado e fixo em "produto" no caminho **frigorífico** (André: "evitar margem pra erro"); industrializado mantém a escolha (front `2bb6d34e`).
- 📅 **Renato presencial 10-14/ago; reunião frigorífico QUARTA 12/ago** ("pegar na mão e pôr no ar"). Layout da habilitação (centralização Giovanna, categoria+subcategoria=G1/W17) fica **por último**, antes disso.
- ✅ *decisão confirmada:* cliente (JBS/BRF) **não valida** o SIH — sem acesso (confidencialidade; André).

**🎯 RETORNO DOS ANALISTAS FAMBRAS 30/jul (e-mails Guilherme Luz + Caio França, thread "Considerações Sistema Analistas"; Renato liberou acesso a mais analistas p/ validar o GC) — 13 pontos triados CONTRA O CÓDIGO, 6 lotes autorizados 30/jul.**
> ✅ **OS 6 LOTES ESTÃO EM PRODUÇÃO (31/jul).** back `02c251e0`→`e363f18d`→`ecf4f445`→`71760b1a`→`3731bc9f` · front `02c251e0`→`27bcb0c8`→`ced1955f`→`dc481dc3`. Todos pushados na `release` com pipeline verde. **Falta VALIDAR com a FAMBRAS** — e revalidar o retorno original, porque os analistas testaram uma versão sem o `81e52f6c` (ver §6).
> Hashes por lote: **L1** importação `02c251e0` · **L2** verify back `ecf4f445` + front `27bcb0c8` · **L3** renderers `71760b1a` · **L4** emissão `ced1955f` · **L5+L6** back `3731bc9f` + front `dc481dc3`.
> 🔧 **[Renato] pendente de validação em prod:** (a) importar a planilha oficial da AD Foods pela UI → 38 produtos, 0 erros; (b) escanear o QR de um rascunho → faixa âmbar "DOCUMENTO NÃO EMITIDO"; (c) emitir com 3 DTs + muitas normas → conferir que nada fica tampado; (d) reprovar um rascunho pelo Fuad → o analista recebe notificação e lê o motivo em "Meus Rascunhos"; (e) emitir com "Sem norma externa" → só DT + selo FAMBRAS. ⚠️ **Avisar os analistas do 6.3:** `analista`, `gestor`, `auditor` e `empresa` **perderam** a permissão de ABRIR reclamação (decisão 6) — quem tentar recebe 403.
> Evidências do diagnóstico: PDF do cert `ADFOODS TRSTE` (rascunho), print da tela `/verify`, print da assinatura na tela, e a planilha real `C:\HalalSphere\avaliacoes\Cópia de AD Foods - FM 7.2.1.5 - Controle de Escopo atualizado 2701.xlsx`. Replay do parser rodado contra o arquivo (script em scratchpad).
> **Decisões FAMBRAS que destravam os lotes: §4.3 bloco "Retorno analistas 30/jul".**

- 🧩 **Lote 1 · Importação de escopo (Trilha B — `BulkImportProductsDialog.tsx`).** **Causa raiz PROVADA por replay:** a eleição de cabeçalho pega **a primeira linha com qualquer célula casando `/produto|nome/`** — e a linha 1 da planilha oficial FAMBRAS é o título mesclado *"Listagem dos **Produtos** a Serem Certificados"*. Resultado: `colMap={produto:0}` = coluna **Nº** → o cabeçalho real vira o "produto" **`Nº`** (foi o que saiu no certificado do Guilherme) e as 38 linhas reais caem no guard de nome numérico → **1 produto, 143 erros**. Com as 3 regras abaixo: **38 produtos, 0 erros**. (1.1) cabeçalho = linha com **mais colunas mapeadas** (título casa 1, cabeçalho real casa 4); (1.2) coluna de índice (`Nº/N°/Item/#/Seq`) nunca vira nome; (1.3) linha sem conteúdo em **nenhuma** coluna mapeada **não é erro** — o template FAMBRAS traz a numeração **40→144 pré-preenchida e vazia** (sem essa regra, 105 erros falsos); (1.4) mensagem de erro passa a exibir os títulos lidos + CSV aceita vírgula; (1.5) **Title Case só quando o texto vier TODO em minúsculas** (decisão 1) — hoje deforma 33 dos 38 nomes (`INNOVA OL100`→`Innova Ol100`, `OL2C`→`Ol2c`, `20 kg`→`20 Kg`); **código sempre preservado**; (1.6) regressão com a planilha AD Foods.
- 🧩 **Lote 2 · Verify público + número (Trilha A).** O QR **funciona** — o backend achou o cert e devolveu a mensagem de rascunho; quem mente é a **tela**, sem estado para `rascunho`: cai no `else` genérico e pinta **"CERTIFICADO NÃO ENCONTRADO"** em vermelho (`VerifyCertificate.tsx:109-117`), com a linha em inglês **hardcoded** (`:256`) contradizendo a mensagem PT vinda do backend. (2.1) estado âmbar **"DOCUMENTO NÃO EMITIDO / Not Issued"**; (2.2) backend devolve `messageEn` (fonte única bilíngue); (2.3) normalizar número na emissão manual (sem espaço interno — o `ADFOODS TRSTE` sobreviveu via `%20`, mas é frágil). ⚠️ push na `release` do front dispara **2 pipelines** (app + `halalsphere-verify-web`).
- 🧩 **Lote 3 · Sobreposição no PDF (Trilha A — os 4 renderers).** Bloco DT+normas cresce a partir de Y fixo, mas a âncora seguinte é **fixa** → colisão. Confirmado nos 4: `certificate-renderer.ts:124-140` (DTs em 315, "For the following products" cravado em **370**) · `certificate-arabic-renderer.ts:156-169` (âncora 360) · `approval-renderer.ts:138-152` e `approval-arabic-renderer.ts:139` ("Products/Scope" em 300). Aritmética do caso real: 3 DTs (315/326/337) + normas em 3 linhas (348/359/**370**) = colisão exata. **Não depende só do nº de DTs** — multimercado com muitas normas estoura com 1 DT só. (3.1) medir altura e reflowar **só no estouro** (§5.1 layout congelado: caso normal sai idêntico); (3.2) replicar nos 4 (não herdam de base — §feedback); (3.3) specs 1 DT × 3 DTs+6 normas; (3.4) PDFs de amostra p/ o Guilherme **antes** de publicar.
- 🧩 **Lote 4 · Tela de emissão (Trilha A — `ManualCertificateEmission.tsx`, 3 itens no MESMO arquivo → SERIALIZAR).** (4.1) vencimento recalcula a partir da emissão — hoje o default é `hoje+3a` calculado 1× na abertura e **não acompanha** a data de emissão (`:365-370`), errando a vigência em cert retroativo; (4.2) opção agrupada **"Demais países do Golfo"** marcando BH/KW/OM/QA/YE — o backend **já colapsa** os 5 no rótulo único da FM 4.1.X (`fambras-market-resolver.ts:19`), individual é só a seleção; (4.3) **emissão sem norma externa** (decisão 4): hoje a tela exige ≥1 norma (`:1205`), mas o catálogo já modela via **`dtOnly`** e o destino "Brasil (Mercado Interno)" já existe → liberar p/ sair **só DT + selo FAMBRAS**.
- 🧩 **Lote 5 · Reprovação visível ao analista (F2 Fatia B parcial).** `rejectCertificate` grava o motivo **só** em `CertificationHistory` (`certificate.service.ts:1578-1586`) — **sem notificação** e sem tela onde o analista leia o motivo (relato do Caio testando com o Fuad). (5.1) notificar `issuedById`; (5.2) motivo exibido junto ao rascunho; (5.3) lista/badge de reprovados p/ o analista. **Reabrir o rascunho p/ edição com re-resolução (Fatia B completa) fica FORA** — maior, não bloqueia validação.
- 🧩 **Lote 6 · UX curto.** (6.1) menu da seção Certificação em ordem de processo (`Sidebar.tsx`; sugestão do Guilherme: Análise Documental → Homologação de MP → NC → Emitir → Certificados → Certificações → Reclamações); (6.2) **"Analista Responsável"** na TELA do certificado (`Certificate.tsx:513-525` — rótulo fixo "Auditor Responsável" recebendo `issuedBy`; **o PDF NÃO muda**, decisão 5 — o documento leva só o signatário canônico §5.4); (6.3) registro de reclamação **restrito à Qualidade** (decisão 6) — hoje `complaint.controller.ts:17` permite `POST` a admin/gestor/analista/**empresa**/auditor/gestor_auditoria/qualidade.

**Fora deste ciclo (do mesmo retorno):** ❓ **escopo diferente por categoria** (Caio 2 — existe split por categoria com **mesmo** escopo e existe partição por **produtos** (`productGroups`), falta o cruzamento; ~2-3 dias, pedir caso concreto K×CII → **pós-go-live**) · ❓ **rótulo de categoria em inglês por padrão** no PDF (saiu `K - Fabricação de materiais bioquímicos` num doc EN/AR; destravado pelo `CATEGORIAS.docx` do Fuad — ver §4.3) · ⚠️ **erro na emissão relatado pelo Caio (item 1) — SEM EVIDÊNCIA**, a função está em prod (foi ela que gerou o cert dele): pedido print ou data/hora p/ puxar log · 🔧 **login de cliente de teste** (Guilherme + Caio) — escrita na base do GC, **depende de autorização do Renato**.

**Já funcionava — o analista não achou (sem ação de código):** **SMIIC em cert separado** (Caio 4) = função "múltiplos certificados por **grupo de normas**" (`normGroups`, com botão de sugestão); o próprio número do cert dele (`QUI.VNC.2607.0041**.3**.ECU`) traz o sufixo de grupo. **Reclamações não é visível a todos** (Caio 6): backend já restringe listar/ver a admin/gestor/analista/gestor_auditoria/qualidade. **Golfo já sai agrupado** no certificado (Caio 5). *(Nota: o rótulo `DEMAIS_GOLFO` tem "Kwuait"/"Iemên" — cópia **literal** da coluna B da FM 4.1.X, usado como **chave de busca** no catálogo, não impresso. Corrigir exigiria código + migration de seed juntos → **deixar como está**.)*

**📋 FOLLOW-UP SEMANAL FAMBRAS 27/jul (só Fuad/Dib/Nilsa presentes — Soha/Lina/André/Elaine convidados mas NÃO falaram; reunião leve, Dib: "hoje tá meio vazio"; transcrição Fireflies `01KXYDPNW54WF6RVNCP51PWJFA`) — triado:**
- ✅ *VALIDADO por Fuad ao vivo (evidência p/ §4.3-1):* **nacionais + selo vazio corretos** — emitiu com Indonésia+Malásia+Singapura, "apareceu corretamente" e o **selo saiu VAZIO** ("Indonésia, Malásia, Singapura não tem selo… apareceu o vazio, então tá correto"); "aqui deu **todas as normas corretas**". ⇒ a **lógica de normas/derivação nacionais→GSO + selo-por-grupo (§5.22-g) está validada pela FAMBRAS**. *(O que ele reprovou é LAYOUT, item abaixo — não a lógica.)*
- 🟨 **REPROVADO por Fuad — LAYOUT do PDF de INDUSTRIALIZADO** — *(**status 31/jul: (e), (f) e (g) FECHADOS; (a), (b), (c) e (d) SEGUEM ABERTOS.** (e)+(f) = a sobreposição de DT/normas, corrigida pelo **Lote 3** do retorno dos analistas (`71760b1a`, os 4 renderers, EM PROD) — o Guilherme reportou o MESMO defeito 3 dias depois, por outro canal; (g) PT→EN fechado por `e363f18d`, **pushado 31/jul**.)* — texto original: **ainda quebrado (os MESMOS erros da semana passada; o "card mente" `4ba11e4a` foi só p/ frigorífico):** (a) nome de empresa longo **cobre o número** da empresa · (b) **endereço incompleto** · (c) **marca de produtos não aparece** (G2/W14) · (d) aparece um **"número 2" indevido** ("acima da CNPJ") — ⚠️ diagnosticar se é o `.2.`/índice ou artefato de render, encosta no "Fuad `.1.`" · (e) **muitas DT-73/74 sobrepõem** (overflow de altura, mesma classe do fix dinâmico do SIH) · (f) **normas "ficaram feias, sobrescrevendo"** · (g) texto em **PT onde devia ser EN**. 🔧 **[Renato] atacar o LAYOUT do PDF industrializado hoje/amanhã (28-29/jul), pixel-a-pixel contra o original** (Fuad vai enviar o certificado original + o gerado). Renato: *"não mexi no PDF, mexi até chegar nele"* — lógica ok, render não. Converge com o backlog de render (G1/W17, G2/W14, G4/W8, W9 caps, W10 sigla) — **agora com reprovação fresca + prazo duro (âncora 10-14/ago).**
  - ✅ **[FAMBRAS/Fuad] lista de categorias em INGLÊS ENTREGUE** — `reunioes_fambras/2707_1100/CATEGORIAS.docx`: **19 códigos GFSI/FSSC completos em EN** (AI Farming of Animals… · CV Animal slaughtering · **K Production of (Bio) Chemicals** · I Food Packaging · J Equipment… etc). **Mata a pendência "textos EN por categoria (só K confirmado)"** — destrava o (g) PT→EN. ✅ *(APLICADO E EM PROD — back `e363f18d`, pushado 31/jul)* `category-display-map.ts` GSO+SMIIC → **inglês** (19 do docx exatos; E/HI/HII/HIII/LI-LIV traduzidos, marcados p/ confirmar). Vale p/ **novas emissões** (cert emitido congela no snapshot). tsc 0; 49/49. ⚠️ **typo na FONTE:** "CII" aparece **2×** (o 3º é "Processing of perishable animal and plant products (mixed products)" = deveria ser **CIII**) — usar CIII no de-para, não replicar o erro.
- 🆕 **NOVO PEDIDO (Fuad/Dib) — empresa × FABRICANTE no certificado (marca própria / terceiros):** algumas empresas têm **nome da empresa + nome do fabricante** (ex.: FIBRO [detentora] × Química Real [fabricante]) e o cert deve exibir **os dois** ("fabricado por"). Dib: *"marcas próprias e terceiros — vai começar a acontecer muito"*. ❓ **[Claude/Renato] verificar se o dado existe no fluxo de certificação** (Renato: "informação nova, fui pego desprevenido, não lembro se tenho esse dado") — pode exigir campo novo. **Não codar antes de confirmar a fonte do dado.** Só industrializado.
- ❓ **NOVO (Dib, diretor) — layout MENOS POLUÍDO / mais "clean":** Dib incomodado com o cert **e a habilitação de frigorífico** "muito poluídos, tudo junto"; quer algo mais harmônico, idealmente **1 página**, e usar o **QR Code p/ os detalhes** ("menos é mais; para maiores detalhes leia o QR"). ⚠️ **Tensiona o §5.1 (layout CONGELADO)** — mas é o **próprio diretor** pedindo. Renato: "vou pensar e sugerir". ⇒ tratar como **proposta de estudo**, não como release; qualquer layout novo ainda passa por qualidade → Dr. Mohamed → acreditadores (§5.1). Não é autorização p/ mexer no gabarito acreditado.
- ❓ **NOVO — DT-73/74 nos nacionais = decisão da QUALIDADE (Soha, ausente hoje):** Dib questionou por que imprimir as DT internas FAMBRAS (requisitos gerais) quando o destino já tem norma nacional própria (BPJPH/SNI). Fuad: a **DT identifica o TIPO de produto** (7.1 alimentício · 7.4 químico · 7.5 farmacêutico), por isso entra. Renato propôs **consolidar o "General Requirements for FAMBRAS HALAL" repetido numa lista única** (ganha espaço) + QR p/ detalhe. ⇒ **[FAMBRAS] levar à qualidade/Soha na próxima reunião** (formato das normas no cert). Sem decisão = mantém como está.
- 🔧 **[FAMBRAS — Fuad/Dib/Nilsa] validar o fluxo rascunho→aprovar (F2)** no lado INDUSTRIALIZADO (gate: André=frigorífico / **Fuad=industrializado**, "pelo menos p/ testar; depois define se é você, outra ou +1 pessoa"). Fuad ainda **não testou**; Renato "acho que já habilitei, fiquei na dúvida" — **[Renato] confirmar que o draft-gate está ativo no ambiente do Fuad**. Idem testar multi-categoria/objeto/card do lote 24/jul no industrializado.
- 🔧 **[FAMBRAS — revisão a 4 mãos, AINDA NÃO FEITA] validar a normalização de cadastro** (Trilha B): Renato pediu Fuad/Dib/Nilsa sentarem e revisarem **empresas → produtos → escopos** ("dor de cabeça do Minerva S.A. × Minerva S.A."). É o **dono do dado** que valida. ⇒ pré-requisito p/ fechar a normalização; nada a codar, é entrega da FAMBRAS.
- ✅ *reconfirmado:* **presencial 10-14/ago** (frigorífico já pegou **12/ago**; industrializado/qualidade a encaixar); **[FAMBRAS/Fuad+Dib] montam o cronograma interno** das áreas p/ a semana. Meta declarada por Renato: "sair com o sistema funcionando" na semana de 14/ago.
- ℹ️ *contexto SysHalal (não-ação):* JBS cobrou ajustes de filtro via Nilsa; Renato **não mexe na versão atual** — "SISBOV/Cisralau V2 sai p/ setembro, não faz sentido mexer agora".

- ✅ *(16/jul)* **`base-template.renderer.ts` deletado** — `353a0b79`. Zero consumidores confirmado por grep (a classe só aparecia na própria declaração); carregava caminho de `userPassword` e foi a origem da divergência entre renderers. ⚠️ *(corrigido 17/jul — §6)*: o doc dizia "commit local, push pendente do OK"; **já estava em `origin/release`** (`git branch -r --contains` confirma) e agora também em `origin/develop` pela reconciliação. Mais uma de doc-relata-intenção × git-relata-fato.
- 🧩 `main.ts` — último "HalalSphere" interno (Swagger title + log de boot). Baixa.
- ⏸ **Parkeados:** parser xlsx (aguarda arquivo de escopo real da Lina) · emissão assíncrona (rebaixada — o "timeout dos 151" era shadow-copy do OneDrive, não escala) · DSM/IFF cert-de-produto (atrás da digitalização do escopo da indústria).

**GC · Trilha A — 🎯 KERNEL DE NORMAS + backlog de emissão (reunião 20/jul + testes Giovanna/William + WhatsApp Soha 22/jul).**
> ✅✅ **VALIDAÇÃO FINAL 23/jul — T1–T7 TODOS FECHADOS.** 1ª rodada Nilsa (22/jul) OK; reteste 23/jul reabriu T3/T4/T7 → **Renato reproduziu ao vivo pós-deploy do lote `977a909c`+`02729ca3`**: **T3 ✅** preset intersectou (Minerva bovino → 1 grupo GSO+UAE; PDF `MIN.BRS.2607.0032.1.BRA` com `GSO 2055-1/2015+993`, GAC+ENAS, DT 7.2.2) · **T4 ✅ motor completo** — Único c/ GSO+BPJPH **bloqueou** com a mensagem certa, e o caminho "Vários por grupo" **splitou em 2 certs** `…0033.1.BRA` (GSO+993, selo GAC) e `…0033.2.BRA` (KEPKABAN/PEPRES, **SEM selo** — regra Elaine viva) — *a reprovação da Nilsa era o TOAST genérico/enganoso, não o comportamento* · **T7 ✅** — o item "PDF bloqueia cópia" já validado pelo Renato em 16/jul (Nilsa só não sabia). **Banco 100% por SQL (outputs literais).** Pendências residuais: provar `first-render-freeze` (reimprimir 1 cert pré-22/jul) · confirmar se as 4 emissões `.1.BRA` de 22/jul eram frigorífico (senão, bug `.1.` do Fuad na fila).

**🎯 GESTÃO DE ESCOPO — lote 23/jul ENTREGUE (back `7ce654fd` · front `5ebe7dd4`):** **Opção A** (analista vê TODAS as 1.177 certificações — o escopo forçado por `analystId` escondia o seed inteiro; era o "apenas 6???" do Renato, confirmado por SQL) · **Sidebar** "Certificações" p/ analista+gestor (faltava só neles; URL na mão) · **paginação** 20/pág + **KPIs via `/statistics`** (antes calculados da página ≤20 = mentiriam) · **"Emitir certificado" no detalhe da certificação** → emissão pré-preenchida pelo **escopo VIVO** (F1-corrigido): planta, categorias, normas, destinos, produtos+marcas — fecha o ciclo corrigir-escopo→emitir · **spec do F1 consertado** (quebrado EM SILÊNCIO desde 13/jul — mock sem `$transaction`; 223/223 agora). 🔧 [Renato] validar: lista completa como analista + paginação + botão Emitir hidratando.

**🎨 UX EMISSÃO v2 — ✅ itens 1-6 ENTREGUES (front `5571ac71`); resta só o #7 (❓PO).** Toast honesto (fala a regra violada) · aviso âmbar NA MARCAÇÃO · "Usando sua seleção: …" no bloco de grupos · tipo de instalação OBRIGATÓRIO em habilitação (default bovino segue em branco — André 07/jul) · "Tipo de Certificação"→"Objeto da certificação" · entrar no "Vários por grupo" já sugere os grupos. tsc limpo. Lista original (histórico):
1. 🧩 **Toast honesto** — "campos obrigatórios não preenchidos" quando é **regra violada** é enganoso; o toast deve repetir a mensagem específica (ou rolar até ela). *(Foi o que reprovou o T4 da Nilsa.)*
2. 🧩 **Prevenir na marcação, não só no submit** — aviso ao vivo assim que GSO+BPJPH coexistirem em contexto de habilitação (pedido do Renato no T4).
3. 🧩 **Normas marcadas somem ao trocar p/ "Vários por grupo"** sem aviso (estado persiste, tela não mostra) — exibir "usando sua seleção: GSO, UAE.S" no bloco de grupos.
4. 🧩 **Droplist de instalação (seção 8)** sem explicação e **opcional em silêncio** — PDF de habilitação sai **sem a frase da facility** (aconteceu no teste do Renato: `…0032.1.BRA` sem "HALAL SLAUGHTERING…"). Tornar obrigatório (ou default) quando FM 7.7.1.
5. 🧩 **Dois "Tipo" colidentes** — "Tipo de Certificação" (produto/processo/serviço) × "Tipo de certificado" (habilitação/único). Renomear um.
6. 🧩 **Auto-sugerir grupos ao entrar no modo "Vários por grupo"** (com a intenção normas∪destinos) — elimina o erro-tardio que o Renato tomou no submit.
7. ❓ **[PO] Caminho feliz frigorífico** — caso mais comum exige as 10 seções; atalho "emitir habilitação padrão" pré-preenchendo 2/4/5/8. Decisão de produto.
> Arquitetura completa: **`halalsphere-docs/ARCHITECTURE/ADR-KERNEL-NORMAS-CERTIFICADO-2026-07-22`** (§8). Decisão travada = **§5.22**. **Nada codado — zero hash.**

_Raiz (stress-test + inventário 22/jul):_ o `Certificate` **não persiste as normas resolvidas** — o PDF re-resolve ao vivo (`certificate-pdf.service.ts:685-737`), lendo catálogo + `category-display-map` (que vaza o rótulo `2055-2` para a linha de norma). É o bug do 2055 **e** uma violação de imutabilidade ISO 17065 já hoje.

- ✅ *(22/jul)* **Fatia 0 — `f9d22111` (⚠️ commit LOCAL em `release`, push pendente do OK).** `Certificate.resolvedScopeSnapshot` (JSONB, migration `20260722190000` aditiva/idempotente) congela normas+produtos+selos+categoria+variante+datas **write-once** no `generateAndStore`; cert emitido renderiza **só do snapshot** (`renderFromSnapshot`) — input não troca template de cert emitido; legado congela na 1ª impressão (`first-render-freeze`). `sealsByIds()` sem re-filtro de allowlist (imprime o que foi emitido; correção=novo nº, §5.3). Sem rota nova → sem regen API GW. tsc limpo; spec **13/13** (3 novos). Achado no caminho: `extractProducts` também re-resolvia vivo (o `productsSnapshot` antigo nunca foi lido pelo render) — com Edição de Escopo F1 em prod + `CERTIFICATE_REPRINT_FORCE_REGEN=true`, a violação estava armada; **pós-deploy o force-regen vira operação segura** (layout refresh, conteúdo imutável). 🔧 **[Renato] Validar pós-deploy:** emitir cert novo → conferir snapshot gravado (SQL: `SELECT certificate_number, resolved_scope_snapshot->>'source' FROM certificates WHERE resolved_scope_snapshot IS NOT NULL`) · reimprimir cert antigo com force-regen → 1ª vez congela (`first-render-freeze`), 2ª idêntica · migration aplicada.
- 🚧 **Fatia 1** — em curso:
  - ✅ *(22/jul)* **1.1 — fallback fonte ÚNICA + W1** (⚠️ commits LOCAIS em `release`, push pendente): back `1c83a68c` (mapa duplicado → `data/standard-labels.ts`; `deriveStandardFromStandards` põe UAE na família GSO) · front `e386c449` (`deriveStandard` idem — "Sem norma acreditada" some ao marcar só UAE.S). Nacionais seguem VOLUNTARY até a §4.3-1. tsc limpo nos 2; jest 28/28.
  - ✅ *(22/jul)* **1.2 — `dt_only` tipado + 3 reparos** (`c685f6ec`, LOCAL): migration `20260722210000` (coluna + backfill + reparos idempotentes); `lookupStandardsFromCatalog` decide por `row.dtOnly`. **Reparos de dado achados na leitura do seed:** (a) DT 7.5/7.6 interno tinham o sentinela DENTRO de `standards` → cert de fármacos/cosméticos interno **imprimia "Só deixar a DT…" como norma**; (b) Singapura DT 7.1 rotulada "(velha…)" nunca casava; (c) resolver grafava "(com cochonilha)" ≠ seed "(com conchonilha)". jest 30/30.
  - ✅ *(22/jul)* **1.3 — totalidade país→mercado** (`5f1db8b3`, LOCAL): `Record<Country,…>` **exaustivo** — país novo no enum sem mapear = erro de compilação; achados e fechados os buracos **JO/LB/IQ/IR** (→ Demais Ásia; Golfo da FM é lista fechada); OTHER = irresolúvel explícito; destino irresolúvel **logado** (era silencioso). Spec do resolver varre o enum inteiro (regressão permanente). jest 36/36. ⚠️ `@IsIn` do DTO segue nos 12 países — expandir o seletor da UI é fatia própria (produto).
  - ✅ **SQLs 1-3 de validação de banco CONFERIDOS pelo Renato (22/jul, output literal):** 3 migrations aplicadas (15:06/17:27/19:09Z) · seed conchonilha 5 regras exatas · 10 linhas dt_only (todas interno; DT 7.5/7.6 com standards={} = reparo OK; 7.2.1/7.2.2 fora = correto). ✅ Singapura DT 7.1 renomeada confirmada. ✅ **SQL #4 (snapshots): FUNCIONANDO em prod** — 4 certs emitidos 22/jul 19:26–19:36 todos com snapshot `source=emission` (output literal). ⏳ Falta provar `first-render-freeze`: **reimprimir 1 cert pré-22/jul** e re-rodar o SQL. 🚩 **Os 4 números terminam `.1.BRA`** — se algum for industrial único, é o **bug do `.1.` do Fuad ainda vivo** (numeração; confirmar tipo das emissões antes de subir o fix na fila).
  - ✅ *(22/jul)* **Fatia 2 — ingredientes restritos** (back `15f77a57` + front `cd4ad539`, EM PROD): **2 tabelas NOVAS** `restricted_ingredients` + `restricted_ingredient_standard_rules` (migration `20260722230000`; seed conchonilha: GSO/UAE rejeitam, BPJPH/MUIS/MS aceitam, OIC **sem regra de propósito** — Soha não falou de Turquia); checkbox "contém carmim (E120)" na emissão → resolve variante "(com conchonilha)" do catálogo DT 7.1 + **aviso W11** (banner) quando norma selecionada rejeita; snapshot congela o flag. Curadoria via DBeaver → futura UI Catálogo. jest 36/36.
  - 🟡 **UI "Catálogo de Normas" — FATIA A (ingredientes restritos) FEITA, commitada, SEM PUSH (09/ago): back `80dde176` · front `4aeadb8b`.** Fecha a metade `restricted_ingredients` da fatia futura (decisão Renato 22/jul: "dá mais segurança à FAMBRAS"). ⚠️ **`certification_standards_by_market` NÃO entrou** — é a Fatia B, segue DBeaver-only.
    - **Contrato TRI-ESTADO, não booleano:** `accepted`/`rejected`/`undeclared`, sendo `undeclared` a **ausência de linha** (o estado que o kernel trata como silêncio). Um checkbox transformaria "ninguém decidiu" em "a norma rejeita" — barraria mercado sem decisão da FAMBRAS. O aviso disso aparece na lista **e** no form, e o "sem posição" não usa a família visual de "rejeita".
    - **`PUT /:id/standard-rules` da matriz inteira** em vez de POST/DELETE por norma (padrão do gêmeo `critical-raw-material`): o operador decide as 6 numa sentada e 6 chamadas deixariam estado intermediário incoerente em prod se uma falhasse no meio. Normas ausentes do array ficam intocadas.
    - **Condição de segurança #4 do ADR atendida** por migration aditiva/idempotente `20260809120000_restricted_ingredient_provenance` (`approved_by` + `source_doc_ref` + `effective_from`), na **REGRA e não no ingrediente** — quem declara "GSO rejeita" pode não ser quem declara "MS aceita", com documentos e datas distintas. Provenance da única linha existente preenchida (cochonilha = Soha 22/jul). Histórico reusa a **`audit_trail` genérica** (`AuditTrailEntity += restricted_ingredient`), sem tabela nova — e a ficha exibe.
    - **Sem DELETE de propósito:** apagar cascatearia em `scope_product_restricted_ingredients`, que é dado de escopo curado pelo analista. Inativar preserva e já tira da emissão; o modal consulta antes **quantos produtos de escopo estão marcados** e mostra o número (inativar muda a separação automática).
    - Escrita `admin` + `qualidade`; leitura para staff logado. Menu nos dois perfis; seção "Catálogos (Homologação MP)" → **"Catálogos"** (deixou de ser só de MP).
    - Prefixo NOVO → **swagger + 3 JSONs do API GW regenerados no MESMO commit** (166→168 paths; zero rota removida). Pelo precedente de 08/ago (§4.1) o gateway tem catch-all com CORS ⇒ import no console deve ser opcional, **confirmar por sondagem** pós-deploy.
    - Validação: `nest build` + `tsc --noEmit` limpos · `tsc -b` + `vite build` limpos (revalidados APÓS os commits paralelos do Renato nos menus) · **jest 800/801**.
    - 🚨 **O baseline do jest estava errado no doc: era 788/790, não 789/790.** O `06a62cf3` (em `origin/release` desde 08/ago) passou a gravar `emailVerified` no `resetPassword` e a asserção do `password.spec` ficou parada. Corrigido no mesmo commit.
    - 🔧 **[Renato] falta:** (1) aplicar a migration; (2) validar E2E — declarar uma posição, conferir o histórico, e checar que trocar para "Sem posição" **apaga** a regra; (3) decidir o **propor→aprovar** do ADR, que **NÃO foi implementado** (hoje quem escreve grava direto, com autoria registrada).
    - ⚠️ **A tela nasce quase vazia sem a FAMBRAS:** a tabela tem 1 ingrediente e 5 regras. A pauta para a Soha declarar as demais está em `halalsphere-docs/PLANNING/PAUTA-INGREDIENTES-RESTRITOS-POR-NORMA-2026-08-09.md` (§8) — ~45 candidatos, células **vazias de propósito**.
    - 🕳️ **Limites que a tela NÃO resolve** (medidos no código, não supostos): o **banner W11 na emissão está preso à cochonilha** (`dto.containsCarmine` + `code: 'conchonilha'` hardcoded no `certificate.service`) ⇒ ingrediente novo **separa** o certificado corretamente mas **não avisa** o operador; e a variante de DT `(com/sem conchonilha)` do **FM 4.1.X só existe para cochonilha** ⇒ se a posição de um ingrediente novo tiver que mudar a linha de norma impressa, o FM precisa ganhar a variante (decisão de documento da FAMBRAS). O que **já é genérico**: marcação por produto, `GET /certificates/restricted-ingredients` e a separação automática por assinatura.
- 🧩 **Fatia 2** — effective-dating por norma + provenance (`approved_by`/`source_doc_ref`/`effective_from`) + pin da versão na `Certification` no gate da **auditoria**; tabela **ingrediente restrito × mercado** + flag do operador (long-term via FAM-0017).

_Backlog de emissão (bugs dos testers — render/split, correm em paralelo ao kernel):_
- [ ] **W2/#1** GSO sai `2055-2` na linha de norma (deveria `2055-1`+`993`) — o vazamento; selo GAC mantém `2055-2:2021` (correto). [Fatia 1]
- [x] ✅ *(22/jul)* **W1** UAE.S "sem norma acreditada" — **corrigido** (back `1c83a68c` + front `e386c449`, locais): UAE.S = família GSO, acreditada. [Fatia 1.1]
- [ ] **G1/W17** Products/Scope: sai só a subcategoria, em PT; deve ser `CATEGORY <G> – <nome>/ SUBCATEGORY <cód> – <nome>` + facility + BRANDS, em **EN** (bloqueado pelos textos EN por categoria, §4.3). [Fatia 1]
- [x] 🔁 *(22-23/jul)* **G5/W6** Split de norma — **v1 `4130ac1a` (em prod) tinha BURACO no frigorífico**, pego pelo **vídeo da Nilsa** (T3 falhou de verdade, não era cache): no fluxo FM 7.7.1 o seletor de normas é **oculto** → `applicableStandards` vazio → a interseção não filtrava e o preset seguia sugerindo todos. **G5-v2 `02729ca3` (LOCAL):** intenção = normas marcadas **∪ normas derivadas dos DESTINOS** (Indonésia→BPJPH etc.) — era exatamente o que a Nilsa fez no vídeo (marcou destinos e o preset ignorou). W6 (trava no Único 7.7.1) mantido; com W4 (destino puxa norma mesmo oculto) o fluxo frigorífico fecha. **Bônus do vídeo:** UI vazava "**GSO 2055-2**" no chip/box de norma (norma do ORGANISMO, §5.22-c) → rótulos "GSO (Golfo)"/"OIC/SMIIC (Turquia/HAK)", display only. Roteiro T3 do artifact reescrito p/ fluxo real (via destinos). ⚠️ Reteste da Nilsa SÓ após o deploy do lote `977a909c`+`02729ca3`.
- [x] ✅ *(22/jul)* **W11** — entregue em 2 camadas: **trava** de grupos de habilitação no Único 7.7.1 (`4130ac1a`, G5/W6) + **aviso** de ingrediente restrito × norma (`15f77a57`+`cd4ad539`, Fatia 2). Eixos auditor/habilitação ficam p/ ciclo completo (§5.22-b).
- [ ] **W7** picklist de produto conforme FM 7.2.1.2/7.2.1.3 ("ticar" válidos) — em vez de texto livre. Conecta parser xlsx/escopo real.
- [ ] ❓ **W15** possível emitir aves só desossa OU só abate — **BLOQUEADO POR FALTA DE REGRA, não por código** (05/ago). É norma, e a base **não permite derivar**: `scope_facilities` está **vazia** e só **3 certificados** gravam `facilityLabel` no snapshot (todos bovino) — os 1.445 anteriores não guardam o tipo de instalação. A trava Malásia×desossa (`295380a6`, em prod) é o molde pronto: aviso ao vivo + bloqueio no submit + guard no `manualEmit`. ⇒ **[William/FAMBRAS] qual combinação espécie×instalação é proibida em aves?** Com a regra escrita, é implementação de 1 lote. **Não inventar trava normativa a 9 dias do go-live** (trava errada em prod = emissão parada).
- [x] ✅ *(22/jul)* **W3/W4/W5** — **entregues** (front `977a909c`, LOCAL): seção "Emissão e mercados" subiu p/ posição **2** com renumeração (W3) · marcar destino **puxa a norma** do país (W4; BR não puxa — interno=só DT; só adiciona, desligar não remove) · DTs **re-derivam** ao trocar categoria/planta — `dtTouched` era preso, causa raiz do "não seleciona as DT corretas" (W5). tsc limpo.
- [x] ✅ *(05/ago, back `10841636` — commitado, ⚠️ conferir push)* **W10 · G4/W8 · W14 FECHADOS por medição.** **W10** endereço saía `Guarulhos – SP – BR`; UF e país passam a sair por extenso e em inglês, normalizados **na origem** (`buildCompany`) e não em cada renderer. Medido em prod: o país está gravado de **6 formas** (Brazil 340 · BR 297 · Brasil 17 · BRAZIL 17 · Brazil. 1) — 🔧 **2 empresas gravadas como `Btazil`** saem intactas de propósito (adivinhar typo no render esconderia o erro de cadastro; **corrigir o DADO**). · **G4/W8** o árabe era posto por offset estimado: medido, **invadia o inglês em 2 linhas** (+2,3 e +3,2 pt) e **sobrava vão em 2** (−9 pt); rótulos passam a ser medidos e as 3 datas usam **coluna comum** (no renderer árabe a coluna 180 tinha só 6 pt de folga → cede para 186). · **W14 "marca não aparece" NÃO era dado:** o bloco cresce de y=300 e o do certificado começa em **y=370 fixo** — com ~12 produtos a linha de marcas caía em **y≈378, desenhada por baixo** do número (mesma classe do Lote 3). Espaço das marcas passa a ser **reservado**, medido no MESMO corpo escolhido (`chooseFittedSize`). Simulação: 3 produtos→corpo 9/y=325 · 12→7,5/356 · 25→6/359 · 63 (p95)→6/359, todos dentro do limite. 🚩 **LIMITE CONHECIDO: acima de ~21 produtos de nome longo a lista inline não cabe nem no menor corpo** — esse caso pede o template de CERTIFICADO com anexo paginado, não a habilitação (p95 em prod = **63 produtos/escopo**, então existe). Specs 15/15; suíte 789/790 (falha pré-existente).
- [ ] **G2** marca não centralizada (habilitação) — segue aberto. · ❓ **W9 "caps só na unidade"** — **não implementado de propósito:** a anotação não permite reconstruir o campo nem a regra. Pergunta enviada ao William (rascunho pronto). Não adivinhar caixa de texto em documento acreditado.
- [ ] **Fuad `.1.` — RESOLVIDO por evidência:** cert real mostra `.1.` legítimo em **frigorífico** (índice de grupo `ABC.SIG.ANOMES.SEQQ.NORMA.PAIS`); o bug é `.1.` em **industrial único** (CP Kelco). Não re-perguntar.

**GC · Trilha B (normalização):**
- ✅⭐ *(EM PROD 26-27/jul)* **Reconciliação do cadastro contra o SIGSIF/MAPA + auditoria do seed.** Motivada pela pergunta do Renato ("o seed não inferiu CNPJ no chute?"). **Auditoria concluiu: o import NÃO inferiu — vinculou por CNPJ literal e quarentenou ambiguidade.** Fonte oficial: `C:\HalalSphere\sigsif...csv` ([[reference_sigsif_mapa_fonte_reconciliacao_cadastro]]). **Das 322 plantas BR com SIF, 290 (90%) batem o MAPA**; 11 divergem, 3 SIF fora do MAPA, 18 estrangeiras (registro próprio). ⚠️ Lição gravada: **validar CNPJ pelo dígito verificador; não presumir lado; FM 7.8.x tem typos (Minerva `337`≠`377`), o MAPA é a régua.**
  - ✅ **Balde A+B EXECUTADO em prod** (SQL versionado `prisma/2026-07-26-normalizacao-cnpj-sif-mapa-baldeAB.sql`, commit GC `6969ae2a`): 5 estabelecimentos re-ancorados no MAPA — BRF Chapecó 104, Seara 490, SZR 3152 (CNPJ fantasma→oficial), Prima 177 (planta movida p/ empresa correta), **Minerva Araraquara 2240** (era entreposto mal-pendurado na matriz de Barretos → criada empresa própria `/0051-83`, tipo armazenamento). Transação, output antes/depois conferido, rowcount 1/1.
  - 🧩 **Resíduo (nada tocado — decisão Renato/FAMBRAS):** balde **C** (5 casos de dono/M&A — Frigomarca 4687, Hexus×Vidara 407, GTB→Aurora 1889, RKO 4516, Rio Branco 2) · **D** (registro-teste 1908 "GRUPO ECOHALAL") · 3 empresas Minerva `337…` (typo do FM, 1 planta inativa cada) · 3 SIF fora do MAPA (JBS Ponta Porã 3412, Supremo 267, Mafrial 547) · stub SIF-null da Prima. ❓ decisão Renato/FAMBRAS (re-parentam empresa/grupo).
- ✅ *(17/jul)* **Fallback CNPJ-only — FEITO nas DUAS pontas.** Planta sem SIF (químico/casing: Kin Master ×2, Minerva Casing = `NAO_APLICAVEL`) não casava por definição; agora casa por CNPJ.
  - **GC** `853ed242` (**pushado** em `release`, CI/CD disparado): `resolvePlant` — com SIF mantém SIF+CNPJ e devolve null se não casar (**divergência real não se mascara**); sem SIF cai para CNPJ **só se INEQUÍVOCO** (2+ plantas sem SIF no mesmo CNPJ → null, não chuta). Beneficia os 2 endpoints (`raw-materials/by-plant` + `plant-summary`). Rota existente → **sem regen de API GW**.
  - **SIH** `6daeff9` (**pushado** em `ecohalal/release`) — Trilha D tocada com OK do Renato (§0.3): o guard exigia `sanitaryCode` E `cnpj` e **derrubava a chamada dentro do SIH**, antes de sair; agora só CNPJ é obrigatório e o SIF vai vazio.
  - Reconciliado release→base: GC `a8f375b4` (develop) · SIH `0e42e70` (development) → **ahead=0** (§1, §3.4).
  - Validado contra prod: Kin Master e Minerva Casing resolvem 1:1; Rolândia (com SIF) não entra no fallback = **zero regressão**. tsc ok nos 2 repos.
  - 🔧 **[Renato] Validar pós-deploy:** abrir Kin Master/Minerva Casing no SIH → espelho e MP devem aparecer (antes davam "falta SIF e CNPJ").
- ✅ *(12/jul)* **Fix do import de produtos** — `BulkImportProductsDialog` (`parseAoa`/`parseCsv`) assumia ordem posicional sem cabeçalho e gravava a coluna "Nº" como nome (origem dos produtos numéricos). Agora **exige o cabeçalho do modelo** (sem ele: erro claro + 0 linhas, Aplicar desabilitado), **rejeita nome numérico** e tem **cap de 500** linhas. `3c620550` (release) + reconciliado develop `2a53b7ad`. *(não constava no §4 — trazido pela regra §0.5)*
- ✅ *(12/jul)* **Correções de dado no SIH** (`db_ecohalal_sih.plants`, sem hash — é dado): 3 chaves completadas **a partir do GC master** (BRF Nova Mutum +CNPJ `01838723049487` · Minerva Jose Bonifacio +CNPJ `67620377000386` · Curtume Jangadas +SIF `3471`) → **31→34 de 39** casando · **Minerva Casing**: removido **SIF 451 ERRADO** (451 é da Minerva principal `…0386`) · **Ecotrace Teste**: `isActive=false` (**tinha 1 registro → desativada, não deletada**).
- 🧩 Reenriquecer AR/PY/estrangeiras via SysHalal por CUIT/RUC.
- 🧩 Cadastrar no GC: Padoca Maricota + Kin Master Passo Fundo (`…0296`) · merge do dup Kin Master no SIH (**migrar, não deletar** — tem operação: 1 relatório + 1 origem-MP).
- 🧩 Lotes MP **N5b** (OUTROS) · **N5c** (GRUPO JBS consolidado) · INTERMEDIÁRIAS — das 28 planilhas FAM-0017 só Rolândia + N5 carregados.
- ❓ **`scope_brands`: rebuild total (opção b)?** O parse extrai 6.356 marcas mas com **ruído** — o 4º campo do FM é "comercial×marca" ambíguo (ADM = 21 nomes-de-produto × Forno de Minas = 6 marcas reais). Feito só o mínimo (opção c): 12 numéricas apagadas + 109 scopes do seed populados + 7 listas multi-marca splitadas → 2.699. **Rebuild total injetaria ~4 mil nomes-de-produto como marca** → decisão do Renato.
- 🧩 **`_ScopeProductBrands` (marca por produto) está VAZIO** — só há marca por escopo; produto↔marca nunca foi linkado.
- 🧩 **Badge UI da flag "validado FAMBRAS"** = `approved_by_id IS NOT NULL` nos `raw_material_masters` (hoje é derivável, não visível). Baixa.
- ⚠️ **Dívida:** os scripts de carga vivem no **scratchpad (efêmero, não versionado)** — as cargas de dado têm número antes/depois, mas **nenhum hash**. Se precisar rastreabilidade, é aqui.

**GC · Trilha C (escopo):** 🧩 **Fase 2 (C-F2)** — campos gerais (datas, norma, observações), `industrialCategories` M2M, ~~market scopes~~. Número segue **travado**.
- 🟡 *(23/jul)* **C-F2 IMPLEMENTADA e PUSHADA em `release` (CI/CD disparado) — aguarda validação do Renato.** back `d7212546` · front `647e7178` (ambos `origin/release`, 23/jul). ✅ **`release→develop` reconciliado** (back merge `383eabcb`, front `d533ab89` — ambos `ahead=0`; §1). Doc: **`halalsphere-docs/PLANNING/EDICAO-ESCOPO-CERTIFICADO-FASE2-REESCOPO-2026-07-23`** (§8).
  - **C-F2.1 back** (`d7212546`): `certification.update()` transacional — mantém `status_*` e passa a gravar `certification_updated` com diff `{campo:{from,to}}` (tipo/norma/notas/analista/datas-ISO; status fora do diff) · `updateScope()` transacional + `scope_general_updated` (controller passa `req.user.id`) · rotas de categoria (`industrial-classification`) transacionais com `industrial_category_added/removed/primary_changed` + **P2: primária sincroniza os legados** (`industrialGroup/Category/SubcategoryId`). **Zero rota nova (sem regen API GW), zero migration.** Specs: 148/148, tsc build exit 0.
  - **C-F2.2 front** (`647e7178`): novo `CertificationGeneralEditor.tsx` (modal), botão "Editar dados gerais" no card Informações (gate analista/gestor/admin); seções vigência (sanidade ano + avisos >3a/expira) · norma (GSO/SMIIC/BOTH/VOLUNTARY + justificativa) · observações+capacidade/turnos/funcionários · categorias M2M (listar/primária/remover/adicionar) + histórico no rodapé. `UpdateCertificationDto` ganhou `standard`/`standardNotes`. tsc -b exit 0.
  - **Decisões aplicadas (P1–P5 = sim):** observações = `standardNotes` + `scope.description`; sync legados c/ primária; capacidade/turnos/funcionários no modal; VOLUNTARY exposto. **Fronteira A/C** gravada no §2: `certificate/*`=A · `certification/*`+`certification-scope/*`+categorias do `industrial-classification`=C; **MarketScope 100% fora** (destinos = A); snapshot da Fatia 0 garante que nada alcança PDF emitido.
  - 🔧 **[Renato] Validar (roteiro 6 passos do doc §4/C-F2.3):** editar `validUntil` → aviso + status derivado + KPI · trocar norma+justificativa · add/primária/remove categoria (conferir legados) · observações · **histórico** (quem/quando/diff) · **prova da fronteira**: "Emitir do escopo" hidrata os valores NOVOS × reimprimir cert já emitido = PDF idêntico (snapshot intacto).
- ⚠️ **F2 (draft→aprovar→travar)** do bloco da Trilha A declara que "toca trilhas A e C" → quando andar, coordenar; não iniciar em paralelo. *(≠ C-F2 — desambiguação no §0 do doc de reescopo.)*

**GC · FAM-0017:** 🧩 **F4 — FK opcional `ScopeSupplier`/`SupplierHomologation` + RevisionLog** (*prioridade sobre F5/F6* — acreditabilidade ISO 17065) · F5 UI U7 `/homologacao-mp` · F6 import das 29 planilhas · seeds S1 (~40 certificadoras) e S2 (231 intermediários) → DBeaver quando curados.
- 🧩 **Validade/vencimento do certificado de MP visível + alerta ao analista** — *(reunião 30/jun, Soha; trazido ao §4 em 17/jul pela §0.5)*. A **auditoria interna pegou certificados de MP vencidos passando** pelo analista: mostrar a validade na listagem e avisar quando vencido. ⚠️ **≠** do "certs vencidos-mas-ativos (Gelita)" do §4.3 — aquele é **cert de habilitação**, este é **cert de matéria-prima**.
- 🧩 **Subir no menu a tela de busca do catálogo global de MP** — busca por produto/fornecedor + vínculos + documentos/avaliações anexados. A tela **existe**, só não está exposta no menu. Baixa. *(§0.5, 30/jun)*

**SIH:** ✅ *(23/jul)* ~~Regenerar swagger das rotas `auth`~~ — **verificado: NO-OP, item fechado sem código.** Os 3 `deploy/sih-api.*.json` já estavam atuais (regeneração local deu **diff zero**, byte-idêntico; última regen foi `4d202db`/`ab0314c`) e `/auth/{proxy+}` já roteava `forgot-password`/`reset-password`. O `swagger.json` raiz é **gitignored** (não versionável) e o controller usa Zod + `Record<string, unknown>` (sem DTO class → sem schema novo a emitir). Nada a commitar. · 🧩 acoplar MP aprovada à validação de produção/abate (❓ PO — **desenho fechado no bloco abaixo**) · 🧩 catálogo produtos 5A-2 (❓ aguarda .xlsx).
- ✅ *(23/jul, EM PROD) pacote de refinamentos:* back `ae10132` · front `6bc283a`.
  - **`formNumber` do abate DERIVADO da espécie** no service (antes o dado gravado podia ficar incoerente; o PDF já derivava). +teste.
  - **PDF de transferência** imprime o **Certificado Halal do destino** (`halalCertData`: número + empresa + status) quando existe — informativo, como a reunião de 17/jul pediu.
  - **NC — link contextual:** o aviso "preencher FM 7.1.6.1" do checklist agora leva ao NCForm **pré-preenchido** (planta + tipo + relatório de origem) em vez de form em branco.
  - ℹ️ **Descoberto que já estavam PRONTOS (backlog desatualizado):** **embarque multi-origem A/B/C** (`LinkedSourcesField`, datas em faixa, PDF com origens, snapshot na assinatura — commits `168f41e`/`d65361c`) e **a tela de NC** (CRUD + workflow + 8 seções do FM 7.1.6.1). `getDerivableSources` avaliado: **não mexer** (buscar por SIF ignorando `plantId` é correto — multi-origem tem produtos de outras plantas).
  - ✅ *(23/jul, EM PROD — pushado com OK do Renato, deploys disparados; release→development reconciliado: dev `6fe61c2`/`a29bb5c`, ahead=0)* **Resíduo do multi-origem FECHADO**: back `39d0e92` (rota `GET /shipping-reports/derivable-sources` sem `:id` — a busca já era global por SIF, o id era só check de existência; sub-path do `{proxy+}` → regen API GW rodada, **diff zero** nos 3 JSONs) · front `4deba7c` (`LinkedSourcesField` também na criação, vínculos vão no POST — o create já aceitava `linkedSources`; "Preencher da derivação" agora agrega multi-origem na tabela flat: SIFs distintos juntos + faixa de abate min→max, antes ficava em branco). tsc limpo nos 2; jest **32/32**. 🔧 **[Renato] Validar:** criar embarque NOVO já vinculando fonte por SIF → salvar → composição aparece; produto com >1 origem → "Preencher da derivação" → SIF "X, Y" + faixa min→max na tabela.
- 🧩 **destino no PDF de transferência** — ✅ *(23/jul, acima)*.
- ✅ *(23/jul à noite, EM PROD — front `993e8ef`, dev `f61b729`, ahead=0)* **Fix: bloqueio de assinatura MOSTRA o motivo (5 formulários).** Rodada de teste da Nilsa (SIH-03/04/05/08) triada com evidência de banco: o 400 no `/sign` do embarque `EM-SIF76/2026/00001` era a **trava C4 correta** (2 verificações C/NC em `null`; o DCPOA anexado às 16:20 satisfazia a trava do anexo; produtos tinham nome) — mas os 5 `handleSign` não tinham `onError` e **engoliam o erro** (modal aberto, zero feedback). Corrigido: modal fecha e o motivo sai no banner+toast padrão. **Não-bugs da mesma rodada:** SIH-05 = "tempo de retorno" é do ABATE/insensibilização (roteiro listava na seção errada) · SIH-08 = o botão "Preencher da derivação" só nasce após SALVAR (por desenho; bundle de prod conferido, código de 23/jul no ar). Roteiro artifact reescrito como acompanhamento (status OK/Falhou/Pendente + observações + resumo copiável). 🔧 **[Renato/Nilsa] Reteste:** assinar sem C/NC → mensagem aparece (item 1.4 do roteiro).
- ✅ *(27/jul, EM PROD — back `e518429` pushado com OK do Renato, deploy disparado; dev `fad3a56`, ahead=0)* **Fix: PDF com altura de linha DINÂMICA (reporte do Vitor/FAMBRAS, 27/jul).** Nome longo de produto no PDF de embarque quebrava em várias linhas mas a linha da tabela era fixa (28pt) — texto atravessava as bordas e atropelava a linha seguinte (print do Vitor, "FROZEN BONELESS BEEF…"). Altura agora acompanha o conteúdo (medida com a mesma fonte do render, piso na altura antiga) nas **7 tabelas com texto livre**: produtos do embarque · MP e ingredientes da produção · tabela genérica dos especializados · ocorrência de aves · equipe (pdf-helpers). **Bônus achado no repro:** as notas de rodapé (embarque/produção/coleta) mediam a altura do PLACEHOLDER (`'Nota 1. ...'`) em vez do texto real → a linha EN atropelava a PT; corrigido. Verificado com PDF de reprodução (nomes do print do Vitor: linhas crescem, bordas íntegras, TOTAL ok, notas limpas); jest 32/32; tabelas de insensibilização (rótulo fixo/valor curto) ficaram fora de propósito. 🔧 **[Renato/Vitor] Validar pós-deploy:** reimprimir o PDF do embarque real do Vitor (item 1.5 do roteiro).

**SIH · Conformidade FM (hands-on 17/jul + FM oficiais preenchidos recebidos em 20/jul).** Spec: `SPEC-CONFORMIDADE-FM-SIH-2026-07-20` (§8). Cruzamento campo a campo dos FM 7.1.4.1, 7.1.4.2, 7.1.7.1 e 7.1.7.4 contra o código.
- ✅ *(21/jul)* 🟥 **P0 — C/NC de AVES saía no item ERRADO no PDF assinado.** As listas de verificação viviam duplicadas (12 itens no front × 13 no PDF) e o valor era casado por **índice posicional**; em aves elas divergiam, então o documento atestava item diferente do marcado. `SLAUGHTER_FM` virou fonte única servida por `/fm-metadata/slaughter/*` (padrão que produção e embarque já usavam — o abate era a exceção), e o PDF passou a imprimir o **rótulo gravado no relatório**, para que um documento assinado mostre o que foi atestado mesmo se o FM mudar. Back `9c3ec7f` · front `16afce7`. **Atingiu 2 relatórios em prod — os dois preenchidos ao vivo na reunião de 17/jul; nenhum de operação real.**
- ✅ *(21/jul)* **P1:** observações passam a ser impressas (**6 templates** — o bloco só desenhava o rótulo) · **linha TOTAL** na tabela de produtos do embarque (tela + PDF) · **documento sanitário obrigatório** para assinar embarque (CSI/CSN/DCPOA — o FM manda em caixa alta e nada checava) · **tempo de retorno em `m:ss`** (com `type=number` o `1:04` da reunião virou `104`) · "Natureza dos volumes"/"Nº de volumes" com datalist (1.450 caixas foram lançadas como kg) · `halalCertData`/`halalCertSource` **persistidos** (o front enviava, o Zod descartava e o dado se perdia). Back `f77bb97` · front `b57a5b0`.
- ✅ *(21/jul)* **Bloco A — decisões do Renato aplicadas.** "Aprovados" virou **derivado** (`total − rejeitados`, read-only; das 5 validações de coerência restaram 2) · os **2 relatórios de aves de teste foram removidos de prod** · "Nº do Pedido" saiu da tela (no FM o campo é "pedido / NF", não dado próprio; coluna preservada e o PDF compõe) · **turno exibido como no FM** ("1º/2º/3º Turno" — trocamos o **rótulo**, não o valor, porque `Shift` também alimenta escala e analytics; 3 listas locais eliminadas) · **nº de série Halal GERADO** no formato `SIF/ANO/000000`, só quando o supervisor não informa, partindo do **maior emitido** e não de `count()` · **FM 7.1.7.11 criado** (enum + migration idempotente + metadata + labels + perfil). Back `50bd589`+`878678d` · front `d1feabc`+`beba85e`. Corrigida de passagem uma duplicação no PDF de exportação (invoice impresso 2×).
- ✅ *(21/jul)* **FM 7.1.7.11 conferido contra o documento oficial** (o Renato localizou um preenchido: BRF Dourados→Concórdia, 26/05/2026). Ele **desmentiu 3 inferências** que eu fizera a partir do 7.1.7.3: data da revisão (**06/07/2020**), título (**"EXCLUSIVO ENTRE BRF"**) e as verificações C/NC, que são **próprias**. Tabela bem mais enxuta e **sem campo de nº de série**.
- 🚩 **Achado para a FAMBRAS:** no FM 7.1.7.11 a coluna se chama *"Quantidade por produto (**kg**)"* mas a planta preenche **caixas** (479/201/770 → TOTAL 1450, peso total 37.800,000 à parte). **O rótulo do formulário está errado, não o supervisor** — foi o que o André identificou ao vivo na reunião.
- 🔧 **[Renato] Validar:** PDF de abate de aves com os 13 itens na ordem do FM · observações saindo no PDF · linha TOTAL · tentativa de assinar embarque **sem** anexo sanitário deve bloquear · tempo de retorno aceitando `0:40` · **"Aprovados" calculando sozinho** · **turno saindo como "1º Turno"** · **nº de série gerado** (`4567/2026/000001`) · **FM 7.1.7.11 na lista de tipos** · rodar o `VALIDATE.sql` da migration `20260721120000_add_transferencia_brf_7_1_7_11`.
- ❓ **[FAMBRAS] 3 pontos abertos do Bloco A:** a **nota fiscal** também bloqueia a assinatura (hoje só o documento sanitário)? · a sequência do nº de série deve **contar o 7.1.7.11**, que não tem o campo? · falta um **FM 7.1.7.3 preenchido** — único dos cinco sem documento oficial em mãos.
- ✅ *(21/jul, EM PROD)* **Bloco B — fecha a Onda 2 do embarque.** Endereço de carregamento **sincroniza** ao trocar a planta (antes só preenchia se vazio — era o "Passo Fundo" da reunião) · `products` deixa de ser `z.any()` e **assinar com produto sem nome passa a ser bloqueado** · campo **"Detalhe do produto"** por item (metade do pedido do André: o nome ainda é livre porque o GC não expõe escopo) · datas como faixa **já existiam**. Back `b071b75` · front `9fc4aac`.
- 🚧 **Bloco C — COMMITADO, NÃO PUSHADO** (back `93a3ae5` + `46e7c42` · front `c36ce0d`). ⚠️ **Tem migration** `20260721160000_system_user_document` + **duas travas novas** de assinatura. Conferido no banco antes: produto-sem-nome não afeta rascunho nenhum; C/NC-incompleta bloqueia **1 rascunho**, o da planta Ecotrace Teste (desativada).
  - **C1** `SystemUser.document` (RG/CPF/passaporte): o FM exige o número na declaração assinada (*"portador do documento V444820C"*) e o SIH não tinha onde guardar. O PDF do abate reusava `slaughtererDoc` (do **degolador**, sem input desde que virou item de equipe) e o do embarque lia `report.supervisorDoc` — **campo que não existe no modelo**. Ambos saíam sempre em branco.
  - **C2** "Nome do Supervisor" no cabeçalho passa a ser **quem assinou**, não quem criou.
  - **C3** marcar NC exibe o gancho normativo do FM (*"preencher o check list FM 7.1.6.1"*) com link direto — o sistema não dizia isso em lugar nenhum.
  - **C4** assinar exige **verificação C/NC completa** (abate, produção, embarque). Dava para assinar com todos os itens `null` e o PDF saía com a coluna vazia — insustentável na ISO 17065.
  - ✅ *(21/jul)* **Bloco C fechado** — back `94fbe96` · front `31f1a8a`: horário também na insensibilização **bovina** (a nota do FM pede duas verificações "em horários alternados") · **`Registro SIH: <serial>` no cabeçalho de TODOS os PDFs** (aparecia só na tela; o impresso não podia ser reconciliado com o registro) · **`rejectedSequence` impresso também em aves** (a tela sempre ofereceu, só o bovino imprimia — o dado sumia do documento). 🧩 Resta só a coerência `formNumber`×espécie, baixa.
  - ℹ️ `46e7c42` é **commit à parte**: versiona 5 scripts de junho que estavam soltos no working tree (cargas JBS Couros/Acquion/BFB já executadas + 2 `VALIDATE.sql`). Descartável sem afetar o Bloco C.
- 🚧 **Importação da planilha `FRIGORÍFICOS DIVERSOS FUNCIONÁRIOS ALI CHAHINE.xlsx`** (`C:\SIH\`, fora de git).
  - ✅ *(22/jul, EM PROD) **Fase 1 — plantas**:* das 94 com SIF, **86 importadas** (74 ativas + 12 inativas por marcador "sem produção/fechou/parado"); 7 ficaram **fora** (marcador "verificar/campanha" — dado incerto, curar com o Ali: BGC, Boidourado, Guidara, Golden Imex, Goiás Rendering, J.A, Frisa/Rio Doce); BRF Dourados já existia. Nome = razão social do GC + cidade/UF; CNPJ do GC. Premissa aplicada: **type=frigorifico, division=IN, species={bovino}**. **Carteira IN: 4 → 90 plantas (77 ativas)** — resolve o dropdown vazio de In Natura da reunião. Script versionado: `PLANNING/sql/2026-07-22-import-plantas-ali-chahine-fase1.sql`. Sanidade OK: sem SIF duplicado, espelho GC casa (Astra/Beauvallet/Agra trazem certs ativos).
    - 🔧 **[Renato] Ajustes pontuais da premissa:** **VPJ Pirassununga Entreposto** (SIF 5562) devia ser `type=entreposto`, não frigorifico (entrou inativa). **VPJ Pirassununga** (SIF 3537, inativa) ficou **sem CNPJ** — não tinha empresa no GC. Ver se alguma das 86 é **aves** (premissa foi bovino p/ todas).
  - ✅ *(22/jul, EM PROD) **Fase 2a — degoladores**:* **57 degoladores** → `Collaborator(type=degolador)` + **57 vínculos** por SIF (todos casam com as plantas da Fase 1; nenhum ficou sem planta). Verificado: **0 já existiam**. Documento não vem na planilha (NULL); telefone quando válido. Idempotente. Script: `PLANNING/sql/2026-07-22-import-degoladores-ali-chahine-fase2a.sql`.
  - ✅ *(22/jul, EM PROD) **Fase 2b — supervisores**:* **46 criados** como `SystemUser` (role=supervisor, division=IN, `assignedPlants` por SIF; **0 já existiam**, **0 ficaram sem planta**). **Senha temporária DERIVADA do SIF: `{SIF}@Fambras2026`** (decisão do Renato). Login por e-mail da planilha. bcrypt rounds=10. Idempotente por e-mail. *(As senhas foram primeiro gravadas com typo "@Frambras2026" e re-hasheadas para "@Fambras2026" em 22/jul — validado por `bcrypt.compare`.)* Script: `scratchpad/exec-supervisores.js` (**não versionado — não commitar hashes/senhas**). O 46º (YAHA MADOULA / FRIGOL 4150) tinha e-mail sem `@` na planilha; corrigido para `yahyamadoula123@gmail.com` (confirmado pelo Renato) e criado.
    - ⚠️ **Risco de segurança (aceito como temporário):** o SIH **não tem "forçar troca no 1º login"** — a senha padrão vale até troca manual (tela "esqueci a senha"). E, sendo derivada do SIF (público) + sufixo fixo, **qualquer um que saiba o padrão entra em qualquer conta**. Orientar troca no 1º acesso; se virar permanente, implementar `mustChangePassword`.
  - ✅ **Bloco de importação Ali Chahine CONCLUÍDO** (86 plantas + 57 degoladores + 46 supervisores, todos em prod). Resta só a curadoria das **7 plantas "verificar"** com o Ali e os ajustes pontuais da premissa (VPJ Entreposto → type, VPJ Pirassununga sem CNPJ).
  - ⚠️ **Toca o GC:** a planilha tem **nome real + município** das plantas — insumo para a normalização de nomes do GC (lá estão como "BRF S.A." / "JBS S/A"). E revelou **ruído no endereço do GC** (ex.: Barra Mansa marcada "Sertãozinho/**PB**", sendo SP).
  - ⚠️ **Toca o GC:** a planilha traz **nome real + município** para 94 plantas que no GC estão como "BRF S.A." / "JBS S/A" — insumo pronto para a normalização de nomes, sem depender de nova entrega da FAMBRAS.
- ❓ **[Renato] Os 2 relatórios de aves assinados** têm 12 itens gravados sem rótulo; com o fallback, seguem imprimindo **como antes** (deslocado). Não corrigi retroativamente de propósito — mexer no que um documento assinado mostra é pior que o defeito. Sendo teste, a saída limpa é **descartá-los**.
- ✅ *(23/jul — lista auditada por git; era o retrato de 20/jul)* **Abertos da spec (P2/P3): TODOS fechados pelos Blocos A/B/C + pacote de 23/jul, EXCETO 1.** De-para: documento do supervisor assinante = **C1** · nome = quem assinou = **C2** · vincular NC ao FM 7.1.6.1 = **C3** + link pré-preenchido `6bc283a` · validar `verificationItems` antes de assinar = **C4** · horário no bloco bovino + `serialNumber` no PDF = `94fbe96` · turno com rótulo = Bloco A `d1feabc` · datas como faixa = já existiam (Bloco B) · autofill do endereço de origem = Bloco B `9fc4aac` (`loadingAddress` é o MESMO campo na transferência "Endereço de Origem" e na exportação "Endereço de Carregamento"; sobrescreve ao trocar planta). **Resta só:** produto preso ao escopo (**exige rota nova no GC** — bloqueado; conversa com W7 da Trilha A).
- 🚩 **Número de série do relatório Halal — regra normativa não implementada.** A Nota 1 impressa no FM define `SIF/ANO/SEQUENCIAL`, **sequência única por planta** cobrindo embarque + venda + transferência. Hoje é `halalSerialNumber`, texto livre opcional, sem geração nem unicidade — e os 3 documentos reais usam 3 formatos diferentes (`18/2022/0000200` · `4466/2026/000122` · `SIF 4333/2026/00088`). ❓ **FAMBRAS:** formato canônico + o sistema passa a gerar? Se gerar, vira chave natural e âncora melhor que `container + data` para o vínculo com o SysHalal.
- 🚩 **Correção de relatório (Nota 2 do FM):** cancelar = carimbar **"CANCELADO"** e emitir o novo com sufixo **`A`**, *mantendo a mesma série*. Não existe nada disso no SIH. Conversa com `draft→aprovar→travar` e com a auditoria por linha da ISO 17065.
- ⚠️ **`HalalCertField` na exportação NÃO foi exposto, de propósito:** ele usa `halalSerialNumber` como nº do certificado, mas esse campo é o **nº de série do relatório** — dois conceitos na mesma coluna. Separar exige migration e depende da decisão acima. `destinationCnpj` (hoje só `useState`) idem.

**SIH · Usabilidade (Google Doc "SIH - usabilidade", Nilsa, 14/jul — lido via Drive 21/jul).** Fonte: `docs.google.com/document/d/1YCGcECIZZNvNb-APufGSgU9QZPxfdixJpetC8nBRlf4` (n.almeida@ecotrace.info). 9 prints anexos. **Metade dos pontos já caiu** entre 20 e 21/jul (o doc é anterior aos Blocos P0/P1/A/B/C) — cruzamento feito por git em 21/jul:
- ✅ *(20/jul, `9a6e282`)* **Decimal nos tempos de insensibilização** — faltava `step="any"` em 6 campos; sem `step` o HTML assumia `step=1` e rejeitava `0,08 s`. Era exatamente a queixa *"não está aceitando número decimal"*.
- ✅ *(20/jul, `9a6e282`)* **Campo Horário da insensibilização** (`type=time` por verificação) — não existia no print dela; havia só o rótulo derivado do índice.
- ✅ *(21/jul, `b57a5b0`)* **Tempo de retorno em `m:ss`** (o print mostrava `(s) / Ex: 60`, build velho).
- ✅ *(21/jul, `b57a5b0`)* **Totalizador de produtos** — linha TOTAL em `ProductTable.tsx` (componente compartilhado → exportação, transferência e venda).
- ✅ *(21/jul, P1 `f77bb97`)* **Anexo DCPOA/CSN obrigatório** na transferência (já vinha do Gabriel/FAMBRAS 23/jun).
- ✅ *(22/jul, `39b36fc`)* **A · Acentuação de labels** — 18 arquivos, só strings de exibição (nenhum identificador/rota/enum/query param). Cobre os apontados + irmãos do mesmo defeito achados na varredura: `Sidebar.tsx` (13 itens + 2 grupos), `DateRangeFilter.tsx` (Últimos/Mês/Período), `fm-metadata.service.ts` (5 labels FM), `ProductionReportForm.tsx` (Tipo de Produção), `ControladoriaDashboard.tsx`, `GcRawMaterialsList.tsx`, `MeatInventoryList.tsx`, `CouroFields.tsx`, Tripas/Fracionamento/Heparina (Dt. Fabricação, Lote-mãe), `StatusHistory.tsx`, e o toast "Não foi possível salvar" em 5 telas. `tsc -b` verde.
- ✅ *(22/jul, `39b36fc`)* **B · Menu lateral perde a seleção ao entrar em `/new` ou `/:id`.** `isLinkActive` passou a casar as rotas-filhas por **prefixo** mantendo a checagem dos query params. ⚠️ **Efeito colateral conhecido:** em `/production-reports/new?productionType=couro` acendem **2 itens** (Couro + atalho "Venda/Prod. Couro 7.1.4.5", ambos couro) — cosmético, só no couro; não adicionei "match mais longo vence".
- ✅ *(22/jul, `39b36fc`)* **C · Largura dos formulários** — relatórios padronizados em `max-w-5xl` (Abate/Ocorrência saíam de 3xl, Coleta de 4xl; Produção/Embarque já em 5xl).
- ✅ *(22/jul, `39b36fc`)* **F · Copy do texto de ajuda da MP** em `LinkedSlaughtersField.tsx` — reescrito conforme a Nilsa.
- ❓ **[Nilsa] D1 · validação obrigatória do horário?** O campo Horário existe (`9a6e282`) mas segue **fora** da trava de obrigatoriedade (exige amperagem/voltagem/frequência/cuba/velocidade, **não** `checkTime`). Se a anotação *"validação"* era "obrigar o preenchimento", segue aberto; se era "não tinha onde botar a hora", está fechado.
- ❓ **[Nilsa] G · "Relatório de acompanhamento de embarque"** — título sem texto no doc; conteúdo (se houver) está só no print. Perguntar o que era.
- 🔧 **[Renato] Pedir à Nilsa retestar com o build atual** antes de reabrir — 5 dos pontos já caíram.

**SIH · Fluxo de homologação de MP + travas (reunião FAMBRAS 30/jun):** *(trazido ao §4 em 17/jul pela regra §0.5 — vivia só na ATA/fluxograma de 30/jun, commitados em `1db4442`; nenhuma linha estava no mestre)*. Desenho: `FLUXOGRAMA-RELATORIO-FABRICACAO-MP-2026-06-30` + `ATA-ALINHAMENTO-FAMBRAS-2026-06-30` (§8). **Nada codado — zero hash.**
- 🧩 **Trava na ASSINATURA, não no registro** — supervisor só enxerga MP do escopo (read-only do GC). MP fora do escopo → **alerta no preenchimento** → salva rascunho → **não assina** (pendente de validação). É o detalhamento do "acoplar MP aprovada" acima. ❓ depende de PO.
- 🧩 **3 casos quando falta MP:** (1) já no escopo → segue · (2) **já homologada na base/catálogo global** → analista só **inclui no escopo** (reuso, **sem** homologação nova) · (3) MP nova → homologação completa (cliente alimenta a planilha + analista avalia). ⚠️ O **supervisor não cadastra MP** — sinaliza e orienta o cliente (coerente com §5.16, mas ver o gap de sync no §6).
- 🧩 **Lista viva** — após homologar, a lista suspensa precisa refletir **na hora**; senão trava o supervisor em campo (Elaine: *"a atualização tem que ser simultânea"*).
- 🧩 **Tela de pendências** (controladoria/analistas) — relatórios travados por MP não homologada.
- 🧩 **Liberação sempre REGISTRADA** (quem autorizou) — auditável. Critério de quem destrava = ❓ FAMBRAS (§4.3).
- 🧩 **Roteamento industrializado × In Natura** — cert/relatório de **industrializado** foi classificado na divisão **In Natura** e "ninguém acha" (Lina, 30/jun). Garantir roteamento correto por divisão + relatório localizável independentemente da divisão.
- 🧩 **Dashboard de Ocorrências (qualidade)** — decisão da reunião: ocorrências ficam **centralizadas no SIH**, espelhando o Power BI do time da Elaine. 📦 bloqueado pelo Power BI atualizado (§4.3).
- 🧩 **Perfil "Qualidade" no SIH** — não existe; criar (acesso Elaine/time).
- 🔧 **E-mails de acesso não chegaram** (Elaine e Soha, 30/jun) — investigar envio; encosta no §3.5.
- 🚩 **Ciclo embarque ⇄ nº do cert SysHalal — CONFLITA com o §5.21. NÃO CODAR.** A reunião propôs **travar o Relatório de Embarque** (só avança com o nº do cert) e **travar a emissão no SysHalal** até o embarque ser aprovado. O §5.21 trava o oposto: **manter o embarque como está** e pôr a rastreabilidade no **Dossiê paralelo**. ⇒ ❓ **Decisão [Renato/FAMBRAS] antes de qualquer código:** a trava entra no Dossiê (paralelo) ou re-litiga o §5.21?

**SIH↔SysHalal · integração sem escopo de grupo (código PRONTO, rollout pendente — ver §4.1):**
- ✅ external-api rotas `/integration/{certified,certified_status,certified_pdf}` c/ x-api-key: `25c96a6` (develop `89c6e7e` · staging `6d2c2e7` · **release NÃO — não está em prod**).
- ✅ sih-backend modo dual (`SYSHALAL_INTEGRATION_API_KEY` → /integration; senão legado): `439d4ea` (release, pushado).
- 🧩 [Claude] testes staging quando SSM configurado: certs de **2 grupos** + regressão `/certified` legado (parceiro continua escopado).
- Domínio de arquivos (fora da Trilha D declarada — anti-colisão): `syshalal-external-api/*` · `sih-backend/src/halal-cert/` · `sih-frontend/src/{components/shared/HalalCertField.tsx, services/halal-cert.service.ts}`.

**SIH · QA Nilsa (mai/2026) — ✅ AUDITADA POR CÓDIGO em 23/jul (era o bloco mais stale; metade já tinha caído):**
- ✅ **JÁ RESOLVIDOS, sem ação:** M2.6 e-mail duplicado (409 + "Já existe um usuário com este e-mail", banner claro no `UserForm`) · M3.8 abate sem obrigatórios (validação client-side com mensagem por campo + toast) · M4.13 produção sem planta · M5.10 embarque sem planta (e assinar só existe pós-rascunho) · M7.4 NC (planta+severidade hidratam com fallback `?? nc.plant`; relatório-pai opcional conforme §5.20).
- 🧩 **M11.7 — página `/analytics/supervisors` EXISTE mas está ÓRFÃ:** rota e tela funcionais (`SupervisorAnalytics.tsx`), porém nenhum card/menu navega até ela (o `AnalyticsOverview` tem 6 KPICards, nenhum "Supervisores"; Sidebar só tem `/analytics`). Fix barato: item na Sidebar ou 7º KPICard clicável.
- 🧩 **M11.8 — export de gráficos: nunca construído** (nenhum botão de export/print em `src/pages/analytics/`). Médio-alto; ❓ vale priorizar pré-go-live?
- 🧩 **Tooltip de KPI — aberto:** `KPICard.tsx` e cards do `Dashboard` sem Tooltip explicando o número (o único Tooltip do app é o da Sidebar colapsada). Baixo-médio: prop `tooltip?` no `KPICard` com o `ui/tooltip` já existente.
- 🧩 **Sweep Selects/Inputs `disabled` em view-only — PARCIAL:** só **Planta** convertida p/ texto plano (nas 4 telas). Restam **9 Selects** como display (Turno/Espécie no abate; Tipo de Embarque/Destino/Transporte/Doc sanitário no embarque; Severidade/Turno/Eficácia na NC) + dezenas de Inputs (~218 `disabled={readOnly}` em 22 arquivos — relatório assinado é tela view-only). Fix: componente `DisplayValue` reutilizável (mesma casca da Planta) e conversão mecânica.
- ❓ **Edge de decisão (M2.6):** e-mail de usuário **soft-deleted** também dá 409 com a mesma mensagem genérica — recriar é impossível e não há reativação pela mensagem. Decidir: mensagem específica ("existe usuário inativo…") × fluxo de reativar.

**SysHalal · tela de validação:** 🧩 **erros de hidratação React #418/#422** — *(trazido para o §4 em 17/jul pela regra §0.5; estava só no handoff de 23/jun e escapou da extração)*. **Adiado pelo Renato** na época, nunca fechado. Ocorre na tela `certificadovalidate/[id]` do `syshalal-web`. Suspeita registrada: **skew de cache pós-deploy** → testar **hard refresh** primeiro; se persistir, reproduzir em `next dev` para ter a mensagem não-minificada (suspeita secundária: i18next em SSR). **Não é regressão** dos fixes de 23/jun (`#516`/`#517`) — a UI nova só renderiza pós-fetch. ⚠️ Toca a **Trilha E** (SysHalal), hoje travada pelo WIP solto (§1/§3.2) — **não iniciar antes de resolver o WIP**.

### 4.3 FAMBRAS — decisões e entregas
**❓ Decisões de norma (as 3 mais quentes — Soha):**
1. ✅ *(DECIDIDO Soha 27/jul + EM PROD)* **Nacionais (BPJPH/MUIS/MS) derivam GSO — SIM.** `deriveStandardFromStandards` (back) + `deriveStandard` (front) passam a classificá-los como **família GSO** (antes VOLUNTARY) → "Sem norma acreditada" some p/ eles. **Selo inalterado** (vem do template/marketVariant; nacional segue SEM selo, §5.2). back `ea1dcf19` · front `4ba1a9a3`. (§4.3-3 confirmado: GSO e OIC/SMIIC **nunca** saem no mesmo cert.) ✅ **VALIDADO ao vivo por Fuad no follow-up 27/jul** (Indonésia+Malásia+Singapura corretas + selo vazio; "todas as normas corretas") — detalhe no bloco "FOLLOW-UP SEMANAL FAMBRAS 27/jul" do §4.2.
2. ✅ *(22/jul)* ~~Contradição UAE.S → "sem norma acreditada" × imprime ENAS~~ — **RESOLVIDO (Soha):** UAE.S **tem norma acreditada própria** (imprime ENAS); corrigir a classificação. Vira decisão **§5.22**.
3. **GSO+OIC juntos → template GCC → só o selo GAC** (o OIC não sai). Coerente com "SMIIC só Turquia" — confirmar.

**✅ Respondido pela Soha (WhatsApp 22/jul) — vira decisão §5.22:**
- **A auditoria governa a versão da norma** (update prospectivo, a partir da próxima auditoria; não retroage).
- **Mercado no cert ⊆ mercado auditado por auditor ELEGÍVEL** (Indonésia = grupo de auditores dedicado).
- **2055-2 = norma do organismo (selo GAC) · 2055-1(+993) = norma da planta (linha)** — anos divergentes são normais.
- **Ingrediente restrito filtra mercado** (conchonilha: Ásia Amarela aceita, GSO/UAE não) — modelamos **tabela geral** (decisão Renato).

**✅ *(resolvido — Elaine, reunião anterior)* Selo por grupo-de-norma:** cert de **Indonésia (e nacionais) NÃO leva selo** — nem o nacional, nem GAC/ENAS (que são acreditação do **Golfo**). **Confirma o §5.2** (nacionais nunca) e resolve W12 (GAC/ENAS fora da Indonésia = correto) + W13 (sem selo Indonésia = correto). Modelo: **selo por grupo** — GSO/Golfo→GAC(+ENAS p/ UAE) · OIC/SMIIC→HAK · **nacionais→conjunto vazio**. W16 (Saudi sem GAC) segue bug: Saudi=GSO **deve** levar GAC.

**✅ Retorno analistas 30/jul — 6 decisões RESPONDIDAS (Renato, 30/jul; destravam os lotes do §4.2 "RETORNO DOS ANALISTAS FAMBRAS 30/jul"):**
1. **Nomes de produto na importação: corrigir SÓ minúsculas.** Title Case aplica-se apenas quando o texto vier **todo** em minúsculas (`ad mix frango marinado`→`Ad Mix Frango Marinado`); qualquer texto com maiúscula é **preservado** (`INNOVA OL100` intacto). **Código sempre preservado.** → Lote 1.5.
2. **Descrição de embalagem: em INGLÊS.** A planilha de escopo passa a ser preenchida em inglês (o certificado é EN/AR). Processo FAMBRAS — sem código. *(Efeito colateral positivo: com a regra 1, textos com maiúscula não sofrem Title Case → some o `20 kg`→`20 Kg`.)*
3. **Vigência em renovação: MANUAL por enquanto.** Não automatizar a regra dos 3 anos a partir do ciclo inicial. O ajuste do Lote 4.1 (sugestão acompanhar a data de emissão) **segue valendo** — é sugestão, não trava.
4. **Emissão sem norma externa: só selo FAMBRAS.** Mercado interno sai com **apenas a DT**, nenhum selo além do FAMBRAS. Coerente com §5.2 (nacionais nunca levam selo) e com a coluna `dtOnly` do catálogo. → Lote 4.3.
5. **Segundo assinante: "Analista Responsável" — SÓ NA TELA.** O **certificado NÃO muda** (segue o signatário canônico §5.4, assinatura única do Representante Autorizado). → Lote 6.2.
6. **Registro de reclamação: restrito à QUALIDADE.** Remover `empresa`, `auditor`, `analista`, `gestor` e `admin` do `POST /complaints`; a Qualidade registra e encaminha ao analista. → Lote 6.3.

**❓ Outras decisões:** #3 base por categoria (Minerva 1430×1431) · #9 múltiplos mercados · draft→aprovação existe? · N2b de-para de **14 categorias** · 3 SIFs duplicados (585 FRIGOMARCA×PANTANAL · 4699 LAR×AGROARACA · 2620 FALCAO×BMG) · dedup Hexus×Vidara · REVIEW histórico (7 casos) · overlap couro 7.1.4.5×7.1.4.9 · 5 decisões Fase 5B FAM-0017 (Lina) · certs vencidos-mas-ativos (ex. Gelita).

**❓ Matriz de criticidade de travas — quem destrava o relatório (reunião 30/jun):** *(trazido ao §4 em 17/jul pela §0.5)*. Pré-requisito do fluxo de homologação de MP (§4.2/SIH). Desenho proposto na reunião, **falta a FAMBRAS fechar**:
- **Documento vencido** (ex.: nitrito de sódio) → **controlador libera** (Lina: *"já confiam que ele faça"*).
- **MP fora do escopo** → **trava** até incluir/homologar no GC; caso **emergencial** sobe a **gestor**.
- ✅ *(Renato 27/jul)* **MP crítica — o flag JÁ EXISTE:** enum `Criticality` (baixa/media/alta), **por produto**, em `critical_raw_materials` (decisão Lina 09/mai; `RawMaterialMaster` aponta pra régua). A trava vai chavear nele. ✅ *(27/jul)* **escala (4 níveis) + gate de trava DECIDIDOS** — ver sub-itens abaixo.
  - ✅ *(27/jul, follow-up)* **Planilha GERAL de criticidade ENTREGUE e ANALISADA** — `reunioes_fambras/2707_1100/Complexidade - anlises.xlsx` (geral por produto, não por planta). **2 abas:**
    - **`COMPLEXIDADE`** — **89 produtos** com `PRODUTO | CATEGORIA (código GFSI) | COMPLEXIDADE`. Escala FAMBRAS de **4 NÍVEIS: `MUITO ALTO` (14) · `ALTO` (22) · `MÉDIO` (26) · `BAIXO` (27)**. Categorias predominantes: **C** (41, perecível animal) e **K** (43, (bio)químicos).
    - ✅ *(EM PROD 27/jul, back `1216853f`)* **Escala 4 NÍVEIS + régua ingerida.** enum `Criticality` += `muito_alta` (migration idempotente `20260727180000` + aplicado em prod); **89 produtos** da planilha ingeridos em `critical_raw_materials` (SQL versionado `prisma/2026-07-27-ingest-criticidade-mp-89.sql`, upsert por category+name; **0→89**, níveis baixa 27/media 26/alta 22/muito_alta 14; grupos multi-produto mantidos fiéis à fonte). **Sem mapear p/ 3** — granularidade preservada (régua ISO 17065).
    - 🚧🔴 **TRAVA na assinatura (SIH) — BLOQUEADA; premissa do link DESMENTIDA por preview.** 1ª passada de match MP↔régua (27/jul, só leitura): dos **335** `RawMaterialMaster`, só **11 casam por nome exato** (níveis 2/3/2/4), **1** candidato por nome-base, **323 sem match**. ⇒ **a régua NÃO é uma lista de MP** — é uma **classificação de criticidade por TIPO/categoria de produto** (FAMBRAS); a MP concreta tem nome comercial ("Abacaxi Liofilizado em Pó", "First Stage Batter") que *cai sob* um tipo, mas não tem o mesmo nome. **Linkar por nome não resolve** e classificar cada MP num tipo é **julgamento — não inferir.**
    - ✅ *(DECIDIDO Renato 27/jul = Opção A; back `f472b8c2` / front `beb0674c`, LOCAL)* **A MP concreta ganha criticidade por ATRIBUIÇÃO do analista na homologação**, com a régua como guia. Implementado no GC: `RawMaterialMaster.criticality` (`Criticality?`, migration `20260727183000` idempotente) + DTO + **select de 4 níveis** na tela de homologação (trocou a "Dica de criticidade" texto-livre; helper aponta a régua). tsc 0 back+front. *(Opção B — lookup por categoria GFSI — descartada: categoria tem vários níveis.)*
    - ✅ *(passo 2, back `3b763048`, LOCAL)* **GC expõe `criticality` no `/integration raw-materials/by-plant`** (via `CompanyRawMaterial→RawMaterialMaster.criticality`; null enquanto não casada/sem nível). Aditivo em rota existente → sem regen API GW. tsc 0.
    - 🧩 **Falta p/ o gate (Parte 3) — desenho SIH mapeado 27/jul:** ⚠️ **nó estrutural:** os ingredientes do relatório de produção são **JSON livre** (`approvedIngredients: z.any()`, só tabela no PDF) — **não** referenciam a MP homologada do GC, então o `sign()` não tem por onde resolver criticidade por ingrediente (a `rawMaterials` estruturada é a MP **cárnea/animal**, não os ingredientes industriais C/K da régua). `GcRawMaterialsList` do SIH é listagem read-only avulsa, não o seletor do relatório. ⇒ **3 fatias:**
      - ✅ **P3.1 (SIH front `9e1faff`, EM PROD):** captura de ingredientes ganhou **seletor da MP homologada do GC** (by-plant, traz `criticality`), gravando ref estruturada {gcRef, criticality, homologated} no `approvedIngredients` (snapshot p/ o gate). Aditivo — texto livre segue p/ MP fora do escopo. tsc -b 0.
      - ❓✅ **P3.2 (a codar) — regras DECIDIDAS (Renato 27/jul):** gate no `sign()` dispara quando a MP **está fora do escopo OU tem doc halal vencido**; aí a criticidade grada: `muito_alta`+`alta`→**trava** · `media`→**sobe a gestor** · `baixa`→**livre**. MP **homologada + doc válido = assina livre**. **Fora do escopo** (sem criticidade conhecida) → **sobe a gestor sempre**.
      - 🧩 **P3.3 (a codar):** perfil/fluxo de **liberação pelo gestor no SIH** (registrada) — decisão: **opção (a)**, papel de liberação no próprio SIH (não existe hoje).
      - 🚨 **RISCO DE ROLLOUT (achado ao codar — PARE antes do gate):** ingredientes **legados/manuais** têm `homologated=undefined`. Se o gate tratar `undefined`=fora-do-escopo→gestor, **TODO relatório com ingrediente de texto livre trava até o gestor liberar** — e o fluxo de liberação (P3.3) **ainda não existe** → **assinatura de produção quebra em prod no dia 1**. ⇒ **ordem obrigatória: P3.3 (liberação) ANTES de ligar o gate P3.2 de fora-do-escopo**; e o `undefined` (legado) precisa de regra explícita (grandfathering: só ingredientes do picker entram no gate, OU flip só após adoção do picker). **Não codar o bloqueio sem isso resolvido.**
      - **(1)** [analista] popular `criticality` nas MPs (tela GC, no ar). **Deploy GC no ar** (back `3b763048`, front `beb0674c`); **SIH P3.1 no ar** (`9e1faff`; dev reconciliado `efb3a7b`).
    - **`ANÁLISES`** — Tabelas 3/4: **frequência de análise por risco** (solventes residuais / DNA animal), classificada **Caso I→VI** (I = por lote/mensal … V = só 1º ciclo … VI = isento). É a **cadência de auditoria por risco**, conceito **relacionado mas distinto** da complexidade — não confundir com o gate de trava de MP.
    - ✅ *(DECIDIDO Renato 27/jul)* **Gate de trava por nível:** `muito_alta` + `alta` → **travam direto** (não assina sem homologação) · `media` → **aviso + sobe a gestor** (André frigorífico / Fuad industrial, §4.3) · `baixa` → **livre**. Toda liberação fica **registrada** (quem autorizou). Escala (4 níveis) e gate ambos **fechados**.
    - 🔧 **[Renato/Lina/Fuad] ainda a confirmar (não bloqueia):** se a cadência `ANÁLISES` (Caso I-VI) entra como 2ª dimensão do risco.
- ✅ *(Renato 27/jul)* **Gestor da liberação emergencial = por departamento: André (frigorífico) / Fuad (industrial).** Documento vencido → **controlador libera sozinho**. Toda liberação fica registrada.
- ✅ *(Renato 27/jul)* **SLA da lista viva após homologar = 48 horas.**
- Invariante já acordado: **toda liberação fica registrada** (quem autorizou) — Lina: *"pelo menos a gente sabe de onde veio"*.

**📋 Correção na FONTE (relatório pronto — `halalsphere-docs/PLANNING/RELATORIO-ESCOPO-CORRECAO-FAMBRAS-2026-07-12.xlsx`, `7f565b7`):** *(novo 17/jul)* **3 produtos** cujo nome é **código DSM na própria planilha FM** (`8001 D/P`, `8008 C/U`) · **30 marcas** que são produto/embalagem/lista no lugar da marca. O parser refinado recuperou **312 dos 315** casos automaticamente (315→3); o resíduo é dado ausente/trocado no FM, só a FAMBRAS resolve.

**❓ Curadoria do catálogo de MP (N5 v2):** *(novo 17/jul)* catálogo refeito → **335 masters, 0 nome numérico** (v1 tinha 56 numéricos + blobs de fórmula). **7 famílias ficaram em `pending`** aguardando a FAMBRAS decidir a fusão (ex.: `Ácido Cítrico` × `Ácido Cítrico Anidro` × `Acidulante Ácido Cítrico Pó`) — **não fundi no automático porque em halal a forma/origem importa** (cítrico sintético × microbiano difere). Propostas em `scratchpad/n5v2-merge-propostas.txt`. Também: **7 `company_raw_materials` em `awaiting_matching`** cujo "nome" é **fórmula inteira** ("Açúcar cristal, açúcar mascavo, sal, especiarias…") = receita de produto final, não MP → corrigir na origem.

**📦 Entregas aguardadas:** logos/assinatura em **alta resolução** · lista "certificada desde" · ~~**textos oficiais EN por categoria** (só "K" confirmado)~~ ✅ **RECEBIDO (Fuad, 27/jul — `CATEGORIAS.docx`, 19 códigos GFSI em EN; ver §4.2 bloco follow-up)** — destrava o bug PT→EN do PDF industrializado · arquivo de **escopo real** da Lina (destrava o parser xlsx) · .xlsx do catálogo de produtos (destrava 5A-2) · CSVs FM 7.8.1/7.8.2 · certificado real **preenchido** de referência (A2 layout datas EN/AR) · logo Indonésia (Elaine consulta acreditadora) · **aprovar itens de MP** na tela de review (sem isso `approvedOnly=true` volta vazio) · lançar dados reais no FM 20.1 · lista oficial plantas+CNPJ · ~~criticidade halal~~ ✅ **RECEBIDA (Fuad, 27/jul — planilha geral por produto; ver §4.3 MP crítica)** · códigos reais dos 322 `N5-*` · 16 estrangeiras sem endereço (fonte externa) · Starmilk/Econata (fora do SIGSIF) · **Power BI de qualidade atualizado** (Elaine — destrava o dashboard de Ocorrências do §4.2/SIH; o que está em mãos é a versão de **abril**) *(§0.5, 30/jun)*.

---

## 5. Decisões travadas — NÃO re-litigar

**Certificado / layout**
1. **Layout CONGELADO** — fidelidade ao gabarito acreditado; não inovar. Layout novo só após diretoria → Dr. Mohamed → acreditadores.
2. **Selos:** só **GAC, ENAS e OIC/SMIIC** entram. Nacionais (Indonésia/BPJPH, Malásia/JAKIM, Singapura/MUIS, Qatar, Oman, Kuwait, Saudi, IMANOR, Tailândia) **nunca**. *(Allowlist em `seal-config.ts`, `ee23628f`.)*
3. **PDF imutável** — correção = **novo número**. Número **travado** na edição de escopo.
4. **Signatário canônico:** Mohamed Hussein El Zoghbi / Representante Autorizado (`signatory-config.ts`). Nunca inventar.
5. **Cert sempre ABRE sem senha** (autenticidade via QR). Bloqueia-se **cópia**, nunca abertura. `CERTIFICATE_PDF_UNLOCK_KEY` **não é senha de abertura** — é o *owner password*, só destrava restrições.
6. **Bilíngue EN+AR** em 100% dos certs. **Datas date-only = UTC.**
7. **Idioma extra = certificado adicional** separado (não coluna).
8. **Multi-categoria = certificados separados** via Clonar. **Reconfirmado 16/jul** contra o "#3 base por categoria" da reunião de 14/jul: o fluxo é **manual (Clonar)**, não split automático — o #3 foi **rebaixado** (§4.2). Clonar + guard-rail (`e1f92785`) já cobrem o caso.

**Normas (14/jul)**
9. **`.K.` coexiste** com a numeração IT 4.2 · Cert Único = **1 cert por norma-grupo** · **single também leva `.K.`**.
10. Agrupamento por espécie: **bovinos** `.1.`GSO+UAE.S / `.2.`BPJPH+MUIS+MS / `.3.`OIC-SMIIC — **aves** `.1.`UAE.S / `.2.`GSO / `.3.`BPJPH+MUIS / `.4.`MS / `.5.`OIC-SMIIC.
11. **Catálogo `certification_standards_by_market` está CORRETO — NÃO alterar.** A reclamação vinha do fallback.
12. **Taxonomia de categoria:** SMIIC **só** para OIC/SMIIC (Turquia); todo o resto (GSO, BOTH, VOLUNTARY, nacionais) = **GSO**.

**Identidade**
13. **NUNCA "HalalSphere"** visível. Título/remetente = **"Gestão de Certificações"**; identidade = **"Fambras Halal by Ecohalal"**. Marca EcoHalal = **azul `#118add`**.
14. ✅ **Cor dos e-mails migrada para o azul Ecohalal `#118add`** *(16/jul, `e953c21b`)* — a opção que estava aberta ("segue teal, migração opcional") foi **exercida pelo Renato**. Eram **2 cores**, não 1: verde `#1a5c2e` + subtítulo `#a7d5b5` (4 templates) e teal `#0f766e` (notificações) → tudo `#118add` (subtítulo `#bfe3fa`). Zero cor antiga em `src/email` e `src/notification`. 🔧 **[Renato] Validar** um e-mail real (ex.: Primeiro Acesso) e uma notificação. ⚠️ Esta pendência vinha do handoff 13/jul e **não estava no §4** — gap da extração, fechado aqui (regra §0.5).

**Modelo de dados / arquitetura**
15. `CompanyGroup` = grupo econômico · `Company` = CNPJ da filial · `Plant` = estabelecimento (**1 SIF ↔ 1 CNPJ**, nullable p/ químicos/casing). Unique `(sanitary_code, sanitary_code_type)` está CERTA → **sem migration**. Base DEFINITIVA de cadastro: `C:\HalalSphere\FM78x_atualizados\` (FM 7.8.1 .xlsx + 7.8.2 .xlsb, 25/06) → 652 estabelecimentos (571 BR + 81 estrangeiros).
16. **Cadastro nasce no GC**; SIH **consome** (não cadastra planta). Embarques **permanecem no SysHalal**.
17. **Auditoria por linha obrigatória (ISO 17065)** via `CertificationHistory`; `AuditTrail` genérica dormente. **F4 (RevisionLog) tem prioridade sobre F5/F6.**
18. **Imprimir = `generateAndStore` idempotente** via URL presigned + `window.open` — **nunca XHR→S3** (dá CORS).
19. **Prod do GC tratada como homologação** por ora (FAMBRAS gera sujeira; limpeza depois). Reset final: apagar certs de teste e zerar numeração antes de operar.
20. **M7.4 — NC com relatório-pai: OPCIONAL** (Renato, 22/mai).
21. **Dossiê de exportação** (22/jun): manter o Relatório de Embarque **como está** + construir o Dossiê como **fluxo PARALELO** (rastreabilidade documental completa). O sistema deve **cobrar do supervisor as datas efetivas de abate**.

**Kernel de normas (22/jul — ADR em `halalsphere-docs/ARCHITECTURE/ADR-KERNEL-NORMAS-CERTIFICADO-2026-07-22`, §8)**
22. **Kernel de normas = snapshot-first + tabela plana effective-dated + produtor único.** Regras confirmadas com a Soha (22/jul):
    - **(a)** A **auditoria** governa a versão da norma; update é **prospectivo** (próxima auditoria), não retroage.
    - **(b)** Mercado no cert **⊆ mercado auditado por auditor elegível** para a norma (Indonésia = grupo dedicado). Inativa no modo emissão-manual.
    - **(c)** **2055-2 = norma do OC (selo GAC)** × **2055-1(+993) = norma da planta (linha de norma)** — nunca se cruzam; anos divergentes são normais. UAE.S **tem** norma acreditada (não é "sem norma").
    - **(d)** Elegibilidade de mercado = **base ∩ ingrediente ∩ auditor ∩ habilitação**; **ingrediente restrito × mercado = tabela geral** (conchonilha é a 1ª linha).
    - **(e)** `Certificate` **congela** as normas resolvidas na emissão (satisfaz imutabilidade §5.3); PDF de cert emitido **nunca re-resolve**.
    - **(f)** Kernel **não funde** numeração/audit-days/competência de auditor (bounded contexts próprios); liga a competência **por validação**, não por fusão.
    - **(g)** ✅ **Selo por grupo-de-norma** (Elaine): GSO/Golfo→GAC(+ENAS p/ UAE) · OIC/SMIIC→HAK · **nacionais (Indonésia/BPJPH, MS, MUIS)→SEM selo** (não contradiz §5.2; refina o enforcement — hoje o código põe GAC/ENAS em tudo).

---

## 6. Divergências doc × git encontradas em 16/jul

> Registradas para justificar a regra do §0.1 — e porque podem se repetir.

| Afirmação do doc | Realidade no git |
|---|---|
| Handoff 14/jul: *"FEITO… commitado no pacote por norma"* | **Não estava.** Solto no working tree por 2 dias; commitado em 16/jul (`827f8adf`, `c396f0a5`). |
| Handoff 13/jul (escopo): *"código pronto, NÃO deployado"* | **Está em `release` e pushado** desde 14/jul (`0f3a9b32`, `a44f9e49`). Falta só validar. |
| SIH: *"`439d4ea` commitado local, SEM push"* | **Está pushado** (`ecohalal/release`). Item fechado. |
| Backlog: WIP do `syshalal-api` na branch `txt_multi_linhas` | Branch **não existe**; o WIP está em `carta-correcao-brf-kuwait`. |
| Backlog 30/jun: *"reconciliação release→develop TODAS feitas"* | Hoje: **3 · 7 · 4 · 4 · 2** pendentes. |
| Backlog 2.1: "front per-Company aberto" × Plano de Ataque: "entregue 28-29/jun" | Contradição interna do próprio doc — **resolver ao tocar FAM-0017**. |
| 7 commits de 16/jul (emissão) | **Nenhum doc os mencionava** antes desta consolidação. |
| §4.2: *"`353a0b79` commit local em `release`, push pendente do OK"* | **Já estava em `origin/release`** — `git branch -r --contains` confirma. Corrigido 17/jul. |
| §5.16: *"SIH **não cadastra planta**"* | **O SIH tem `model Plant` próprio** (39 plantas) e **toda a operação pendura nele** (abate/produção/embarque/NC/supervisor/inventário/escala); a UI **permite cadastrar**. A integração é **read-through de uma via, SEM sync** (o `TASK-07` do schema do SIH diz que o sync GC→SIH é **planejado, não feito**). O §5.16 descreve o **alvo**, não o estado. ⇒ enquanto não houver sync, **os dois cadastros divergem em silêncio** e a junção `SIF+CNPJ` quebra sem avisar. *(Achado 17/jul, cruzando os 2 bancos — foi o que motivou o fallback CNPJ-only.)* |
| §1: *"`sih-docs` — WIP solto **0**"* | Havia **16 caminhos não-rastreados**. Entre eles, **specs que o próprio mestre referencia**: `ITEM-A-B-C-MULTI-ORIGEM-SPEC` + `ITEM-A2` (base do "embarque multi-origem A/B/C" do §4.2/SIH) — o mestre apontava para arquivo **inexistente em git**. Também `ROADMAP-DIRETORIA-2026-07-14` e 20,8 MB de planilhas FM 7.4.2.7 em `Reuniões/` (fonte do F6/N5b/N5c). **Corrigido 17/jul:** 15 docs versionados (`1db4442`); `Reuniões/` → `.gitignore` (`f4223ca`, decisão do Renato: binário pesado segue a política de fontes externas do §8). *(Lição: a auditoria de 16/jul cobriu os repos de **código**; os de **docs** passaram batido — e o mestre vive num deles.)* |

**🚨 Divergência de 30/jul — "pushado" ≠ "em produção" (nova classe de erro; o §0.1 não cobria):**

| Afirmação do doc | Realidade no pipeline |
|---|---|
| §4.3: *"Deploy GC no ar (front `beb0674c`)"* | **NÃO estava.** O pipeline `gestaodecertificacoes_ws_production_pipeline` **falhou** em `81e52f6c` e de novo em `beb0674c`; produção ficou parada em `4af0ad85` (27/jul) por 3 dias. Só voltou a subir em 30/jul com `02c251e0`, que **levou os dois junto**. |
| Implícito: *"front `81e52f6c` (fluxo de aprovação honesto + badge + ver-PDF + fix senha UserForm) entregue"* | **Nunca chegou em produção** até 30/jul. ⇒ **os analistas FAMBRAS (Guilherme/Caio) validaram uma tela SEM esse commit** — parte do retorno de 30/jul pode ser da versão antiga, e front-desatualizado×back-novo é hipótese viva para o "erro ao emitir" do Caio. **Repetir a validação depois deste deploy.** |

**Causa raiz:** `81e52f6c` deixou um import não usado (`CheckCircle2`) em `ManualCertificateEmission.tsx`; o script de build é `tsc -b && vite build`, então **`tsc` derrubou o build inteiro**. Ninguém percebeu porque o commit foi pushado com sucesso — o git aceitou, o pipeline é que recusou. Removido em `02c251e0`.
**Regra que nasce daqui (§0):** *"pushado" não é "entregue"* — **rodar `npx tsc -b` antes de todo push do front** e, quando possível, conferir o pipeline verde. Um `tsc` local de 40s teria evitado 3 dias de deploy parado.

**✅ Alarme falso sobre o `halalsphere-verify-web-pipeline` — CORRIGIDO no mesmo dia.** Registrei que ele "não disparava" porque, minutos após o push de `02c251e0`, o console ainda mostrava a execução de 22 dias antes (`aa418b59`). **Estava errado:** o pipeline rodou normalmente no push seguinte (`27bcb0c8`) e o deploy saiu verde. Verificado por fora, sem depender do console: `GET cert.fambrashalal.com.br/verify/assets/VerifyCertificate-BWem2W5i.js` contém `"Not Issued"`, `"Cancelled Certificate"` e `messageEn` — strings que **só existem no build novo**. ⇒ **Lote 2 entregue nos dois endereços.**
✅ **RESPONDIDO pelo Renato (03/ago) — NÃO há filtro de path.** O pipeline do verify não rodava porque o **pipeline principal estava com erro**; assim que ele voltou ao normal, o do verify disparou sozinho. Ou seja, a causa dos 22 dias parados é a MESMA do deploy travado (o `tsc` derrubado pelo import não usado) — um único defeito segurou os dois. ⇒ nada a configurar; a hipótese do filtro de path fica **descartada**.
**Lição de método (vale mais que o achado):** o console lista o estado *daquele instante* — concluir "não disparou" 2 minutos após um push é ler um retrato antes de a foto revelar. Verificação boa é a de fora: baixar o asset servido e procurar a string nova.

---

## 7. Entregue em 16/jul (esta consolidação)

**GC — emissão:** guard-rail de categorias por modelo `e1f92785` · selos visíveis na tela em vez do código do template `82a5e2c1` · "Voluntária" → "Sem norma acreditada (GSO/SMIIC)" `2844a9c2` · busca por SIF `298a346f`+`49f84b7f` · PDF com cópia bloqueada, AES-128 `6fed9470` · env renomeada p/ `CERTIFICATE_PDF_UNLOCK_KEY` `9bf9bab0`.
**Recuperados (estavam soltos no working tree):** preset de normas por espécie + SIF na tela `827f8adf` · normas FAMBRAS + `.K.` `c396f0a5`.
**Antes (13/jul)** — entregas que não estavam registradas em §4 nem §7 (gap da extração, trazidas pela regra §0.5):
- **GC · fix do filtro de empresa** (`/homologacao-mp`): o X não limpava — `[&_svg]:pointer-events-none` do Button engolia o clique; X e chevron saíram de dentro do gatilho do Popover → `160b2cdd`. 🔧 [Renato] validar (§4.1).
- **GC · Trilha C — Edição de escopo F1** (produtos/marcas na tela de detalhe + auditoria transacional em `CertificationHistory`): back `0f3a9b32` · front `a44f9e49`. 🔧 [Renato] validar (§4.1).
- **SIH · recuperação de senha** (forgot/reset self-service): back `06ae5ef` · front `a24385b`. 🔧 [Renato] validar E2E + SES + migration (§4.1).

**Antes (09/jul):** trava de data `295d274f` · clone typeahead `5de76e6a` · descartar-só-produtos `d5ced92a` · allowlist de selos `ee23628f`.

**Os 4 pontos do André estão fechados** (busca por SIF · guard-rail de categorias · "Voluntária" · templates).

---

## 8. Histórico — handoffs e specs (ponteiro, NÃO verdade)

> ⚠️ Estes arquivos descrevem o momento em que foram escritos. **Vários estão defasados (§6).** Use-os para entender *por que* uma decisão foi tomada — nunca para saber *o que está pronto*.
>
> **Nada foi apagado.** As pendências destes handoffs foram extraídas para o §4 em 16/jul, mas a extração é falível: se você encontrar aqui um item aberto que **não** está no §4, **traga-o para o §4** (regra §0.5). O banner marca "não é estado" — não "não leia".

**Specs (referência técnica — continuam válidas):** **`PLANNING/PAUTA-INGREDIENTES-RESTRITOS-POR-NORMA-2026-08-09`** (halalsphere-docs — **pauta para a Soha declarar**, ~45 aditivos candidatos × as 6 normas, células **vazias de propósito**: a lista é nossa, a decisão é da FAMBRAS. Critério de entrada = status depende da ORIGEM/PROCESSO, não da substância. Traz também o que a tela do Catálogo NÃO resolve — banner W11 preso à cochonilha, variante de DT do FM 4.1.X só para cochonilha — e por que nenhuma posição foi importada de fonte externa: sites de consulta dão **veredito global por aditivo**, e um deles classifica E120 como proibido em todo mercado, contradizendo a posição já declarada pela FAMBRAS. Sem procedência rastreável = achado ISO 17065) · **`ARCHITECTURE/ADR-KERNEL-NORMAS-CERTIFICADO-2026-07-22`** (halalsphere-docs — kernel de normas: snapshot-first + matriz effective-dated + as decisões da Soha 22/jul + fatias 0/1/2 + arquivos a consolidar; = decisão §5.22) · **`PLANNING/EDICAO-ESCOPO-CERTIFICADO-FASE2-REESCOPO-2026-07-23`** (halalsphere-docs — reescopo da C-F2: fronteira A/C por escrito + pacote auditoria/UI; C-F2 EM PROD) · **`ARCHITECTURE/ADR-APROVACAO-CONFECCAO-CERTIFICADO-F2-2026-07-24`** (halalsphere-docs — F2: aprovação da confecção do certificado, rascunho→aprovar→travar; desenho fechado 24/jul, aguarda Fatia A) · **`SPEC-CONFORMIDADE-FM-SIH-2026-07-20`** (sih-docs — cruzamento campo a campo dos FM em papel × SIH, a partir dos formulários oficiais preenchidos e assinados; P0/P1 implementados em 21/jul, §4.2) · `SPEC-EMISSAO-MULTI-CERT-NORMAS-GC-2026-07-14` · `CATEGORIA-POR-NORMA-PICKER-EMISSAO-GC-SPEC-2026-07-08` · `SPEC-EMISSAO-MANUAL-CERTIFICADO-2026-07-06` · `REGRAS-NORMAS-POR-DT-MERCADO-2026-07-06` · `REFERENCIA-TEMPLATES-CERTIFICADO-2026-07-06` · `NORMALIZACAO-CADASTROS-PLANO-MESTRE-2026-07-10` (Anexo A tem os SQL de diagnóstico) · `SYSHALAL-INTEGRATION-ENDPOINT-SIH-SPEC-2026-07-06` (sih-docs) · **`SYNC-CADASTRO-GC-SIH-SPEC-2026-08-03`** (sih-docs — importação assistida de planta do GC para o SIH; **proposta, nada implementado**. Mostra por que "sincronizar tudo" é errado: 820×127 plantas e 3 divergências de modelo — `sanitaryCodeType` 9×6 valores [GC multi-país × SIH BR-only], `plantType` [495 do GC são `outro`], `specialtyArea`→`division` [`ambos` sem representação]. ❓ decisão de timing pendente no §7 da spec) · **`FLUXOGRAMA-RELATORIO-FABRICACAO-MP-2026-06-30`** (sih-docs — fluxo de homologação de MP + travas, Rev 2 com raias por ator; pendências extraídas para o §4.2/SIH em 17/jul).

**Planejamento (vista de execução — snapshot, NÃO estado):** **`PLANNING/GO-LIVE-PLANO-LINHA-CRITICA-2026-07-27.html`** (sih-docs — plano de execução até o go-live com **linha crítica** em 4 fases: fechar comprometidos → congelar escopo → presencial 10-14/ago → go-live; status por frente + decisões que travam código + diferidos. **Rev 28/jul: RECONCILIADO com `ROADMAP-DIRETORIA-2026-07-14` + Gantt** — go-live = marco **15/ago**; adicionado o escopo de go-live que o §4 não rastreava (treinamento operacional 1-14/ago, aditivo contratual, telas Onda 1+ "existe/validar", porta 4/setembro); ⚠️ **divergência sinalizada:** dossiê in natura (roadmap=go-live × §4/§5.21=diferido). Derivado do §4; o §4 continua fonte única de estado. Também publicado como artifact claude.ai. ⚠️ **roadmap público (`roadmap-content.ts`, `updatedAt 13/jul`) está DEFASADO** — não reflete F2/MP crítica/watermark/PT→EN etc.; atualizar em ciclo próprio. **Rev 29/jul — auditoria das telas Onda 1+ (por código, 3 clusters):** ✅ **CENTRAIS COMPLETAS** (wizard de solicitação 11 passos, renovação, "3 modos"=renovar/ampliar/manutenção, inbox do analista=`AnalystDashboard`, análise documental=`PATCH /documents/:id/validate`) — o roadmap 14/jul estava pessimista ("GC front 75%/falta"). ❌ **GAP REAL: aditivo contratual AUSENTE + PDF do contrato base é STUB** (`contract.service.ts` retorna placeholder; botão escondido) — no escopo ago do roadmap → **decisão Renato: precisa no go-live ou difere?** 🟡 secundários com função ok: editor de planilha MP é CRUD (não grade/bulk) · catálogo de laboratórios sem tela front (backend pronto, entra como dropdown) · hub de escopo coberto por inline C-F2 + `/ampliar`).

**Handoffs (histórico):** halalsphere-docs `HANDOFF-SESSAO-2026-07-{08,13,14}` · `HANDOFF-NORMALIZACAO-2026-07-{10,12}` · `EDICAO-ESCOPO-CERTIFICADO-FASE1-HANDOFF-2026-07-13` · `HANDOFF-EMISSAO-MANUAL-2026-07-07` · `HANDOFF-SEED-GC-2026-07-{02}` — sih-docs `HANDOFF-SESSAO-2026-07-14` · `RECUPERACAO-SENHA-SIH-HANDOFF-2026-07-13` · **`ATA-ALINHAMENTO-FAMBRAS-2026-06-30`** (alinhamento dos 3 sistemas: fluxo de homologação de MP, dashboard de qualidade, ciclo embarque⇄cert; pendências extraídas para §4.2/§4.3 em 17/jul).

⚠️ **Gap de registro fechado em 17/jul:** a `ATA-ALINHAMENTO-FAMBRAS-2026-06-30` e o `FLUXOGRAMA-RELATORIO-FABRICACAO-MP-2026-06-30` foram versionados em `1db4442` (o lote dos "15 docs que viviam só na máquina local") mas **nunca foram listados aqui nem tiveram pendências extraídas para o §4** — ficaram invisíveis para o mestre por 17 dias. Mesma classe de falha do §6 (docs passam batido na auditoria de código). *(Regra §0.5 aplicada.)*

**Fontes externas (fora de git):** transcrições em `C:\HalalSphere\Alinhamentos de validação\` · gabaritos em `C:\HalalSphere\Gabaritos atualizados\` · `C:\HalalSphere\FM78x_atualizados\` · docx contratuais em `C:\SIH\` · **Google Doc "SIH - usabilidade"** (Nilsa, `docs.google.com/document/d/1YCGcECIZZNvNb-APufGSgU9QZPxfdixJpetC8nBRlf4`; 9 prints, lido via Drive 21/jul — pendências A/B/C/F extraídas ao §4.2) · **`c:\Projetos\Ecohalal\reunioes_fambras\2007_1100\`** (reunião FAMBRAS 20/jul: transcrição + **FM 4.1.X REV 03** ANEXO 1 [.xlsx, matriz DT×mercado→norma canônica] + DT 7.2.1 aves REV 14 + TXTs de numeração `.N.` aves/bovinos) — insumo do ADR do kernel (§5.22); + thread WhatsApp Soha 22/jul (decisões de norma, não versionado).
